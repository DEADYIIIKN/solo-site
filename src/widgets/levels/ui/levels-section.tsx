import { LevelsSectionBelow1024 } from "@/widgets/levels/ui/levels-section-below-1024";

/** Десктоп: интро + хвост «Уровней» внутри секции «Услуг». Здесь только мобильный полный блок. */
export function LevelsSection() {
  return <LevelsSectionBelow1024 />;
}
