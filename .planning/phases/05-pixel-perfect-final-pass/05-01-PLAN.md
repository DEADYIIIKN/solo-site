---
phase: 05-pixel-perfect-final-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md
  - src/widgets/first-screen/ui/first-screen-1440-section.tsx
  - src/widgets/first-screen/ui/first-screen-hero-1440.tsx
  - src/widgets/business-goals/ui/business-goals.tsx
  - src/widgets/services/ui/services-section-1440.tsx
  - src/widgets/cases/ui/cases-section-1440.tsx
  - src/widgets/team/ui/team-section-1440.tsx
  - src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx
  - src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1440.tsx
  - src/widgets/levels/ui/levels-section-1440.tsx
  - src/widgets/footer/ui/footer-1440.tsx
  - src/widgets/lead-form/ui/lead-form-1440.tsx
autonomous: false
requirements:
  - PX-01

must_haves:
  truths:
    - "SVERKA-REPORT.md содержит минимум по одной строке для каждой секции [hero, business-goals, services, cases, team, philosophy-clients, levels, footer, form] с Breakpoint=1440"
    - "Все локальные (≤30 строк) отклонения на 1440 зафиксированы статусом `fixed` в SVERKA-REPORT.md"
    - "Все отклонения требующие >30 строк имеют статус `deferred` и запись в CONTEXT.md §Deferred Ideas"
    - "Figma MCP sverka на 1440px не показывает деривации сверх tolerance (±1px геометрия; точное совпадение типографики/цветов/ассетов)"
  artifacts:
    - path: ".planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md"
      provides: "Единая таблица sverka с колонками Breakpoint|Section|Type|Figma value|Current code value|File:line|Status"
      contains: "Breakpoint \\| Section \\| Type \\| Figma value \\| Current code value \\| File:line \\| Status"
  key_links:
    - from: ".planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md"
      to: "src/widgets/*/ui/*-1440.tsx"
      via: "File:line references внутри отчёта"
      pattern: "src/widgets/.+-1440\\.tsx:[0-9]+"
---

<objective>
Pixel-perfect sverka всех секций страницы на брейкпоинте **1440px** против Figma листа «Адаптивы актуальные» и фикс всех локальных (≤30 строк) отклонений.

Purpose: Закрыть PX-01. Сделать 1440px соответствующим Figma байт-в-байт по геометрии (±1px), типографике, цветам, теням, ассетам.
Output:
- `.planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md` создан, заполнен строками по всем 9 секциям на 1440px.
- Все локальные отклонения исправлены в `*-1440.tsx` файлах.
- Крупные отклонения (>30 строк) помечены `deferred` и добавлены в CONTEXT.md §Deferred Ideas.
- [BLOCKING] пользователь подтвердил таблицу 1440 перед переходом к следующему wave.
</objective>

<execution_context>
@/Users/a_savinkov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pixel-perfect-final-pass/05-CONTEXT.md
@.planning/phases/02-desktop-layout-1440px-1180px/02-CONTEXT.md

**Figma лист:** «Адаптивы актуальные» (URL в PROJECT.md §Figma).
**Breakpoint mapping:** 1440px → `*-1440.tsx` файлы.
**Секции для sverka (все 9, D-03):** hero, business-goals, services, cases, team, philosophy-clients, levels, footer, lead-form.

**Правила (из 05-CONTEXT.md):**
- D-07: tolerance ±1px для геометрии (strict).
- D-08: типографика (font-size/weight/line-height/letter-spacing/family) — точное совпадение.
- D-09: цвета/border/shadow/border-radius — точные hex/rgb из Figma variables.
- D-10: ассеты (SVG paths, фотографии, иконки) — корректные.
- D-11: sub-pixel rendering — НЕ deviation. Источник истины: число в Figma vs число в коде.
- D-12: discovery + fix в одном wave если правка локальная.
- D-13: fix budget ≤30 строк → иначе `deferred` + запись в CONTEXT.md.
- D-14: если находка требует pattern-wide change → [BLOCKING] checkpoint перед фиксом.
- D-04: sverka ТОЛЬКО static state. Hover/focus/modal/scroll — out of scope.

**Figma MCP workflow (паттерн из Phase 2-3):**
1. Найти ноду по имени секции на листе «Адаптивы актуальные» через `mcp__Figma__get_metadata`.
2. `mcp__Figma__get_screenshot` — визуальный референс.
3. `mcp__Figma__get_variable_defs` / `get_design_context` — точные числа (gap, padding, font-size, color tokens).
4. `preview_start` → `preview_resize` 1440 → `preview_screenshot` секции.
5. Diff Figma value vs code value → строка в отчёте.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Инициализировать 05-SVERKA-REPORT.md</name>
  <files>.planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md</files>
  <read_first>
    - .planning/phases/05-pixel-perfect-final-pass/05-CONTEXT.md §decisions (D-05 — формат отчёта)
  </read_first>
  <action>
Создать `.planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md` со следующей структурой (exact):

```
# Phase 5 — Sverka Report

**Figma sheet:** «Адаптивы актуальные» (см. PROJECT.md §Figma)
**Tolerance:** ±1px геометрия (D-07). Типографика/цвета/ассеты — exact (D-08/D-09/D-10).
**Status legend:** `fixed` | `deferred` | `ok` (нет отклонения)

## Breakpoint 1440px

| Breakpoint | Section | Type | Figma value | Current code value | File:line | Status |
|------------|---------|------|-------------|--------------------|-----------|--------|

## Breakpoint 1180px

| Breakpoint | Section | Type | Figma value | Current code value | File:line | Status |
|------------|---------|------|-------------|--------------------|-----------|--------|

## Breakpoint 820px

| Breakpoint | Section | Type | Figma value | Current code value | File:line | Status |
|------------|---------|------|-------------|--------------------|-----------|--------|

## Breakpoint 360px

| Breakpoint | Section | Type | Figma value | Current code value | File:line | Status |
|------------|---------|------|-------------|--------------------|-----------|--------|

## Deferred Items Summary

| Breakpoint | Section | Reason (>30 lines / pattern-wide) | Tracked in |
|------------|---------|-----------------------------------|-----------|
```

Тип колонки `Type` принимает одно из: `size` | `typography` | `color` | `asset`.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md &amp;&amp; grep -q "Breakpoint | Section | Type | Figma value | Current code value | File:line | Status" .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Breakpoint" 05-SVERKA-REPORT.md` выводит `4`.
    - `grep -c "Breakpoint | Section | Type | Figma value | Current code value | File:line | Status" 05-SVERKA-REPORT.md` выводит `4` (одна шапка на каждый брейкпоинт).
    - Присутствует секция `## Deferred Items Summary`.
  </acceptance_criteria>
  <done>Файл отчёта создан с полной структурой; пустые таблицы готовы принимать строки.</done>
</task>

<task type="auto">
  <name>Task 2: Discovery + Fix — 1440px, секции first-screen/hero, business-goals, services, cases</name>
  <files>
    .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md,
    src/widgets/first-screen/ui/first-screen-1440-section.tsx,
    src/widgets/first-screen/ui/first-screen-hero-1440.tsx,
    src/widgets/business-goals/ui/business-goals.tsx,
    src/widgets/services/ui/services-section-1440.tsx,
    src/widgets/cases/ui/cases-section-1440.tsx
  </files>
  <read_first>
    - src/widgets/first-screen/ui/first-screen-1440-section.tsx (все строки — для inline Figma node ID комментариев)
    - src/widgets/first-screen/ui/first-screen-hero-1440.tsx
    - src/widgets/business-goals/ui/business-goals.tsx (только 1440-ветка; ориентируйся на `useBreakpoint` / `md:`/`lg:`/arbitrary классы типа `1440:`)
    - src/widgets/services/ui/services-section-1440.tsx
    - src/widgets/cases/ui/cases-section-1440.tsx
    - .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md
  </read_first>
  <action>
Для КАЖДОЙ из 4 секций (hero/first-screen, business-goals, services, cases) на 1440px:

1. **Discovery:**
   - `preview_start` → `preview_resize` width=1440.
   - `mcp__Figma__get_metadata` + `get_screenshot` + `get_variable_defs` для секции на листе «Адаптивы актуальные» (ищи по имени секции — паттерн из Phase 2).
   - `preview_screenshot` текущей секции на 1440.
   - Извлечь числа из Figma: `width`, `height`, `gap`, `padding`, `margin`, `font-size`, `font-weight`, `line-height`, `letter-spacing`, `color` (фон/текст/бордер), `border-radius`, `box-shadow`, SVG/asset paths.
   - Сравнить с числами в коде (`className` arbitrary values, inline `style`, CSS variables).
2. **Отчёт:** для каждого обнаруженного отклонения добавить строку в таблицу `## Breakpoint 1440px`:
   `| 1440 | <section> | <size|typography|color|asset> | <figma> | <code> | <file:line> | <status> |`
   Если отклонений нет — добавить одну сводную строку `| 1440 | <section> | — | matches Figma | matches Figma | <main file> | ok |`.
3. **Fix (D-12/D-13):**
   - Если правка локальная и ≤30 строк → править сразу в соответствующем `*-1440.tsx` файле (для business-goals — в `business-goals.tsx` в 1440-ветке).
   - Записать `Status=fixed` в отчёт и обновить `Current code value` на новое значение.
   - Если правка >30 строк или pattern-wide → **НЕ ФИКСИТЬ**. Записать `Status=deferred`, добавить строку в `## Deferred Items Summary`, и добавить запись в CONTEXT.md §Deferred Ideas (формат `- **<section>-1440-<aspect>**: <brief reason>`). При pattern-wide change перед деферралом — подать [BLOCKING] сигнал пользователю (D-14).
4. **Constraints:**
   - Не трогать файлы других брейкпоинтов в этой задаче (1024/768/360/480).
   - Не изменять motion-анимации из Phase 4.
   - Не создавать новые компоненты/файлы.

Важно: inline-комментарии вида `// Figma 783:9521` используй как якорь для `mcp__Figma__get_metadata` по node ID.
  </action>
  <verify>
    <automated>grep -E "^\| 1440 \| (hero|first-screen|business-goals|services|cases) \|" .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md | wc -l | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - В отчёте, в разделе `## Breakpoint 1440px`, присутствует минимум по одной строке для каждой из секций: `first-screen`/`hero`, `business-goals`, `services`, `cases`.
    - Для каждой строки со статусом `fixed`: `grep -n "<new code value>" <file>` возвращает матч, а `grep -n "<old code value>" <file>` — 0 матчей (старое значение заменено).
    - Для каждой строки со статусом `deferred`: в `.planning/phases/05-pixel-perfect-final-pass/05-CONTEXT.md` §Deferred Ideas есть соответствующая запись.
    - Нет модификаций в файлах `*-1024.tsx` / `*-768.tsx` / `*-360.tsx` / `*-480.tsx`: `git diff --name-only | grep -E '\-(1024|768|360|480)\.tsx$'` возвращает пусто.
  </acceptance_criteria>
  <done>4 секции на 1440 покрыты в отчёте; все локальные deviations зафиксированы; крупные deferred задокументированы.</done>
</task>

<task type="auto">
  <name>Task 3: Discovery + Fix — 1440px, секции team, philosophy-clients, levels, footer, lead-form</name>
  <files>
    .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md,
    src/widgets/team/ui/team-section-1440.tsx,
    src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx,
    src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1440.tsx,
    src/widgets/levels/ui/levels-section-1440.tsx,
    src/widgets/footer/ui/footer-1440.tsx,
    src/widgets/lead-form/ui/lead-form-1440.tsx
  </files>
  <read_first>
    - src/widgets/team/ui/team-section-1440.tsx
    - src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx
    - src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1440.tsx
    - src/widgets/levels/ui/levels-section-1440.tsx
    - src/widgets/footer/ui/footer-1440.tsx
    - src/widgets/lead-form/ui/lead-form-1440.tsx
    - .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md
  </read_first>
  <action>
Повторить процесс Task 2 для 5 оставшихся секций на 1440:
- **team** (team-section-1440.tsx)
- **philosophy-clients** (philosophy-clients-1440.tsx + philosophy-clients-marquee-1440.tsx)
- **levels** (levels-section-1440.tsx)
- **footer** (footer-1440.tsx)
- **lead-form** (lead-form-1440.tsx)

Для каждой:
1. Figma MCP discovery (get_metadata → get_screenshot → get_variable_defs).
2. `preview_resize` 1440 → `preview_screenshot`.
3. Diff числа в коде vs Figma.
4. Запись строк в отчёт (D-05 формат).
5. Fix локальных deviations ≤30 строк; deferred + запись в CONTEXT.md для крупных.
6. [BLOCKING] перед pattern-wide fix (D-14).

Те же ограничения: только `*-1440.tsx`; static state only; не ломать motion-анимации.
  </action>
  <verify>
    <automated>grep -E "^\| 1440 \| (team|philosophy-clients|levels|footer|lead-form|form) \|" .planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md | wc -l | awk '{ if ($1 &gt;= 5) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - В разделе `## Breakpoint 1440px` присутствует минимум по одной строке для каждой из секций: `team`, `philosophy-clients`, `levels`, `footer`, `lead-form` (или `form`).
    - Вместе с Task 2 — все 9 секций покрыты на 1440.
    - Для каждой `fixed` строки: grep на new value возвращает матч в `<file>`; grep на old value возвращает 0.
    - Для каждой `deferred` строки: запись присутствует в `## Deferred Items Summary` И в `05-CONTEXT.md §Deferred Ideas`.
    - Нет модификаций в файлах других брейкпоинтов.
  </acceptance_criteria>
  <done>Все 9 секций на 1440 покрыты в отчёте; фиксы применены; крупные отложены.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] User review — таблица 1440 в SVERKA-REPORT</name>
  <what-built>
    - `05-SVERKA-REPORT.md` секция `## Breakpoint 1440px` содержит строки по всем 9 секциям страницы.
    - Локальные deviations исправлены в `*-1440.tsx` файлах.
    - Крупные deviations помечены `deferred` и занесены в CONTEXT.md.
  </what-built>
  <how-to-verify>
    1. Открой `.planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md`.
    2. Проверь раздел `## Breakpoint 1440px` — присутствие строк по [hero, business-goals, services, cases, team, philosophy-clients, levels, footer, lead-form].
    3. Запусти `pnpm dev` (или уже запущенный `preview_start`) и открой `http://localhost:3000` на ширине 1440.
    4. Визуально сравни с Figma листом «Адаптивы актуальные» для каждой `fixed` секции.
    5. Проверь `## Deferred Items Summary` — согласен ли ты с отсрочкой этих правок?
    6. Если всё ок — напиши `approved`. Если нет — перечисли что переделать.
  </how-to-verify>
  <resume-signal>Напиши `approved` или опиши замечания</resume-signal>
</task>

</tasks>

<threat_model>
Security enforcement not applicable for pixel-perfect sverka — phase touches only visual CSS/markup values, no trust boundaries crossed, no data flow changes.
</threat_model>

<verification>
- `05-SVERKA-REPORT.md` раздел 1440 содержит строки по всем 9 секциям.
- Все `fixed` строки: в файле есть новое значение, старого нет.
- Все `deferred` имеют запись в CONTEXT.md §Deferred Ideas.
- User approved в checkpoint.
</verification>

<success_criteria>
Figma MCP sverka на 1440 не находит deviations сверх tolerance (PX-01).
</success_criteria>

<output>
После завершения создать `.planning/phases/05-pixel-perfect-final-pass/05-01-SUMMARY.md`.
</output>
