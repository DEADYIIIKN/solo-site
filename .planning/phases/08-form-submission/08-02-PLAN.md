---
phase: 08-form-submission
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/api/leads/route.ts
  - src/lib/leads/rate-limit.ts
  - src/lib/leads/validation.ts
  - src/lib/leads/forward-webhook.ts
  - .env.example
autonomous: true
requirements:
  - FUNC-01
  - FUNC-02
  - FUNC-04
user_setup:
  - service: n8n
    why: "Webhook URL для форварда заявок (FUNC-01)"
    env_vars:
      - name: N8N_WEBHOOK_URL
        source: "n8n flow → Webhook node → Production URL"
    dashboard_config:
      - task: "Создать n8n flow с Webhook node, ожидающим JSON shape { name, phone, message, consent, contactMethod, source, timestamp }. Скопировать Production URL в .env.local / Docker secret."
        location: "n8n UI → Workflows → новый flow"

must_haves:
  truths:
    - "POST /api/leads с валидным телом возвращает 200 {ok: true}"
    - "Заявка сохраняется в Payload Collection «leads» ДО форварда в n8n (always-save)"
    - "При успешном форварде запись обновлена forwardedToWebhook=true"
    - "При сбое webhook (timeout/non-2xx) запись содержит webhookError, ответ всё равно 200 (D4)"
    - "11+ запросов с одного IP за 5 минут получают 200 {ok:true,accepted:false} но НЕ сохраняются и НЕ форвардятся (D3 compromise)"
    - "Невалидное тело (нет phone / нет consent) → 400"
    - "Если N8N_WEBHOOK_URL не задан — заявка сохраняется в Collection, ответ 200, в логе warn (не падать)"
  artifacts:
    - path: "src/app/api/leads/route.ts"
      provides: "POST handler — validate → rate-limit → save → forward"
      exports: ["POST"]
      min_lines: 80
    - path: "src/lib/leads/rate-limit.ts"
      provides: "in-memory Map<ip, timestamps[]> с TTL 5 мин, лимит 10"
      exports: ["checkRateLimit"]
    - path: "src/lib/leads/validation.ts"
      provides: "zod schema для тела запроса"
      exports: ["leadSchema", "type LeadInput"]
    - path: "src/lib/leads/forward-webhook.ts"
      provides: "fetch к N8N_WEBHOOK_URL с AbortController(10s)"
      exports: ["forwardLeadToWebhook"]
  key_links:
    - from: "src/app/api/leads/route.ts"
      to: "payload.create({ collection: 'leads' })"
      via: "getPayload(...).create — always-save перед форвардом"
      pattern: "collection:\\s*['\"]leads['\"]"
    - from: "src/app/api/leads/route.ts"
      to: "src/lib/leads/forward-webhook.ts"
      via: "forwardLeadToWebhook(payload, leadId, data)"
      pattern: "forwardLeadToWebhook"
    - from: "src/app/api/leads/route.ts"
      to: "src/lib/leads/rate-limit.ts"
      via: "checkRateLimit(ip)"
      pattern: "checkRateLimit"
---

<objective>
Реализовать POST /api/leads — единственный endpoint для submit'а формы. По LOCKED архитектуре из 08-CONTEXT.md (D1-D4): rate-limit → validate → save в Collection (FUNC-02 always-first) → forward в n8n webhook (FUNC-01) → user всегда видит success при сохранённой заявке (D4).

Purpose: реализовать FUNC-01 (доставка заявки в канал), FUNC-02 (локальный fallback), FUNC-04 (server-side rate limit). Client-side debounce — отдельный план 08-03.

Output: route handler + 3 хелпера (rate-limit / validation / webhook forward) + env var в .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-form-submission/08-CONTEXT.md
@.planning/phases/08-form-submission/08-01-SUMMARY.md
@src/payload.config.ts
@.env.example

<interfaces>
<!-- Архитектура (LOCKED в 08-CONTEXT.md § Locked Architecture):

Browser → POST /api/leads
  ├─ rate-limit check (in-memory Map per-IP)
  │   └─ if exceeded: return 200 {ok:true, accepted:false, reason:"rate-limit"} (D4 compromise — НЕ сохранять)
  ├─ validate (zod) — phone/name/consent required
  │   └─ if invalid: return 400
  ├─ payload.create({ collection: "leads", data }) — ALWAYS first
  ├─ forwardLeadToWebhook (timeout 10s)
  │   ├─ if 2xx: payload.update({ collection: "leads", id, data: { forwardedToWebhook: true } })
  │   └─ if fail: payload.update({ ..., data: { webhookError: String(err) } }) — НЕ throw
  └─ return 200 {ok:true}
-->

<!-- Payload Local API в Next.js App Router route handler -->
```typescript
import { getPayload } from "payload";
import config from "@/payload.config";

export async function POST(req: Request) {
  const payload = await getPayload({ config });
  const lead = await payload.create({ collection: "leads", data: { ... } });
}
```

<!-- IP extraction (Traefik forwards x-forwarded-for) -->
```typescript
function getClientIp(req: Request): string {
  const xff = req.headers.get("x-forwarded-for");
  if (xff) return xff.split(",")[0].trim();
  return req.headers.get("x-real-ip") ?? "unknown";
}
```

<!-- Phone validation: переиспользовать isConsultationPhoneValid -->
```typescript
// src/widgets/first-screen/model/first-screen-consultation-form.ts
export function isConsultationPhoneValid(phone: string): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Хелперы — rate-limit, validation, forward-webhook</name>
  <files>
src/lib/leads/rate-limit.ts,
src/lib/leads/validation.ts,
src/lib/leads/forward-webhook.ts
  </files>
  <behavior>
- `checkRateLimit(ip, now)`: возвращает `{ allowed: boolean, retryAfterMs?: number }`. Лимит 10 запросов / 5 мин per IP. Хранилище — module-level `Map<string, number[]>`. На каждый вызов: фильтрует timestamps >= now-300_000, добавляет now, проверяет length <= 10. Чистка старых ключей (опционально — fire-and-forget, не блокирующая).
- `leadSchema` (zod): `name: string min(1)`, `phone: string` с `.refine(isConsultationPhoneValid)`, `message: string optional`, `consent: literal(true)`, `contactMethod: enum(["call","telegram","whatsapp"])`, `source: string min(1)`. Тип `LeadInput = z.infer<typeof leadSchema>`.
- `forwardLeadToWebhook(data, { timeoutMs = 10_000 })`: fetch на `process.env.N8N_WEBHOOK_URL` POST application/json. AbortController через `setTimeout(controller.abort, timeoutMs)`. Body: `{ name, phone, message, consent, contactMethod, source, timestamp: new Date().toISOString() }`. Возвращает `{ ok: boolean, error?: string }`. Если URL не задан → `{ ok: false, error: "N8N_WEBHOOK_URL not configured" }` + console.warn (один раз, через module-level флаг). НЕ throw.
  </behavior>
  <action>
Создать три файла:

**src/lib/leads/rate-limit.ts:**
```typescript
const WINDOW_MS = 5 * 60 * 1000;
const LIMIT = 10;
const buckets = new Map<string, number[]>();

export function checkRateLimit(ip: string, now: number = Date.now()): { allowed: boolean; retryAfterMs?: number } {
  const cutoff = now - WINDOW_MS;
  const prev = (buckets.get(ip) ?? []).filter((t) => t >= cutoff);
  if (prev.length >= LIMIT) {
    const retryAfterMs = WINDOW_MS - (now - prev[0]);
    buckets.set(ip, prev);
    return { allowed: false, retryAfterMs };
  }
  prev.push(now);
  buckets.set(ip, prev);
  return { allowed: true };
}

// Только для тестов
export function __resetRateLimit() {
  buckets.clear();
}
```

**src/lib/leads/validation.ts:**
```typescript
import { z } from "zod";
import { isConsultationPhoneValid } from "@/widgets/first-screen/model/first-screen-consultation-form";

export const leadSchema = z.object({
  name: z.string().min(1, "Имя обязательно"),
  phone: z.string().refine(isConsultationPhoneValid, "Невалидный телефон"),
  message: z.string().optional().default(""),
  consent: z.literal(true, { message: "Требуется согласие" }),
  contactMethod: z.enum(["call", "telegram", "whatsapp"]),
  source: z.string().min(1, "Источник обязателен"),
});

export type LeadInput = z.infer<typeof leadSchema>;
```

**src/lib/leads/forward-webhook.ts:**
```typescript
import type { LeadInput } from "./validation";

let warnedMissingUrl = false;

export async function forwardLeadToWebhook(
  data: LeadInput,
  options: { timeoutMs?: number } = {},
): Promise<{ ok: boolean; error?: string }> {
  const url = process.env.N8N_WEBHOOK_URL;
  if (!url) {
    if (!warnedMissingUrl) {
      console.warn("[leads] N8N_WEBHOOK_URL not configured — заявка сохранена только локально");
      warnedMissingUrl = true;
    }
    return { ok: false, error: "N8N_WEBHOOK_URL not configured" };
  }

  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), options.timeoutMs ?? 10_000);
  try {
    const res = await fetch(url, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ ...data, timestamp: new Date().toISOString() }),
      signal: controller.signal,
    });
    if (!res.ok) {
      return { ok: false, error: `Webhook responded ${res.status}` };
    }
    return { ok: true };
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err) };
  } finally {
    clearTimeout(timeout);
  }
}
```

Если zod не установлен — добавить через `pnpm add zod`. Проверить package.json до установки (возможно уже стоит для других нужд).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -E "src/lib/leads/" || echo "OK"</automated>
  </verify>
  <done>Три файла созданы, типы экспортируются, tsc проходит. zod в dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API route POST /api/leads</name>
  <files>src/app/api/leads/route.ts</files>
  <behavior>
- Валидное тело + не превышен лимит → 200 `{ok:true, accepted:true, leadId}`. Запись в Collection создана. Если webhook ok — `forwardedToWebhook=true`. Если webhook fail — `webhookError` заполнен, ответ всё равно 200 (D4).
- Превышен rate-limit → 200 `{ok:true, accepted:false, reason:"rate-limit"}`. Никакого save и forward (D3 compromise).
- Невалидное тело → 400 `{ok:false, errors: [...]}`.
- БД упала (payload.create throws) → 500 `{ok:false, error:"server"}` (D4: единственный true error).
- IP извлекается из `x-forwarded-for` (первый), fallback `x-real-ip`, fallback "unknown".
  </behavior>
  <action>
Создать `src/app/api/leads/route.ts`:

```typescript
import { getPayload } from "payload";
import config from "@/payload.config";
import { checkRateLimit } from "@/lib/leads/rate-limit";
import { leadSchema } from "@/lib/leads/validation";
import { forwardLeadToWebhook } from "@/lib/leads/forward-webhook";

function getClientIp(req: Request): string {
  const xff = req.headers.get("x-forwarded-for");
  if (xff) return xff.split(",")[0].trim();
  return req.headers.get("x-real-ip") ?? "unknown";
}

export async function POST(req: Request) {
  const ip = getClientIp(req);

  // 1. Rate limit (D3 compromise: возвращаем success-shape, но НЕ сохраняем)
  const rl = checkRateLimit(ip);
  if (!rl.allowed) {
    return Response.json({ ok: true, accepted: false, reason: "rate-limit" }, { status: 200 });
  }

  // 2. Validate
  let body: unknown;
  try {
    body = await req.json();
  } catch {
    return Response.json({ ok: false, error: "invalid-json" }, { status: 400 });
  }
  const parsed = leadSchema.safeParse(body);
  if (!parsed.success) {
    return Response.json(
      { ok: false, errors: parsed.error.flatten() },
      { status: 400 },
    );
  }
  const data = parsed.data;

  // 3. Save to Collection FIRST (FUNC-02 always-save per CONTEXT.md note 2)
  let leadId: string | number;
  try {
    const payload = await getPayload({ config });
    const lead = await payload.create({
      collection: "leads",
      data: {
        name: data.name,
        phone: data.phone,
        message: data.message,
        consent: data.consent,
        contactMethod: data.contactMethod,
        source: data.source,
        forwardedToWebhook: false,
        userIp: ip,
      },
    });
    leadId = lead.id;
  } catch (err) {
    console.error("[leads] Failed to save lead", err);
    return Response.json({ ok: false, error: "server" }, { status: 500 });
  }

  // 4. Forward to n8n (best-effort; ошибка НЕ фейлит запрос — D4)
  const fwd = await forwardLeadToWebhook(data);
  try {
    const payload = await getPayload({ config });
    await payload.update({
      collection: "leads",
      id: leadId,
      data: fwd.ok
        ? { forwardedToWebhook: true }
        : { webhookError: fwd.error ?? "unknown" },
    });
  } catch (err) {
    console.error("[leads] Failed to update lead with webhook status", err);
    // не фейлим — заявка уже сохранена
  }

  return Response.json({ ok: true, accepted: true, leadId }, { status: 200 });
}
```

Если в проекте уже используется alias `@/payload.config` — оставить как есть. Если нет — использовать relative import (проверить tsconfig paths).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -E "api/leads/route" || echo "OK"</automated>
  </verify>
  <done>Route handler существует, экспортирует POST, tsc проходит. Smoke-тест curl-ом (после `pnpm dev`):
`curl -X POST http://localhost:3000/api/leads -H 'Content-Type: application/json' -d '{"name":"тест","phone":"+7 (900) 000-00-00","consent":true,"contactMethod":"call","source":"smoke"}'` → 200 + leadId, запись видна в admin UI «Заявки».</done>
</task>

<task type="auto">
  <name>Task 3: Документировать N8N_WEBHOOK_URL в .env.example</name>
  <files>.env.example</files>
  <action>
В `.env.example` уже есть placeholder секция Phase 8 (строки 26-30) — оставить как есть, она корректна.

Если её нет (на момент исполнения) — добавить в конец файла:

```
# Phase 8 — n8n webhook for lead submissions (FUNC-01).
# Без этой переменной API роут /api/leads сохранит заявку только в Payload Collection «leads»
# (admin UI), пользователь увидит success state (по D4 в 08-CONTEXT.md). Включить, когда n8n
# flow готов — заявки начнут форвардиться в автоматизацию.
# N8N_WEBHOOK_URL=https://n8n.example.com/webhook/UUID
```

Не коммитить реальный URL в репо — только пример.
  </action>
  <verify>
    <automated>grep -q "N8N_WEBHOOK_URL" .env.example && echo "OK"</automated>
  </verify>
  <done>`.env.example` содержит закомментированный пример N8N_WEBHOOK_URL с пояснением.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/leads | Untrusted JSON body (атакующий может слать что угодно) |
| /api/leads → Payload Collection | Доверенная граница (server) |
| /api/leads → N8N_WEBHOOK_URL | Доверенная граница (исходящий fetch к контролируемому URL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | route.ts body parsing | mitigate | zod schema (`leadSchema.safeParse`), non-zod fields игнорируются |
| T-08-02 | Denial of Service | route.ts (spam-flood) | mitigate | `checkRateLimit` 10 req / 5 min per IP (D3) |
| T-08-03 | Information Disclosure | userIp в Collection | accept | Поле помечено в admin description как PII, не отдаётся в публичные эндпоинты, доступ к Collection только авторизованным Payload users |
| T-08-04 | Spoofing | x-forwarded-for header | accept | Trust Traefik (deployed behind reverse proxy); если без прокси — fallback на "unknown", лимит всё равно работает в worst case как глобальный |
| T-08-05 | Denial of Service | webhook timeout вешает route | mitigate | AbortController(10s) в forwardLeadToWebhook |
| T-08-06 | Repudiation | sender claims заявку не отправлял | accept | timestamp + userIp + source в Collection — достаточно для дебага, не judicial-grade |
</threat_model>

<verification>
- `pnpm tsc --noEmit` без ошибок
- `pnpm test:unit` (если существующие тесты есть) — passing (новые тесты в плане 08-04)
- Smoke curl после `pnpm dev` возвращает 200 с leadId, запись видна в admin
- Без N8N_WEBHOOK_URL: smoke curl всё равно 200, Collection содержит webhookError="N8N_WEBHOOK_URL not configured"
</verification>

<success_criteria>
1. POST /api/leads принимает валидное тело и возвращает 200 + leadId
2. Запись создаётся в Collection «leads» ДО форварда
3. forwardedToWebhook обновляется true/false в зависимости от результата n8n
4. Rate-limit отбрасывает 11+ запросов за 5 мин (200 + accepted:false)
5. Невалидное тело → 400
6. БД ошибка → 500
7. zod установлен, tsc проходит
</success_criteria>

<output>
После завершения создать `.planning/phases/08-form-submission/08-02-SUMMARY.md` с архитектурным flow, путями файлов и smoke-test результатами.
</output>
