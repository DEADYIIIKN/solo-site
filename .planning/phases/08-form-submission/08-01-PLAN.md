---
phase: 08-form-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cms/collections/leads.ts
  - src/payload.config.ts
autonomous: false
requirements:
  - FUNC-02
user_setup:
  - service: payload-sqlite
    why: "Применить новую схему Collection «leads» к существующей БД."
    dashboard_config:
      - task: "Запустить `PAYLOAD_DATABASE_PUSH=1 pnpm dev` один раз после регистрации Collection (drizzle push). На проде ensure-скрипт сам применит, если база пустая."
        location: "Локальная dev-среда / прод-деплой"

must_haves:
  truths:
    - "В Payload admin UI появилась группа Collection «Заявки» (slug: leads)"
    - "Запись lead создаётся через payload.create({ collection: 'leads', data }) без ошибок схемы"
    - "Поля name/phone/message/contactMethod/consent/source/forwardedToWebhook/webhookError/userIp доступны в admin"
  artifacts:
    - path: "src/cms/collections/leads.ts"
      provides: "Payload CollectionConfig «leads»"
      contains: "slug: \"leads\""
      min_lines: 60
    - path: "src/payload.config.ts"
      provides: "регистрация Leads в массиве collections"
      contains: "Leads"
  key_links:
    - from: "src/payload.config.ts"
      to: "src/cms/collections/leads.ts"
      via: "import { Leads } from \"./cms/collections/leads.ts\""
      pattern: "collections:.*Leads"
---

<objective>
Создать Payload Collection «leads» как локальный fallback (D2 из 08-CONTEXT.md): любая заявка сохраняется в БД до форварда в n8n. Schema видна в admin UI (группа «Заявки»), экспортируется через Payload UI/API.

Purpose: реализовать FUNC-02 — заявки не теряются даже если n8n webhook отвалится. Все поля формы + meta для рейт-лимит дебага и webhook-статуса.

Output: новый CollectionConfig + регистрация + миграция через PAYLOAD_DATABASE_PUSH=1 (checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-form-submission/08-CONTEXT.md
@src/cms/collections/secrets-post.ts
@src/payload.config.ts

<interfaces>
<!-- Payload CollectionConfig pattern (из secrets-post.ts) -->
```typescript
import type { CollectionConfig } from "payload";

export const Foo: CollectionConfig = {
  slug: "foo",
  labels: { singular: "...", plural: "..." },
  admin: { useAsTitle: "...", group: "...", defaultColumns: [...] },
  access: { read: () => true },
  fields: [
    { name: "...", type: "text" | "textarea" | "checkbox" | "select" | "date", required?: true },
  ],
};
```

Регистрация (payload.config.ts:52):
```typescript
collections: [Users, Media, CasesVertical, CasesAdvertising, SecretsPost],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать Collection «leads»</name>
  <files>src/cms/collections/leads.ts</files>
  <action>
Создать новый CollectionConfig по образцу `src/cms/collections/secrets-post.ts`.

Spec:
- `slug: "leads"`
- `labels`: singular «Заявка», plural «Заявки»
- `admin`: `useAsTitle: "phone"`, `group: "Заявки"`, `defaultColumns: ["phone", "name", "contactMethod", "forwardedToWebhook", "createdAt"]`
- `access`: `{ read: () => true }` (доступ через UI авторизованных пользователей; фронт-чтения нет — Collection пишется только из API route с учётной записью Payload local API)

Поля (порядок ниже = порядок в admin):
1. `name` — type: "text" — required: true — label: «Имя»
2. `phone` — type: "text" — required: true — label: «Телефон» — admin: { description: «Формат: +7 (xxx) xxx-xx-xx» }
3. `message` — type: "textarea" — label: «Сообщение»
4. `contactMethod` — type: "select" — required: true — options: `[{ label: "Позвонить", value: "call" }, { label: "Telegram", value: "telegram" }, { label: "WhatsApp", value: "whatsapp" }]` — defaultValue: "call"
5. `consent` — type: "checkbox" — required: true — defaultValue: false — label: «Согласие на обработку ПД»
6. `source` — type: "text" — label: «Источник» — admin: { description: «header-cta / hero-cta / services-cta / lead-form / consultation-modal» }
7. `forwardedToWebhook` — type: "checkbox" — defaultValue: false — label: «Отправлено в n8n» — admin: { position: "sidebar" }
8. `webhookError` — type: "textarea" — label: «Ошибка webhook» — admin: { position: "sidebar", description: «Заполняется автоматически при сбое n8n» }
9. `userIp` — type: "text" — label: «IP пользователя» — admin: { position: "sidebar", description: «Для дебага рейт-лимита; PII — не показывать публично» }

`createdAt` Payload генерирует автоматически (timestamps включены по умолчанию для всех Collection).

Экспорт: `export const Leads: CollectionConfig = { ... }`. Комментарий-шапка в стиле `secrets-post.ts` (1-2 строки на русском что это).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -E "leads\\.ts" || echo "OK"</automated>
  </verify>
  <done>Файл `src/cms/collections/leads.ts` существует, экспортирует `Leads`, TypeScript компилируется без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: Зарегистрировать Leads в payload.config.ts</name>
  <files>src/payload.config.ts</files>
  <action>
В `src/payload.config.ts`:
1. Добавить импорт рядом с другими collections (по алфавиту между `Media` и `SecretsPost` или в конец списка collections):
   `import { Leads } from "./cms/collections/leads.ts";`
2. В массив `collections:` (строка 52) добавить `Leads` (порядок: после `SecretsPost` или в конец — `[Users, Media, CasesVertical, CasesAdvertising, SecretsPost, Leads]`).

Не трогать другие настройки (livePreview, db, upload).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -E "(payload\\.config|leads)" || echo "OK"</automated>
  </verify>
  <done>`Leads` импортирован и присутствует в `collections:` массиве. tsc проходит.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: применить миграцию через PAYLOAD_DATABASE_PUSH=1</name>
  <what-built>Зарегистрирована новая Collection «leads» в Payload config. Drizzle нужно прогнать через push, чтобы создать таблицу `leads` в SQLite.</what-built>
  <how-to-verify>
1. Остановить текущий `pnpm dev` если запущен.
2. Запустить один раз: `PAYLOAD_DATABASE_PUSH=1 pnpm dev`
3. Дождаться лога Payload «Database is up to date» / отсутствия ошибок drizzle.
4. Открыть admin UI: `http://localhost:3000/admin` → должна появиться секция «Заявки» в сайдбаре.
5. Кликнуть «Создать» в Заявках → форма с полями name / phone / message / contactMethod / consent / source / sidebar поля forwardedToWebhook / webhookError / userIp.
6. Сохранить тестовую запись (name=«test», phone=«+7 (900) 000-00-00», consent=true) → запись появилась в списке.
7. Остановить процесс, перезапустить уже без `PAYLOAD_DATABASE_PUSH=1` (`pnpm dev`) — admin продолжает показывать запись (схема жива).

После проверки удалить тестовую запись через admin UI.
  </how-to-verify>
  <resume-signal>Напиши «approved» если миграция прошла и Collection видна в admin. Если ошибки drizzle — приложи лог.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` — без ошибок
- Admin UI: группа «Заявки» отображается, ручное создание записи работает
- `payload.db` содержит таблицу `leads` (можно проверить через `sqlite3 payload.db ".schema leads"`)
</verification>

<success_criteria>
1. CollectionConfig `Leads` экспортируется из `src/cms/collections/leads.ts`
2. Зарегистрирован в `src/payload.config.ts`
3. Миграция применена (drizzle push), таблица `leads` существует
4. Admin UI рендерит секцию «Заявки» с правильными полями
</success_criteria>

<output>
После завершения создать `.planning/phases/08-form-submission/08-01-SUMMARY.md` со списком созданных файлов, схемой Collection и подтверждением успешной миграции.
</output>
