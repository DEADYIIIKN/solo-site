---
phase: 08-form-submission
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/widgets/lead-form/ui/lead-form-fields.tsx
autonomous: false
requirements:
  - FUNC-01
  - FUNC-03
  - FUNC-04

must_haves:
  truths:
    - "При submit формы выполняется POST к /api/leads с {name,phone,message,consent,contactMethod,source}"
    - "Кнопка submit отключается на ~5с после клика (client-side debounce — D3)"
    - "На 2xx ответ — открывается success modal (existing flow), форма сбрасывается"
    - "На 5xx / network error — показывается error state с возможностью retry (FUNC-03)"
    - "TODO на строке 213 удалён — нет осиротевшего комментария"
    - "Source передаётся в payload (для аналитики) — параметр пропа компонента"
  artifacts:
    - path: "src/widgets/lead-form/ui/lead-form-fields.tsx"
      provides: "fetch к /api/leads + debounce 5s + error UI"
      contains: "/api/leads"
  key_links:
    - from: "src/widgets/lead-form/ui/lead-form-fields.tsx"
      to: "/api/leads"
      via: "fetch в onSubmit"
      pattern: "fetch\\([^)]*['\"]\\/api\\/leads"
    - from: "lead-form-fields.tsx onSubmit"
      to: "submitting state"
      via: "useState + setTimeout 5000"
      pattern: "setSubmitting"
---

<objective>
Заменить TODO на строке 213 в `src/widgets/lead-form/ui/lead-form-fields.tsx` на реальный submit к `/api/leads` (создан в 08-02). Добавить client-side debounce 5с (D3) и error state с retry (FUNC-03). На 2xx — existing success modal. На 5xx — error UI.

Purpose: завершить связку «форма ↔ API», реализовать FUNC-01 на клиенте, FUNC-03 (success/error states), FUNC-04 (client-side debounce).

Output: единственный модифицированный файл — все 5 точек открытия (header CTA / hero CTA / services CTA / lead-form section / consultation modals) автоматически получают рабочий submit, потому что handler централизован (см. 08-CONTEXT § Codebase Scout).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-form-submission/08-CONTEXT.md
@.planning/phases/08-form-submission/08-02-SUMMARY.md
@src/widgets/lead-form/ui/lead-form-fields.tsx

<interfaces>
<!-- Текущий submit handler (строки 209-217): -->
```typescript
onSubmit={(e) => {
  e.preventDefault();
  setSubmitAttempted(true);
  if (!name.trim() || !isConsultationPhoneValid(phone) || !consent) return;
  /* TODO: отправка заявки */
  setSuccessOpen(true);
  setFormState(defaultFirstScreenConsultationFormState);
  setSubmitAttempted(false);
}}
```

<!-- API contract (из 08-02) -->
```typescript
POST /api/leads
Body: { name, phone, message, consent: true, contactMethod, source }
Response 200: { ok: true, accepted: boolean, leadId? }
Response 400: { ok: false, errors }
Response 500: { ok: false, error: "server" }
```

<!-- Existing props (line 150-161) -->
```typescript
{ density, contactLayout, className, embedInCard }: {
  density: LeadFormFieldsDensity;
  contactLayout: "radio" | "pill";
  className?: string;
  embedInCard?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetch к /api/leads + debounce + error state в lead-form-fields.tsx</name>
  <files>src/widgets/lead-form/ui/lead-form-fields.tsx</files>
  <action>
Изменения в `src/widgets/lead-form/ui/lead-form-fields.tsx`:

**1. Добавить proп `source` (для аналитики, передаётся в API):**

В сигнатуру компонента (line 150-161):
```typescript
export function LeadFormFields({
  density,
  contactLayout,
  className,
  embedInCard = false,
  source,
}: {
  density: LeadFormFieldsDensity;
  contactLayout: "radio" | "pill";
  className?: string;
  embedInCard?: boolean;
  /** Источник заявки для аналитики: "header-cta" | "hero-cta" | "services-cta" | "lead-form" | "consultation-modal" */
  source: string;
})
```

Это breaking change для всех вызовов — при tsc будут ошибки. Найти все usages:
```bash
grep -rn "<LeadFormFields" src/
```
Для каждого добавить `source="..."` исходя из контекста (имя секции / модалки):
- `<HeaderConsultationModal>` или подобное в шапке → `source="header-cta"`
- Hero / first-screen → `source="hero-cta"`
- Services CTA → `source="services-cta"`
- Lead-form section → `source="lead-form"`
- Универсальный ConsultationModalBase → `source="consultation-modal"` (или принимать source как proп ConsultationModalBase и пробрасывать)

Если ConsultationModalBase используется в нескольких местах — добавить `source: string` proп туда же и передавать.

**2. Добавить state для submitting / error:**

Рядом с другими useState (line 167-168):
```typescript
const [submitting, setSubmitting] = useState(false);
const [submitError, setSubmitError] = useState<string | null>(null);
```

**3. Заменить onSubmit:**

```typescript
onSubmit={async (e) => {
  e.preventDefault();
  setSubmitAttempted(true);
  if (!name.trim() || !isConsultationPhoneValid(phone) || !consent) return;
  if (submitting) return;
  setSubmitting(true);
  setSubmitError(null);
  // Client-side debounce (D3): защита от двойного клика; разлочиваем через 5с минимум
  const debounceTimer = setTimeout(() => setSubmitting(false), 5000);
  try {
    const res = await fetch("/api/leads", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        name: name.trim(),
        phone,
        message,
        consent,
        contactMethod,
        source,
      }),
    });
    if (res.ok) {
      // 2xx — success даже если accepted:false (rate-limit) per D4 compromise
      setSuccessOpen(true);
      setFormState(defaultFirstScreenConsultationFormState);
      setSubmitAttempted(false);
    } else {
      // 4xx/5xx — true error UX (D4: только 500 реально показывается)
      setSubmitError("Не удалось отправить заявку. Попробуйте ещё раз.");
    }
  } catch {
    setSubmitError("Проблема со связью. Попробуйте ещё раз.");
  } finally {
    clearTimeout(debounceTimer);
    setSubmitting(false);
  }
}}
```

**4. Кнопка submit — disabled state + текст:**

В `<button data-testid="lead-form-submit" ...>` (line 496-506):
```typescript
<button
  data-testid="lead-form-submit"
  disabled={submitting}
  className={cn(
    "flex w-full items-center justify-center rounded-[50px] border-0 bg-[#ff5c00] px-10 font-semibold lowercase text-white transition-colors hover:bg-[#de4f00]",
    "disabled:cursor-not-allowed disabled:opacity-60",
    d.btn,
    d.btnH,
  )}
  type="submit"
>
  {submitting ? "отправляем…" : "оставить заявку"}
</button>
```

**5. Error UI — inline над кнопкой:**

Между блоком consent (line 494) и кнопкой (line 496) вставить:
```tsx
{submitError && (
  <p
    role="alert"
    data-testid="lead-form-error"
    className={cn("m-0 text-[#e63a24] leading-[1.2]", d.consent)}
  >
    {submitError}
  </p>
)}
```

Stylistic note: использовать существующий error-color `#e63a24` (как у name/phone error).

**6. Удалить старый TODO** — он автоматически удалён заменой onSubmit (строка 213 «TODO: отправка заявки» больше не существует).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -E "lead-form-fields|LeadFormFields" || echo "OK"</automated>
  </verify>
  <done>
- `lead-form-fields.tsx` содержит fetch("/api/leads")
- proп `source: string` обязательный, все usages обновлены
- Кнопка disabled при submitting
- Error UI рендерится при submitError
- TODO удалён (grep "TODO: отправка заявки" → нет матчей)
- tsc проходит без ошибок
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: ручная проверка submit flow в браузере</name>
  <what-built>Submit handler заменён на реальный fetch к /api/leads с debounce 5с, success/error UI, source-аналитикой.</what-built>
  <how-to-verify>
1. `pnpm dev` (без `PAYLOAD_DATABASE_PUSH=1` — миграция уже применена в 08-01).
2. Открыть `http://localhost:3000` → выбрать любую точку (header CTA, hero CTA, services CTA, lead-form секция, consultation modal).
3. **Happy path:** заполнить name + phone (`+7 (900) 000-00-00`) + consent → submit → success modal открылась, форма сброшена.
4. Открыть `http://localhost:3000/admin` → «Заявки» → новая запись существует с правильным `source` (header-cta / hero-cta / etc. в зависимости от точки), `forwardedToWebhook=true` если N8N_WEBHOOK_URL задан, иначе `webhookError`.
5. **Debounce:** на чистой форме заполнить → нажать submit и сразу же снова кликнуть кнопку → второй клик не должен ничего делать (кнопка disabled, текст «отправляем…»).
6. **Rate-limit (опционально):** через DevTools Network → Repeat request 11 раз подряд → последние ответы должны быть `accepted:false, reason:"rate-limit"` но статус 200, success modal всё равно открывается.
7. **Error path (моки в spec — план 08-04):** если хочется проверить руками — временно сломать `N8N_WEBHOOK_URL=http://localhost:1` (refused) → submit → success state (потому что save в Collection прошёл). Чтобы увидеть error UI — временно throw в payload.create в API route → submit → красная плашка «Не удалось отправить...».

После проверки — вернуть N8N_WEBHOOK_URL / API route в нормальное состояние.
  </how-to-verify>
  <resume-signal>Напиши «approved» если happy path и debounce работают, заявка появилась в admin. Если что-то отвалилось — приложи лог консоли и Network tab.</resume-signal>
</task>

</tasks>

<verification>
- Смоук в браузере: submit с любой точки CTA создаёт запись в Payload Collection «Заявки»
- В Collection корректный `source` per точку входа
- Кнопка submit реально отключается на 5с
- Error UI появляется только при 5xx (D4 compromise)
- `pnpm tsc --noEmit` без ошибок
</verification>

<success_criteria>
1. TODO на строке ~213 удалён, вместо него — fetch к /api/leads
2. Все вызовы `<LeadFormFields>` имеют обязательный proп `source`
3. Submit-кнопка disabled на ~5с после клика (D3 client-side)
4. На 2xx — success modal (existing); на 5xx — error inline + retry возможен (FUNC-03)
5. Заявки реально доходят до Payload (вид в admin) — FUNC-01 + FUNC-02 проверены end-to-end
</success_criteria>

<output>
После завершения создать `.planning/phases/08-form-submission/08-03-SUMMARY.md` с описанием изменений в lead-form-fields.tsx и списком updated callsites.
</output>
