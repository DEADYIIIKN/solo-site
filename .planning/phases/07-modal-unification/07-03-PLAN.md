---
phase: 07-modal-unification
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx
autonomous: true
requirements:
  - REFAC-01
  - REFAC-02
  - REFAC-03
must_haves:
  truths:
    - "Каждый из 5 файлов first-screen-consultation-modal-XXX.tsx содержит ≤20 строк кода и состоит только из re-export wrapper'а вокруг ConsultationModal"
    - "Все 13 callsite-импортов FirstScreenConsultationModalXXX продолжают работать без изменений"
    - "Visual & behavior parity на всех 5 breakpoints — pnpm exec playwright test tests/e2e/consultation-modal.spec.ts проходит без правок"
    - "pnpm exec playwright test tests/e2e/cross-browser.spec.ts проходит (smoke render на chromium + webkit + mobile-safari)"
    - "Submit handler shape unchanged — TODO в lead-form-fields.tsx и форме модалки не трогаем (D4)"
  artifacts:
    - path: "src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx"
      provides: "Thin wrapper: <ConsultationModal variant=\"1440\" {...props} />"
      contains: "variant=\"1440\""
    - path: "src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx"
      provides: "Thin wrapper: <ConsultationModal variant=\"360\" {...props} />"
      contains: "variant=\"360\""
  key_links:
    - from: "src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx"
      to: "src/widgets/first-screen/ui/consultation-modal.tsx"
      via: "delegation в base component"
      pattern: "<ConsultationModal\\s+variant="
    - from: "src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx"
      to: "src/widgets/first-screen/ui/consultation-modal.tsx"
      via: "delegation в base component"
      pattern: "<ConsultationModal\\s+variant="
---

<objective>
Конвертировать 5 существующих файлов модалок в thin wrapper'ы, делегирующие весь рендер в `ConsultationModal` из Plan 07-02. Это **критический шаг** big-bang миграции (D3) — после этого PR старые 2433 строки исчезают.

**Per CONTEXT.md D5 (visual parity guard):** после конверсии **обязательный** прогон E2E + cross-browser. Любое падение — заблокировать commit и завести issue.

**Output:** Каждый из 5 файлов сводится к ≤20 строкам:
```tsx
"use client";
import { ConsultationModal } from "./consultation-modal";
import type { /* prop types */ } from "@/widgets/first-screen/model/first-screen-consultation-form-state";
import type { Dispatch, SetStateAction } from "react";

type Props = { open: boolean; onClose: () => void; formState: ...; setFormState: ...; titleVariant?: ... };

export function FirstScreenConsultationModal1440(props: Props) {
  return <ConsultationModal variant="1440" {...props} />;
}
```

**Соответствие locked decisions:** D1 (используем variant prop), D3 (big-bang за один атомарный коммит), D4 (submit shape без изменений), D5 (E2E + cross-browser в verify).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-modal-unification/07-CONTEXT.md
@.planning/phases/07-modal-unification/07-01-SUMMARY.md
@.planning/phases/07-modal-unification/07-02-SUMMARY.md
@src/widgets/first-screen/ui/consultation-modal.tsx
@src/widgets/first-screen/ui/consultation-modal-variants.ts
@src/widgets/first-screen/model/first-screen-consultation-form-state.ts

<interfaces>
<!-- Public API ConsultationModal (от Plan 07-02) -->

```typescript
import type { Dispatch, SetStateAction } from "react";
import type {
  FirstScreenConsultationFormState,
  FirstScreenConsultationModalTitleVariant,
} from "@/widgets/first-screen/model/first-screen-consultation-form-state";
import type { ConsultationModalVariant } from "./consultation-modal-variants";

export type ConsultationModalProps = {
  variant: ConsultationModalVariant;
  open: boolean;
  onClose: () => void;
  formState: FirstScreenConsultationFormState;
  setFormState: Dispatch<SetStateAction<FirstScreenConsultationFormState>>;
  titleVariant?: FirstScreenConsultationModalTitleVariant;
};

export function ConsultationModal(props: ConsultationModalProps): JSX.Element | null;
```

<!-- Существующие public exports (которые НЕЛЬЗЯ менять) -->

```typescript
// Все 5 файлов экспортируют named функцию с этой сигнатурой:
export function FirstScreenConsultationModal{1440|1024|768|480|360}(props: {
  open: boolean;
  onClose: () => void;
  formState: FirstScreenConsultationFormState;
  setFormState: Dispatch<SetStateAction<FirstScreenConsultationFormState>>;
  titleVariant?: FirstScreenConsultationModalTitleVariant;
}): JSX.Element | null;
```

13 callsites импортируют по этим именам — должны остаться рабочими.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Конвертировать modal-1440 в thin wrapper (источник правды + проверка)</name>
  <files>src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx</files>
  <action>
**Полностью переписать содержимое файла** (нынешние 569 строк → ~20 строк):

```tsx
"use client";

import type { Dispatch, SetStateAction } from "react";

import type {
  FirstScreenConsultationFormState,
  FirstScreenConsultationModalTitleVariant,
} from "@/widgets/first-screen/model/first-screen-consultation-form-state";
import { ConsultationModal } from "@/widgets/first-screen/ui/consultation-modal";

type FirstScreenConsultationModal1440Props = {
  open: boolean;
  onClose: () => void;
  formState: FirstScreenConsultationFormState;
  setFormState: Dispatch<SetStateAction<FirstScreenConsultationFormState>>;
  titleVariant?: FirstScreenConsultationModalTitleVariant;
};

export function FirstScreenConsultationModal1440(
  props: FirstScreenConsultationModal1440Props,
) {
  return <ConsultationModal variant="1440" {...props} />;
}
```

**ВАЖНО (от Plan 07-01):** modal-1440 содержит временные re-exports types/const для backwards compat. **Эти re-exports ещё не удаляем** — это сделает Plan 07-04 (cleanup), когда подтвердится что никто из них не импортирует. Сохраняем их в конце нового файла **точно** так же:

```tsx
// Re-exports для обратной совместимости. Удалить в Plan 07-04 после конверсии всех импортов.
// (содержимое из Plan 07-01)
export type {
  FirstScreenConsultationContactMethod,
  FirstScreenConsultationFormState,
  FirstScreenConsultationModalTitleVariant,
} from "@/widgets/first-screen/model/first-screen-consultation-form-state";
export { defaultFirstScreenConsultationFormState } from "@/widgets/first-screen/model/first-screen-consultation-form-state";
```

После записи: `pnpm exec tsc --noEmit` + сразу прогнать E2E (в Task 2/3 будет финальный прогон по всем модалкам).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; wc -l src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx | awk '$1 &lt;= 30 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>Файл ≤30 строк, экспортирует FirstScreenConsultationModal1440 как thin wrapper, re-exports types сохранены, tsc проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Конвертировать остальные 4 модалки (1024/768/480/360) в thin wrappers</name>
  <files>
    src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx
  </files>
  <action>
Для **каждого** из 4 файлов — полностью заменить содержимое на thin wrapper по шаблону Task 1, с правильным variant.

**Пример для 360:**

```tsx
"use client";

import type { Dispatch, SetStateAction } from "react";

import type {
  FirstScreenConsultationFormState,
  FirstScreenConsultationModalTitleVariant,
} from "@/widgets/first-screen/model/first-screen-consultation-form-state";
import { ConsultationModal } from "@/widgets/first-screen/ui/consultation-modal";

type FirstScreenConsultationModal360Props = {
  open: boolean;
  onClose: () => void;
  formState: FirstScreenConsultationFormState;
  setFormState: Dispatch<SetStateAction<FirstScreenConsultationFormState>>;
  titleVariant?: FirstScreenConsultationModalTitleVariant;
};

export function FirstScreenConsultationModal360(
  props: FirstScreenConsultationModal360Props,
) {
  return <ConsultationModal variant="360" {...props} />;
}
```

Аналогично для 480, 768, 1024 (поменять только variant и имя exported функции / типа Props).

**Эти 4 файла НЕ содержат re-exports** (типы они импортировали в Plan 07-01). Просто thin wrapper.

После записи всех 4 — sanity grep:
```bash
grep -c "ConsultationModal variant=" src/widgets/first-screen/ui/first-screen-consultation-modal-{360,480,768,1024,1440}.tsx
```
Должно быть 5 строк, каждая с count=1.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; for f in src/widgets/first-screen/ui/first-screen-consultation-modal-{360,480,768,1024}.tsx; do test "$(wc -l &lt; "$f")" -le 25 || { echo "$f &gt; 25 lines"; exit 1; }; done</automated>
  </verify>
  <done>Все 4 файла ≤25 строк, каждый — thin wrapper с правильным variant. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Visual + behavior parity guard (E2E + cross-browser)</name>
  <files></files>
  <action>
**КРИТИЧЕСКИЙ visual parity guard (CONTEXT.md D5).** После Task 1+2 в рантайме теперь работает unified ConsultationModal во ВСЕХ breakpoints. До коммита нужно подтвердить отсутствие регрессий.

Запустить **в указанном порядке**, не пропускать:

1. **Type-check + lint:**
   ```bash
   pnpm exec tsc --noEmit
   pnpm exec eslint src/widgets/first-screen/ui
   ```

2. **E2E behavior spec (главный гард):**
   ```bash
   pnpm exec playwright test tests/e2e/consultation-modal.spec.ts --reporter=line
   ```
   Должно пройти **без правок spec'а** (REFAC-02 требование).

3. **Cross-browser smoke (chromium + webkit + mobile-safari):**
   ```bash
   pnpm exec playwright test tests/e2e/cross-browser.spec.ts --reporter=line
   ```
   Если падает только на каком-то одном браузере — высокая вероятность что breakpoint variant отрисовался не там, где должен (layerVisibility class неточно скопирован). Проверить `consultation-modal-variants.ts` против каждого старого файла.

4. **Unit tests (на всякий — phone format / validation):**
   ```bash
   pnpm exec vitest run --reporter=verbose phone-format lead-form-validation
   ```

5. **Sanity grep — никто не импортирует удалённую разметку:**
   ```bash
   grep -rn "first-screen-consultation-modal-1440\"" src/ | grep -v "from.*consultation-modal-1440\"$"
   ```
   Должны остаться только импорты `FirstScreenConsultationModal1440` named export — никаких импортов внутренних helpers (CloseIcon, contactOptions, MODAL_TRANSITION_MS), которые исчезли при конверсии.

**Если что-то падает:** НЕ коммитить. Diagnose-fix-retry. В summary зафиксировать что упало и как пофиксили.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm exec playwright test tests/e2e/consultation-modal.spec.ts tests/e2e/cross-browser.spec.ts --reporter=line</automated>
  </verify>
  <done>
- E2E consultation-modal.spec.ts passing на main config.
- cross-browser.spec.ts passing на chromium / webkit / mobile-safari.
- Total LOC reduction: ~2433 → ~120 (5 thin wrappers + base + variants), подтверждается через `wc -l src/widgets/first-screen/ui/{consultation-modal,consultation-modal-variants,first-screen-consultation-modal-*}.{ts,tsx}`.
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` — zero errors
2. `pnpm exec eslint src/widgets/first-screen/ui` — clean
3. `pnpm exec playwright test tests/e2e/consultation-modal.spec.ts` — passing
4. `pnpm exec playwright test tests/e2e/cross-browser.spec.ts` — passing
5. `pnpm exec vitest run` — all unit tests passing
6. `wc -l src/widgets/first-screen/ui/first-screen-consultation-modal-*.tsx` — каждый файл ≤30 строк
7. Manual smoke на dev: открыть `/` на 1440 viewport, кликнуть консультация-CTA — модалка открылась, форма работает, success step плавно появляется. (Полная Figma sverka — Plan 07-04.)
</verification>

<success_criteria>
- 5 файлов first-screen-consultation-modal-XXX.tsx сократились до thin wrappers (≤30 строк, по сути ≤20 кода).
- 13 callsite импортов работают без изменений.
- E2E + cross-browser specs zero regression.
- Submit handler shape без изменений (D4 honored).
- Visual parity preserved на всех 5 breakpoints (Playwright это покрывает; manual Figma sverka — Plan 07-04).
</success_criteria>

<output>
После выполнения создать `.planning/phases/07-modal-unification/07-03-SUMMARY.md`. Зафиксировать:
- Финальный LOC reduction (точные цифры до/после).
- Любые E2E/cross-browser падения которые потребовали правок variant table — diagnose + fix log.
- Sanity-grep результаты (никто не импортирует исчезнувшие internal helpers).
- Ready-state для Plan 07-04 (cleanup + Figma sverka).
</output>
