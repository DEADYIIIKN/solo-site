---
phase: 09-lead-form-pixel-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/lead-form/ui/lead-form-fields.tsx
  - src/widgets/lead-form/ui/lead-form-below-1024.tsx
autonomous: false
requirements:
  - LF-DRIFT-01
tags:
  - lead-form
  - pixel-perfect
  - mobile
  - figma-sverka

must_haves:
  truths:
    - "На 360px все элементы lead-form (heading, «Это бесплатно», bullets, name input, tel input, contact-method buttons, textarea, consent, submit) имеют y-coord в пределах ±2px от Figma 783:10314"
    - "На 480px все элементы lead-form имеют y-coord в пределах ±2px от Figma 783:10873"
    - "На 1440px lead-form pixel-perfect не регрессит — y-coord ключевых элементов не меняется vs 05-SVERKA-REPORT"
    - "На 1180px (1024 layout) lead-form не регрессит"
    - "На 820px (768 layout) lead-form не регрессит"
    - "x-coord всех элементов lead-form остаётся EXACT match Figma на всех breakpoints (не должен сдвигаться от y-fix)"
    - "E2E spec tests/e2e/lead-form.spec.ts остаётся зелёным (happy path / consent / submit / error states)"
  artifacts:
    - path: "src/widgets/lead-form/ui/lead-form-fields.tsx"
      provides: "Targeted gap/padding/leading правки для density=below1024|360|480 без затрагивания density=1440|1024|768"
      contains: "density === \"below1024\" || density === \"360\" || density === \"480\""
    - path: ".planning/phases/09-lead-form-pixel-cleanup/09-01-SUMMARY.md"
      provides: "Отчёт о targeted fix: что измерили (Figma vs DOM до/после), что изменили, regression результаты"
  key_links:
    - from: "src/widgets/lead-form/ui/lead-form-below-1024.tsx"
      to: "LeadFormFields с density='below1024' embedInCard"
      via: "<LeadFormFields contactLayout='pill' density='below1024' embedInCard ... />"
      pattern: "density=\"below1024\""
    - from: "src/widgets/lead-form/ui/lead-form-fields.tsx"
      to: "density-conditional gap classes"
      via: "cn() с density === '480' || density === '360' guards"
      pattern: "density === \"(below1024|480|360)\""
---

<objective>
Закрыть LF-DRIFT-01 — D-19 carryover из v1.0. На 360px и 480px breakpoints
накопленный y-drift в shared `LeadFormFields` достигает +51..+77px от Figma values
(Figma 783:10314 / 783:10873). Цель — свести drift к ±2px на 360/480 БЕЗ регрессии
на 1440 / 1180 / 820 (где сейчас pixel-perfect).

Purpose: Закрыть последний открытый pixel-perfect долг из v1.0 milestone, чтобы lead-form
на mobile breakpoints соответствовал Figma как и desktop.

Output: Targeted edits в `lead-form-fields.tsx` (и опционально `lead-form-below-1024.tsx`)
с density-guarded conditions, чтобы fix применялся только на below1024/360/480.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pixel-perfect-final-pass/05-SVERKA-REPORT.md

@src/widgets/lead-form/ui/lead-form-fields.tsx
@src/widgets/lead-form/ui/lead-form-below-1024.tsx
@src/widgets/lead-form/ui/lead-form-bullets.tsx

<interfaces>
<!-- Ключевые контракты, которые executor не должен пере-открывать в коде. -->

`LeadFormFieldsDensity` (lead-form-fields.tsx:48-55):
```ts
export type LeadFormFieldsDensity =
  | "1440" | "1024" | "768" | "480" | "360" | "below1024";
```

`LeadFormFields` props (lead-form-fields.tsx:150-167):
```ts
{
  density: LeadFormFieldsDensity;
  contactLayout: "radio" | "pill";
  className?: string;
  embedInCard?: boolean;
  source: string;
}
```

Текущие mobile-only ветки (которые МОЖНО менять):
- form-level outer: `density === "below1024"` → `"w-full max-w-[520px] gap-6 p-6 max-[767px]:max-w-none max-[479px]:gap-6 max-[479px]:p-4"` (line 213-214)
- header gap: `density === "768" || density === "480" || density === "360" ? "gap-[10px]" : "gap-[12px]"` (line 261-263)
- middle column gap: `embedInCard ? "gap-6" : density === "1440" ? "gap-[30px]" : "gap-6"` (line 279-283)

Desktop ветки (НЕ ТРОГАТЬ — pixel-perfect на 1440/1180/820):
- `density === "1440"` — `min-h-[608px] gap-[30px] p-[30px]`
- `density === "1024"` — `h-[550px] px-6 pb-9 pt-6`
- `density === "768"` — `px-[20px] pb-[24px] pt-[20px]`
</interfaces>

**Figma reference values (из 05-SVERKA-REPORT):**

360px (Figma 783:10314, frame 360×1049):
- name input: abs(32, 488) 296×38
- tel input: abs(32, 550) 296×38
- channel: abs(32, 613) 296×36
- textarea: abs(32, 683) 296×80
- checkbox: abs(32, 787) 296×34
- submit: abs(32, 845) 296×44

Текущий drift на 360 (после v1.0 partial fix):
- name (32, 494) +6, tel +13, textarea +46, submit (32, 896) +51
- section h: 1126 vs Figma 1049 → +35 (после partial gap-6→gap-3 + divider 30→12px)

480px (Figma 783:10873, frame 480×1170):
- heading: abs(48, 110) 384×48 ✓ EXACT
- name: abs(48, 553) 384×40
- tel: abs(48, 617) 384×40
- textarea: abs(48, 775) 384×100
- submit: abs(48, 948) 384×52

Текущий drift на 480:
- name (48, 600) +47, tel +54, textarea +73, submit (48, 1025) +77
- section h: 1247 vs Figma 1170 → +77 cumulative

x EXACT для всех на обоих breakpoints — трогать только y (gap / leading / padding / margin).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discovery — собрать текущие y-coord и сравнить с Figma на 360 + 480</name>
  <files>
    Read-only: src/widgets/lead-form/ui/lead-form-fields.tsx, src/widgets/lead-form/ui/lead-form-below-1024.tsx
    Output (notes only — НЕ файл): инлайн в Task 2 plan execution log
  </files>
  <action>
    Используя Figma MCP и Chrome MCP (или Playwright snapshot):
    1. Открыть локальный dev-сервер (`pnpm dev`) на http://localhost:3000.
    2. Прокрутить до lead-form section (id="lead-form-section-mobile").
    3. На viewport 360×800: измерить getBoundingClientRect() для каждого элемента:
       - heading (LeadFormTitle), «Это абсолютно бесплатно» p, «Мы покажем:» p, bullets-stacked
       - dark card top
       - lead-form-name input
       - lead-form-phone input
       - contact-method buttons row (первая кнопка)
       - lead-form-message textarea
       - lead-form-consent label (checkbox + text)
       - lead-form-submit button
    4. Повторить на viewport 480×900.
    5. Сравнить с Figma values из <context> блока. Зафиксировать deltas (current.y - figma.y) для каждого элемента в обеих секциях.
    6. Идентифицировать ИСТОЧНИК cumulative drift по областям:
       - heading-блок (LeadFormTitle + «Это бесплатно» + bullets) — drift внутри light section
       - dark card padding (pt-4 / pt-6)
       - LeadFormFields: gap между header «Это бесплатно» и middle column (line 192/193)
       - LeadFormFields middle column gap-6 (line 283, 289) — между rows
       - textarea height (h-[80px] / h-[100px])
       - consent block gap (line 474)
       - submit button btnH (min-h-[44px])

    Записать findings в комментарий для Task 2 — какой именно элемент / класс отвечает за каждый Δ>2px.

    Важно: НЕ менять файлы в Task 1. Только observation.
  </action>
  <verify>
    <automated>MISSING — discovery task, выход = inline notes для Task 2. Проверка: executor зафиксировал deltas для всех элементов на 360 и 480 в structured table format (element / figma.y / current.y / delta).</automated>
  </verify>
  <done>
    Таблица deltas (10+ элементов × 2 breakpoints) с указанием источника drift (CSS class / line). Готов план targeted правок для Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Targeted fix — density-guarded правки для below1024/360/480</name>
  <files>
    src/widgets/lead-form/ui/lead-form-fields.tsx
    src/widgets/lead-form/ui/lead-form-below-1024.tsx (если нужно — wrapper-уровневые spacings: pt-6, divider h-[12px], dark-card padding)
  </files>
  <action>
    На основе findings Task 1, применить ТОЧЕЧНЫЕ изменения для устранения drift на 360 и 480.

    **Жёсткие constraints:**
    1. Любой новый/изменённый CSS класс должен быть guarded условием density === "below1024" || density === "360" || density === "480" (или max-[767px]: prefix внутри below1024 ветки).
       НЕЛЬЗЯ менять классы которые активны при density === "1440" | "1024" | "768".
    2. Если правка касается lead-form-below-1024.tsx — она должна быть в max-[479px]: или max-[767px]: prefix зоне (НЕ в min-[480px]: и НЕ в default — это касается только 480-767 диапазона иногда, осторожно).
    3. x-positions НЕ трогать (контейнер padding px-4 на 360 / px-6 на 480 — фиксирован Figma и matches).
    4. Сохранить структуру компонента — это targeted prop/class adjustment, не редизайн.

    **Кандидаты на правку (приоритет по источнику drift из Task 1):**
    - Outer form gap для below1024: `gap-6` → попробовать меньше (`gap-4` / `gap-3`) под `max-[479px]:` и `max-[767px]:` prefixes
    - Middle column gap (line 279-283 lead-form-fields.tsx): для below1024 текущий `gap-6` — возможно нужен `max-[479px]:gap-4` / `max-[767px]:gap-5`
    - Inner row gap (line 288-289): аналогично — но это column flex на mobile, не critical
    - leadUnderlinePadding `pb-[20px] pt-[10px]` — это shared, но Figma underline pad на mobile может быть меньше; если меняем — guard через wrapper или через density param в helper
    - textarea label `pb-[30px] pt-[10px]` (line 451) — на mobile может быть pb-[20px]
    - btnH `min-h-[44px]` для 360/480 — Figma 360: h=44 ✓, Figma 480: h=52 → ПРОВЕРИТЬ что 480 использует 52, не 44 (сейчас оба mobile = 44)
    - Bullets / heading gap внутри light section (lead-form-below-1024.tsx line 44, 64, 76)
    - Divider height `h-[12px] min-[480px]:h-[40px]` (line 94) — Figma 480: gap-40 уже корректно; 360 partial fixed на 12

    **Подход — итеративный:**
    1. Применить наиболее вероятные правки (top-3 источника drift из Task 1).
    2. После каждой группы правок — re-measure через DOM (refresh + getBoundingClientRect).
    3. Когда delta для всех элементов на 360 и 480 ≤ ±2px — STOP. Не over-fix.

    **Документировать в коде:**
    Каждый density-guarded класс снабдить inline-комментарием Figma reference:
    ```tsx
    /* Figma 783:10314 (360) — name y=488, tel y=550 → gap=24 between rows */
    "max-[479px]:gap-[24px]"
    ```

    **Special check — 480 submit btnH:** если сейчас `min-h-[44px]` для density="480" но Figma submit h=52 — добавить density-conditional: `density === "480" ? "min-h-[52px]" : "min-h-[44px]"`. Аналогично для below1024 — `min-[480px]:min-h-[52px]` под max-[767px]: zone.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/lead-form.spec.ts --reporter=list</automated>
  </verify>
  <done>
    - lead-form-fields.tsx изменён только в density === "below1024"|"360"|"480" ветках (grep подтверждает: каждое изменение имеет density guard или max-[479px]:|max-[767px]: prefix)
    - lead-form.spec.ts E2E spec проходит зелёным
    - На 360 viewport DOM-измерения каждого элемента имеют delta ≤ ±2px от Figma 783:10314
    - На 480 viewport delta ≤ ±2px от Figma 783:10873
    - Section h на 360 ≤ 1051 (Figma 1049 + tolerance), на 480 ≤ 1172 (Figma 1170 + tolerance)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regression sverka — 1440 / 1180 / 820 + visual diff на 360 / 480</name>
  <what-built>
    Targeted y-drift fix в lead-form-fields.tsx + lead-form-below-1024.tsx с density-guards для below1024/360/480.
    Все изменения изолированы — НЕ должны затрагивать desktop layouts.
  </what-built>
  <how-to-verify>
    1. Запустить dev-сервер: `pnpm dev`.
    2. Открыть http://localhost:3000 в Chrome DevTools.
    3. **Regression check (desktop — должно matches как было в v1.0):**
       - Viewport 1440×900: lead-form section — визуально идентично Figma 783:9081 (sverka v1.0).
         Особо проверить: form card 470×608, inputs y={488..540}, contact buttons row, submit btn 470×59 — НЕ изменились.
       - Viewport 1180×900: lead-form (layout="1024" — lead-form-1024.tsx) — Figma 783:8366.
         Form card h=550, inputs, submit 466×56 — НЕ изменились.
       - Viewport 820×900: lead-form (layout="768" — lead-form-768.tsx) — Figma 783:11522.
         Form card 360×wide, submit 360×52 — НЕ изменились.
    4. **Target check (mobile — должно теперь matches Figma):**
       - Viewport 480×900: lead-form (lead-form-below-1024.tsx) — Figma 783:10873.
         Heading y=110, name y=553, tel y=617, textarea y=775, submit y=948, section h=1170. Все ±2px.
       - Viewport 360×800: lead-form (lead-form-below-1024.tsx) — Figma 783:10314.
         name y=488, tel y=550, textarea y=683, submit y=845, section h=1049. Все ±2px.
    5. **Visual quick-check:** open Figma 783:10314 + 783:10873 в соседних tabs, side-by-side compare с DevTools screenshots — ничего не «съехало» в горизонталь, тёмная карточка edge-to-edge ровная, gap'ы чистые.
    6. **E2E green:** `pnpm exec playwright test tests/e2e/lead-form.spec.ts` — зелёное.

    Если хотя бы один из 1440/1180/820 регрессировал — ABORT, вернуться в Task 2 и пере-guard класс который зацепил desktop.
  </how-to-verify>
  <resume-signal>Type "approved" если все 5 breakpoints совпадают с Figma в пределах ±2px и E2E зелёный. Иначе перечисли regressions.</resume-signal>
</task>

</tasks>

<verification>
- Grep: каждое изменение в lead-form-fields.tsx имеет density guard для below1024|360|480 ИЛИ находится в max-[479px]:/max-[767px]: prefix зоне
- Grep НЕ находит изменений в density === "1440"|"1024"|"768" ветках кода
- DOM measurements на 360: все элементы delta ≤ ±2px от Figma 783:10314
- DOM measurements на 480: все элементы delta ≤ ±2px от Figma 783:10873
- DOM measurements на 1440 / 1180 / 820: НЕ изменились vs 05-SVERKA-REPORT
- Playwright E2E lead-form.spec.ts: passes
</verification>

<success_criteria>
1. На 360 и 480 viewports y-drift каждого элемента lead-form ≤ ±2px от Figma values
2. На 1440 / 1180 / 820 sverka не регрессировала vs v1.0 results
3. tests/e2e/lead-form.spec.ts зелёный (нет регрессии в behavior)
4. Все изменения isolated через density guards — никаких side-effects на desktop
</success_criteria>

<output>
После завершения создать `.planning/phases/09-lead-form-pixel-cleanup/09-01-SUMMARY.md` с:
- Таблица deltas до/после на 360 и 480 (element / figma.y / before.y / after.y / delta)
- Список изменённых строк lead-form-fields.tsx + lead-form-below-1024.tsx с Figma references
- Regression results на 1440 / 1180 / 820 (no-change confirmation)
- Закрытие LF-DRIFT-01 в REQUIREMENTS.md (отметить ✅)
</output>
