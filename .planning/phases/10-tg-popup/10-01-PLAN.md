---
phase: 10-tg-popup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/use-activity-timer.ts
  - tests/unit/use-activity-timer.test.tsx
autonomous: true
requirements:
  - TG-01
  - TEST-07
must_haves:
  truths:
    - "useActivityTimer вызывает onElapsed ровно через durationMs накопленной активности"
    - "Если 30+ секунд нет активных событий — accumulator паузится (idle gate)"
    - "Если document.visibilityState === 'hidden' — accumulator паузится (visibility gate)"
    - "При unmount все слушатели и interval очищаются (no leaks)"
  artifacts:
    - path: src/shared/lib/use-activity-timer.ts
      provides: "Хук useActivityTimer(durationMs, onElapsed) — accumulator timer с idle/visibility gate"
      contains: "export function useActivityTimer"
      min_lines: 60
    - path: tests/unit/use-activity-timer.test.tsx
      provides: "Unit-тесты для useActivityTimer (TEST-07)"
      contains: "describe(\"useActivityTimer\""
      min_lines: 80
  key_links:
    - from: tests/unit/use-activity-timer.test.tsx
      to: src/shared/lib/use-activity-timer.ts
      via: "import { useActivityTimer }"
      pattern: "from \"@/shared/lib/use-activity-timer\""
---

<objective>
Создать testable foundation для TG-popup: хук `useActivityTimer(durationMs, onElapsed)` с idle/visibility-gating, плюс unit-тесты (TEST-07).

Purpose: Изолированно протестировать логику таймера до интеграции в UI (D1 locked: 60s активности с idle 30s + visibility gate).
Output: `src/shared/lib/use-activity-timer.ts` + `tests/unit/use-activity-timer.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-tg-popup/10-CONTEXT.md
@src/shared/lib/use-viewport-layout.ts
@tests/unit/use-in-view-once.test.tsx
@tests/unit/setup.ts

<interfaces>
<!-- Существующий паттерн client-хуков в проекте (use-viewport-layout.ts) -->

```typescript
// src/shared/lib/use-viewport-layout.ts
"use client";
import { useEffect, useState } from "react";
// useEffect-only side-effects, listener add/remove в return cleanup
```

<!-- Существующая инфраструктура unit-тестов: vitest + jsdom (см. tests/unit/setup.ts) -->
<!-- Команда: npm test -- tests/unit/use-activity-timer.test.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Реализовать useActivityTimer хук с тестами (RED → GREEN)</name>
  <files>src/shared/lib/use-activity-timer.ts, tests/unit/use-activity-timer.test.tsx</files>
  <behavior>
    - Test 1: вызывает onElapsed после durationMs если активность есть на каждом tick (mousemove)
    - Test 2: НЕ вызывает onElapsed если 30+ секунд нет событий (idle gate)
    - Test 3: НЕ вызывает onElapsed если document.visibilityState === "hidden"
    - Test 4: cleanup при unmount — нет leaked listeners / intervals (vi.clearAllTimers + проверка removeEventListener calls)
    - Test 5: после повторной активности после idle — таймер продолжает с того же места (accumulator не сбрасывается, только паузится)
  </behavior>
  <action>
    Создать `src/shared/lib/use-activity-timer.ts` (per D1 locked):

    ```typescript
    "use client";
    import { useEffect, useRef } from "react";

    const ACTIVITY_EVENTS = ["scroll", "mousemove", "keydown", "touchstart", "pointermove"] as const;
    const IDLE_THRESHOLD_MS = 30_000;
    const TICK_MS = 1_000;

    export function useActivityTimer(durationMs: number, onElapsed: () => void): void {
      const onElapsedRef = useRef(onElapsed);
      onElapsedRef.current = onElapsed;

      useEffect(() => {
        if (typeof window === "undefined") return;

        let accumulatedMs = 0;
        let lastActivityAt = Date.now();
        let fired = false;

        const markActivity = () => { lastActivityAt = Date.now(); };

        for (const evt of ACTIVITY_EVENTS) {
          window.addEventListener(evt, markActivity, { passive: true });
        }

        const intervalId = window.setInterval(() => {
          if (fired) return;
          if (document.visibilityState === "hidden") return;
          const idleMs = Date.now() - lastActivityAt;
          if (idleMs >= IDLE_THRESHOLD_MS) return;
          accumulatedMs += TICK_MS;
          if (accumulatedMs >= durationMs) {
            fired = true;
            onElapsedRef.current();
          }
        }, TICK_MS);

        return () => {
          window.clearInterval(intervalId);
          for (const evt of ACTIVITY_EVENTS) {
            window.removeEventListener(evt, markActivity);
          }
        };
      }, [durationMs]);
    }
    ```

    Создать `tests/unit/use-activity-timer.test.tsx` через `@testing-library/react` (renderHook) + `vi.useFakeTimers()`:
    - Использовать `vi.advanceTimersByTime` для прокрутки tick'ов.
    - Для Date.now() мокать через `vi.setSystemTime()`.
    - Для idle симулировать "нет активности" — после initial tick прокрутить >30s без dispatchEvent.
    - Для visibility — `Object.defineProperty(document, "visibilityState", { value: "hidden", configurable: true })`.
    - Для активности — `window.dispatchEvent(new Event("mousemove"))` периодически.

    RED: написать тесты первыми → GREEN: реализовать хук → тесты проходят.
  </action>
  <verify>
    <automated>npm test -- tests/unit/use-activity-timer.test.tsx --run</automated>
  </verify>
  <done>
    Файл `src/shared/lib/use-activity-timer.ts` экспортирует `useActivityTimer`, все 5 unit-тестов проходят, нет TypeScript ошибок.
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/unit/use-activity-timer.test.tsx --run` — все тесты passing
- `npx tsc --noEmit` — без ошибок типов на новом файле
- Хук экспортируется из правильного пути (импорт из `@/shared/lib/use-activity-timer` работает)
</verification>

<success_criteria>
- [x] useActivityTimer хук реализован per D1 (events: scroll/mousemove/keydown/touchstart/pointermove, idle 30s, visibility gate, 1s tick)
- [x] Unit-тесты покрывают: happy path, idle gate, visibility gate, cleanup, accumulator persistence — TEST-07 closed
- [x] Все тесты проходят на vitest + jsdom
- [x] Cleanup полный (нет leaked listeners/intervals)
</success_criteria>

<output>
After completion, create `.planning/phases/10-tg-popup/10-01-SUMMARY.md` (artifact, behaviors covered, test commands).
</output>
