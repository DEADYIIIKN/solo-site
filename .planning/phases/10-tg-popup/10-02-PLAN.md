---
phase: 10-tg-popup
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/widgets/tg-popup/ui/tg-popup-variants.ts
  - src/widgets/tg-popup/ui/tg-popup.tsx
  - src/widgets/tg-popup/index.ts
autonomous: true
requirements:
  - TG-02
  - TG-03
  - TG-04
must_haves:
  truths:
    - "TgPopup рендерит per-breakpoint (5 вариантов) overlay с заголовком, body, CTA «Подписаться», крестиком ✕"
    - "Dismiss работает через ✕, click по overlay, ESC keydown — все три триггерят onDismiss"
    - "CTA кнопка ведёт на href из props, target=\"_blank\", rel=\"noopener noreferrer\""
    - "Open/close транзишн через opacity (consistency с ConsultationModal pattern)"
  artifacts:
    - path: src/widgets/tg-popup/ui/tg-popup-variants.ts
      provides: "Variant lookup для 5 breakpoints (1440/1024/768/480/360) с config из Figma"
      contains: "export const tgPopupVariants"
      min_lines: 80
    - path: src/widgets/tg-popup/ui/tg-popup.tsx
      provides: "TgPopup base component (overlay + dismiss handlers + CTA)"
      contains: "export function TgPopup"
      min_lines: 100
    - path: src/widgets/tg-popup/index.ts
      provides: "Barrel exports для widget"
      contains: "export"
      min_lines: 1
  key_links:
    - from: src/widgets/tg-popup/ui/tg-popup.tsx
      to: src/widgets/tg-popup/ui/tg-popup-variants.ts
      via: "import { tgPopupVariants, type TgPopupVariant }"
      pattern: "tgPopupVariants\\["
    - from: src/widgets/tg-popup/ui/tg-popup.tsx
      to: "props.ctaHref (внешняя env var URL)"
      via: "<a href={ctaHref} target=\"_blank\" rel=\"noopener noreferrer\">"
      pattern: "target=\"_blank\""
---

<objective>
Создать UI shell pop-up'а: variant lookup table (5 breakpoints из Figma) + TgPopup base component с dismiss handlers (✕ / overlay click / ESC) и CTA-кнопкой «Подписаться».

Purpose: Закрыть TG-02 (per-breakpoint design) и TG-03 (dismiss flow) и TG-04 (CTA на env var URL). Pure UI слой — без timer/sessionStorage логики (это в 10-03).
Output: `src/widgets/tg-popup/{ui/tg-popup-variants.ts, ui/tg-popup.tsx, index.ts}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-tg-popup/10-CONTEXT.md
@src/widgets/first-screen/ui/consultation-modal.tsx
@src/widgets/first-screen/ui/consultation-modal-variants.ts
@src/shared/lib/utils.ts

<interfaces>
<!-- Существующий паттерн variant lookup (ConsultationModal — Phase 7) -->
<!-- Шаблон для копирования: layerVisibility, outerPadding, columnItems, maxWidth, columnGap, closeIconSize, cardPadding, cardGap, titleSize -->

```typescript
// src/widgets/first-screen/ui/consultation-modal-variants.ts
export type ConsultationModalVariant = "360" | "480" | "768" | "1024" | "1440";
// 1440: max-w-[686px], cardPadding p-[30px], titleSize 40, closeIconSize 34
// 1024: max-w-[616px], cardPadding p-[30px], titleSize 38, closeIconSize 34
// 768:  max-w-[504px], cardPadding p-[24px], titleSize 32, closeIconSize 30
// 480:  max-w-[480px], cardPadding px-[20px] py-[24px], titleSize 30, closeIconSize 28
// 360:  max-w-[360px], cardPadding px-[16px] py-[24px], titleSize 23, closeIconSize 24
```

<!-- ConsultationModal паттерн structure (overlay + ESC handler + click-outside + transition) -->
<!-- Ключевые хуки: useEffect для ESC keydown listener, useState для shouldRender/isEntered transition gating -->
<!-- createPortal в document.body для overlay z-layer -->
</interfaces>

<figma>
Node-ids от пользователя (D3 locked) — researcher / executor определяет mapping breakpoint <-> node через Figma MCP `mcp__figma-dev-mode-mcp-server__get_code` с node-id:
- 783:9762
- 783:9750
- 783:9729
- 783:9708
- 783:9687

Извлечь: max-width / padding / heading font-size / body font-size / button height / icon sizes / colors. Заполнить в `tgPopupVariants[variant]`.
</figma>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Извлечь Figma config + создать tg-popup-variants.ts</name>
  <files>src/widgets/tg-popup/ui/tg-popup-variants.ts</files>
  <action>
    Шаги:
    1. Через Figma MCP (`mcp__figma-dev-mode-mcp-server__get_code` с `nodeId: "783:9762"` и т.д. для всех 5 нод) извлечь dimensions / colors / typography для каждой ноды.
    2. Определить mapping node-id → breakpoint (1440/1024/768/480/360) по width контейнера в Figma (или по очевидным размерам — крестик 34px = desktop, 24px = 360).
    3. Создать `src/widgets/tg-popup/ui/tg-popup-variants.ts` с экспортами:

    ```typescript
    export type TgPopupVariant = "360" | "480" | "768" | "1024" | "1440";

    export type TgPopupVariantConfig = {
      layerVisibility: string;        // "min-[1440px]:block" etc.
      outerPadding: string;            // overlay inner padding
      columnItems: string;             // "items-end" / "items-center"
      maxWidth: string;                // "max-w-[Npx]"
      columnGap: string;               // gap между крестиком и карточкой
      closeIconSize: 24 | 28 | 30 | 34;
      cardPadding: string;             // padding карточки
      cardGap: string;                 // gap внутри карточки (между секциями)
      titleSize: number;               // px, inline style
      titleLayout: "stacked" | "inline"; // как в ConsultationModal
      bodyTextSize: string;            // tailwind class "text-[Npx]"
      ctaButtonHeight: string;         // "h-[Npx]"
      ctaFontSize: number;             // px
      // если в Figma есть TG-icon / preview блоки — добавить поля iconSize, previewLayout
    };

    export const tgPopupVariants: Record<TgPopupVariant, TgPopupVariantConfig> = {
      "360":  { ... },
      "480":  { ... },
      "768":  { ... },
      "1024": { ... },
      "1440": { ... },
    };
    ```

    Все классы статические (Tailwind 4 JIT). Если Figma имеет дополнительные элементы (TG icon, preview постов) — расширить config соответствующими полями (iconSize, previewLayout) и зафиксировать в типе.

    Ориентир для пропорций (взять как baseline до уточнения через Figma MCP):
    - 1440: max-w-[480px], cardPadding p-[40px], titleSize 32, closeIconSize 34
    - 1024: max-w-[440px], cardPadding p-[36px], titleSize 28, closeIconSize 30
    - 768:  max-w-[400px], cardPadding p-[28px], titleSize 24, closeIconSize 28
    - 480:  max-w-[400px], cardPadding p-[24px], titleSize 22, closeIconSize 26
    - 360:  max-w-[328px], cardPadding p-[16px], titleSize 20, closeIconSize 24

    После получения данных из Figma — заменить baseline точными значениями.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Файл существует, экспортирует `TgPopupVariant`, `TgPopupVariantConfig`, `tgPopupVariants` для всех 5 breakpoints. TypeScript компилируется. Все node-id'шки из Figma посещены и значения внесены (не baseline).
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать TgPopup component + barrel export</name>
  <files>src/widgets/tg-popup/ui/tg-popup.tsx, src/widgets/tg-popup/index.ts</files>
  <action>
    Создать `src/widgets/tg-popup/ui/tg-popup.tsx` (`"use client"`):

    Props:
    ```typescript
    export type TgPopupProps = {
      variant: TgPopupVariant;
      open: boolean;
      onDismiss: () => void;
      ctaHref: string;            // NEXT_PUBLIC_TG_CHANNEL_URL — passed from host
      title?: string;              // default из Figma copy
      body?: string;               // default из Figma copy
      ctaLabel?: string;           // default "Подписаться"
    };
    ```

    Структура (скопировать паттерн из `consultation-modal.tsx`):
    - `createPortal(layer, document.body)` для overlay
    - `useState(shouldRender)` + `useState(isEntered)` для transition gating (320ms opacity)
    - `useEffect` для ESC keydown listener (вызывает onDismiss)
    - `useEffect` для body overflow:hidden во время open
    - Backdrop button (`absolute inset-0 bg-[#0d0300]/80`, onClick=onDismiss)
    - Card container с `tgPopupVariants[variant]` config:
      - Крестик ✕ button (CloseIcon copy из ConsultationModal — простой SVG path "M9 9l16 16M25 9L9 25")
      - Heading (titleSize, titleLayout)
      - Body text (bodyTextSize)
      - CTA `<a href={ctaHref} target="_blank" rel="noopener noreferrer" onClick={onDismiss}>` стилизованный как rounded-[50px] bg-[#ff5c00] кнопка (стиль consult submit-button)
    - data-testids: `tg-popup-root`, `tg-popup-close`, `tg-popup-cta`

    После клика по CTA — также вызвать onDismiss (чтобы не показалось снова).

    Текст по умолчанию (D4 locked, до уточнения Figma):
    - title: "Подписывайтесь на наш TG-канал"
    - body: "Обзоры съёмок, кейсы и закадровая жизнь SOLO Production"
    - ctaLabel: "Подписаться"

    Если Figma имеет иной copy — researcher вносит точные строки в default props.

    Создать `src/widgets/tg-popup/index.ts`:
    ```typescript
    export { TgPopup, type TgPopupProps } from "./ui/tg-popup";
    export { tgPopupVariants, type TgPopupVariant } from "./ui/tg-popup-variants";
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint -- --quiet src/widgets/tg-popup</automated>
  </verify>
  <done>
    `TgPopup` рендерит overlay + card на всех 5 variants без рантайм-ошибок. Все 3 dismiss триггера вызывают `onDismiss`. CTA имеет `target="_blank" rel="noopener noreferrer"`. Barrel index.ts экспортирует TgPopup + tgPopupVariants.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — без ошибок
- `npm run lint -- --quiet src/widgets/tg-popup` — без warnings
- Manual smoke: импортировать TgPopup в storybook-like тестовую страницу или напрямую в layout (открыть с `open={true}` хардкодом) → визуально совпадает с Figma на 1 breakpoint
</verification>

<success_criteria>
- [x] tg-popup-variants.ts покрывает все 5 breakpoints с точными значениями из Figma (5 node-ids посещены)
- [x] TgPopup рендерит overlay + card корректно — TG-02 satisfied
- [x] Dismiss через ✕ / overlay click / ESC — все три работают, onDismiss вызывается — TG-03 satisfied
- [x] CTA имеет href из props + target="_blank" + rel="noopener noreferrer" — TG-04 satisfied (URL подаётся хостом из env var)
- [x] Transitions consistent с ConsultationModal pattern (opacity 320ms cubic-bezier)
</success_criteria>

<output>
After completion, create `.planning/phases/10-tg-popup/10-02-SUMMARY.md` (variant config snapshot, dismiss test results).
</output>
