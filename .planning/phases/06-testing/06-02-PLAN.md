---
phase: 06-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - playwright.config.ts
  - tests/e2e/cross-browser.spec.ts
  - tests/e2e/carousel-services.spec.ts
  - tests/e2e/carousel-cases.spec.ts
autonomous: false
requirements:
  - TEST-02

must_haves:
  truths:
    - "playwright.config.ts расширен projects: chromium + webkit (Safari) + Mobile Safari"
    - "tests/e2e/cross-browser.spec.ts: проверка main page load (hero, all sections render) на Chromium + WebKit"
    - "tests/e2e/carousel-services.spec.ts: arrows next/prev, slide change на Chromium + WebKit на 360/480/820"
    - "tests/e2e/carousel-cases.spec.ts: vertical+ad carousels arrows на Chromium + WebKit"
    - "`npm run test:e2e` проходит на всех projects"
  artifacts:
    - path: "playwright.config.ts"
      provides: "config с 3+ projects (chromium / webkit / mobile-safari)"
      contains: "projects.*webkit"
    - path: "tests/e2e/cross-browser.spec.ts"
      provides: "Smoke test main page load multi-browser"
      contains: "test\\(.*page load"
  key_links:
    - from: "tests/e2e/carousel-*.spec.ts"
      to: "src/widgets/{services,cases}/ui/*.tsx (carousel components)"
      via: "DOM selectors"
      pattern: "page\\.(get|locator)"
---

<objective>
Расширить Playwright config до multi-browser (Chromium + WebKit + Mobile Safari), покрыть main page load и carousel interactions cross-browser тестами.

Purpose: Закрыть TEST-02 — cross-browser regression coverage.
Output:
- Multi-project Playwright config
- Smoke spec для main page (4 viewport sizes × 2 browsers)
- Carousel specs (services + cases)
- (Если CI) Cross-browser matrix в workflow
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/06-testing/06-01-PLAN.md
</context>

<tasks>
1. **Extend Playwright config** — добавить projects `webkit`, `mobile-safari` (devices['iPhone 13']).
2. **Create cross-browser smoke** — main page load assertion: hero visible, business-goals/team/philosophy/cases/services/levels/lead-form/footer all rendered. Tests на 1440 + 360 viewports.
3. **Carousel services spec** — на 360/480/820 viewports: click next arrow, assert slide indicator change OR scroll position. Click prev. Verify arrow disabled states at boundaries.
4. **Carousel cases spec** — vertical и ad carousels. Same pattern.
5. **Run on all browsers** — `npx playwright test --project=chromium,webkit`. Fix any browser-specific issues (Safari uses different focus/scroll behavior).
</tasks>
