---
phase: 06-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - tests/e2e/lead-form.spec.ts
  - tests/e2e/cases-form.spec.ts
  - tests/e2e/_fixtures.ts
autonomous: false
requirements:
  - TEST-01

must_haves:
  truths:
    - "playwright.config.ts создан и валиден"
    - "Скрипт `npm run test:e2e` запускает Playwright"
    - "tests/e2e/lead-form.spec.ts покрывает: ввод name+phone+message, toggle consent checkbox, submit click, успешное завершение submission flow"
    - "tests/e2e/cases-form.spec.ts покрывает аналогичный flow внутри case-modals"
    - "Все тесты проходят локально (`npm run test:e2e` exit code 0)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright config — baseURL=http://localhost:3000, projects chromium-only для wave 1"
      contains: "baseURL"
    - path: "tests/e2e/lead-form.spec.ts"
      provides: "E2E spec для главной lead-form"
      contains: "test.describe.*lead-form"
    - path: "tests/e2e/cases-form.spec.ts"
      provides: "E2E spec для case-modal forms"
      contains: "test.describe.*cases"
  key_links:
    - from: "tests/e2e/lead-form.spec.ts"
      to: "src/widgets/lead-form/ui/lead-form-1440.tsx (selector)"
      via: "DOM data-testid или role/label"
      pattern: "page\\.(get|locator)"
---

<objective>
Setup Playwright E2E framework + покрыть form submission flow тестами для главной lead-form (1440/1024/мобайл) и case-modal forms.

Purpose: Закрыть TEST-01 — automated E2E coverage form interactions.
Output:
- playwright.config.ts с базовой конфигурацией
- 2 spec файла с проверкой submission flow
- npm script `test:e2e` для локального запуска
- (Если CI настроен) — Playwright workflow в .github/workflows
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
1. **Setup Playwright config** — создать `playwright.config.ts` с baseURL `http://localhost:3000`, project `chromium`, retries=0 локально, webServer auto-start dev.
2. **Add npm scripts** — `test:e2e`, `test:e2e:ui`, `test:e2e:debug` в package.json.
3. **Create lead-form spec** — selectors через data-testid либо `getByRole('textbox', { name })`. Cover: fill name/phone/message → toggle consent → click submit → assert success state.
4. **Create cases-form spec** — открыть case modal, fill form, submit, assert closed/success.
5. **Add data-testid attrs** в lead-form/case-modal components если нужно для стабильных selectors.
6. **Run `npm run test:e2e`** локально — verify pass.
</tasks>
