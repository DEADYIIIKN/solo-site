---
phase: 04-safari-animations
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - src/shared/ui/boneyard-skeleton.tsx
  - src/bones/registry.js
  - src/bones/cases-ad-card-1024.bones.json
  - src/bones/cases-ad-card-1440.bones.json
  - src/bones/cases-ad-card-360.bones.json
  - src/bones/cases-ad-card-480.bones.json
  - src/bones/cases-ad-card-768.bones.json
  - src/bones/cases-vertical-card-1024.bones.json
  - src/bones/cases-vertical-card-1440.bones.json
  - src/bones/cases-vertical-card-360.bones.json
  - src/bones/cases-vertical-card-480.bones.json
  - src/bones/cases-vertical-card-768.bones.json
  - src/bones/philosophy-team-card-1024.bones.json
  - src/bones/philosophy-team-card-1440.bones.json
  - src/bones/philosophy-team-card-360.bones.json
  - src/bones/philosophy-team-card-432.bones.json
  - src/bones/team-section-photo-default.bones.json
  - src/app/(site)/layout.tsx
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - SAFARI-01
  - SAFARI-02
  - ANI-01
  - ANI-02
must_haves:
  truths:
    - "boneyard-js package is removed from package.json and lockfile"
    - "src/shared/ui/boneyard-skeleton.tsx no longer exists"
    - "src/bones/ directory no longer exists (registry.js + 15 .bones.json files deleted)"
    - "src/app/(site)/layout.tsx no longer contains `import \"@/bones/registry.js\";`"
    - "pnpm typecheck, pnpm lint, and pnpm build all succeed after teardown"
    - "Phase 4 closes with a single atomic commit (Pattern E) so that any residual import breaks the build and is caught before merge"
  artifacts:
    - path: "package.json"
      provides: "Dependencies without boneyard-js"
      pattern: "no \"boneyard-js\" entry"
    - path: "src/app/(site)/layout.tsx"
      provides: "Layout without bones registry import"
      pattern: "no import of \"@/bones/registry.js\""
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "(nothing)"
      via: "bones registry import line 7 removed"
      pattern: "absence of `@/bones/registry`"
---

<objective>
Close Phase 4 with a single atomic teardown commit (Pattern E from PATTERNS.md, per D-02 and RESEARCH.md Pitfall 6). This plan deletes every remaining trace of `boneyard-js` from the project: the wrapper component, the build artifacts directory (`src/bones/`), the layout-side-effect import, and the package.json / lockfile entry.

Because all 10 BoneyardSkeleton consumers were migrated in Plans 04-02 through 04-05, there should be zero remaining imports of `@/shared/ui/boneyard-skeleton` or `boneyard-js` elsewhere. A pre-flight grep sweep confirms this before any deletion. The final `pnpm build` is the authoritative check — per A6 in RESEARCH.md, an unresolved import would hard-break the build.

Purpose: Executes Pattern E as a single commit to maintain the property that the repo never contains an unresolved `boneyard-js` import at any point. Splitting this into multiple commits would leave intermediate states where some consumers still import a deleted file.

Output: Phase 4 is complete. `boneyard-js` is fully excised. Single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-safari-animations/04-CONTEXT.md
@.planning/phases/04-safari-animations/04-RESEARCH.md
@.planning/phases/04-safari-animations/04-PATTERNS.md
@src/shared/ui/boneyard-skeleton.tsx
@src/app/(site)/layout.tsx
@package.json

<interfaces>
<!-- After this plan, these surfaces cease to exist. Any remaining import breaks the build. -->
`BoneyardSkeleton` component: DELETED.
`boneyard-js/react` import path: UNAVAILABLE.
`@/bones/registry`: UNAVAILABLE.

Consumers that MUST have zero references after this plan:
- `src/widgets/team/ui/team-section-photo.tsx` (migrated in Plan 04-02)
- `src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx` (migrated in Plan 04-03)
- `src/widgets/cases/ui/cases-section-{360,480,768}.tsx` (migrated in Plan 04-04)
- `src/widgets/cases/ui/cases-section-{1024,1440}.tsx` (migrated in Plan 04-05)
- `src/widgets/philosophy-clients/ui/philosophy-clients-{1024,1440}.tsx` (migrated in Plan 04-05)

Pre-flight grep must return zero hits for the following patterns (Task 1):
- `BoneyardSkeleton`
- `boneyard-js`
- `@/bones/registry` (except the single line in `src/app/(site)/layout.tsx` that is deleted in this plan)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight grep — verify no remaining boneyard references anywhere except teardown targets</name>
  <files>(read-only)</files>
  <behavior>
    - A grep sweep confirms no code file outside this plan's `files_modified` list still references BoneyardSkeleton, boneyard-js, or `@/bones/registry`.
    - If any reference is found, HALT and report the file + line. A migration gap exists; it must be fixed before teardown.
  </behavior>
  <action>
Run the following Grep queries (via the Grep tool, NOT `grep`/`rg`):

1. `pattern: "BoneyardSkeleton"` across the entire repo. Allowed hits:
   - `src/shared/ui/boneyard-skeleton.tsx` (deleted in Task 2)
   - Files under `.planning/` (documentation — ignore)

   Any hit in `src/widgets/**`, `src/app/**`, `src/shared/**` (other than the component file itself) is a BLOCKER — the corresponding consumer was not migrated. Halt and report.

2. `pattern: "boneyard-js"` across the entire repo. Allowed hits:
   - `package.json` (removed in Task 4)
   - `pnpm-lock.yaml` (removed in Task 4)
   - `.planning/` docs
   - `src/bones/registry.js` (deleted in Task 3)

   Any other hit → BLOCKER.

3. `pattern: "@/bones/registry"` across the entire repo. Allowed hits:
   - `src/app/(site)/layout.tsx` (line 7, removed in Task 2)
   - `.planning/` docs
   - `src/bones/` artifacts (the directory is deleted in Task 3)

   Any other hit → BLOCKER.

If all three greps come back clean (only allowed hits), proceed to Task 2. If any blocker is found, STOP, do not delete anything, and report: "Unexpected residual boneyard reference in <file>:<line>. Migration gap detected — fix before teardown."
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- All three Grep sweeps report only the allowed residual hits.
- No blockers reported.
- `pnpm typecheck` still green (state unchanged — sanity check).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete BoneyardSkeleton component and remove bones registry import from layout</name>
  <files>src/shared/ui/boneyard-skeleton.tsx, src/app/(site)/layout.tsx</files>
  <behavior>
    - `src/shared/ui/boneyard-skeleton.tsx` is deleted from the filesystem.
    - `src/app/(site)/layout.tsx` has `import "@/bones/registry.js";` (currently line 7) removed; other imports and exports untouched.
    - Layout still renders with `SiteMotionConfig` (added in Plan 04-01); no other changes to `SiteLayout`.
  </behavior>
  <action>
1. Delete `src/shared/ui/boneyard-skeleton.tsx` using Bash `rm` (direct filesystem removal). Example: `rm /Users/a_savinkov/solo-site/src/shared/ui/boneyard-skeleton.tsx`.

2. Edit `src/app/(site)/layout.tsx`:
   - Remove the line `import "@/bones/registry.js";` (currently line 7).
   - Do not touch any other import, the `metadata` export, the `localFont` call, or the `SiteLayout` function body.
   - After the edit the imports block should look like:
     ```tsx
     import type { Metadata } from "next";
     import localFont from "next/font/local";

     import { publicSiteUrl } from "@/shared/config/public-site-url";
     import { siteConfig } from "@/shared/config/site";
     import { SiteLoadOverlay } from "@/widgets/site-load";
     import { SiteMotionConfig } from "./motion-config-provider";

     import "../globals.css";
     ```
     (Exact import ordering may vary; the key property is that `@/bones/registry.js` is gone and `SiteMotionConfig` import from Plan 04-01 remains.)

3. Do NOT remove `boneyard-js` from `package.json` in this task — that is Task 4 so that package.json / lockfile / code deletions all land in the same final commit (per Pattern E, single atomic commit — the Tasks are bookkeeping granularity; the `git commit` happens once at Task 5).
  </action>
  <verify>
    <automated>test ! -f src/shared/ui/boneyard-skeleton.tsx && ! grep -q '@/bones/registry' src/app/\(site\)/layout.tsx</automated>
  </verify>
  <done>
- `src/shared/ui/boneyard-skeleton.tsx` no longer exists on disk.
- `src/app/(site)/layout.tsx` does not contain `@/bones/registry`.
- Other imports in layout are preserved (including `SiteMotionConfig`).
- No git commit yet — commit is at Task 5.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete src/bones/ directory (registry.js + 15 .bones.json files)</name>
  <files>src/bones/</files>
  <behavior>
    - `src/bones/` directory is removed entirely.
    - Contents previously verified: `registry.js` + 15 `.bones.json` files (list in `files_modified` frontmatter).
  </behavior>
  <action>
1. Confirm the directory contents one more time via Bash `ls src/bones/` to rule out surprise files (binary assets, etc.) that might warrant separate handling. Expected: `registry.js` + 15 `*.bones.json` files.

2. Remove the directory:
   ```bash
   rm -rf /Users/a_savinkov/solo-site/src/bones
   ```

   `rm -rf` is acceptable here because (a) all contents are build-time auto-generated (`// Auto-generated by npx boneyard-js build — do not edit` per RESEARCH.md Runtime State Inventory), (b) the auto mode system prompt carves out bulk deletes of generated artifacts as non-destructive for user data. This is not shared/production data per the auto-mode carveout.

3. After removal: `ls src/bones/` should return "No such file or directory".

4. No git commit yet.
  </action>
  <verify>
    <automated>test ! -d src/bones</automated>
  </verify>
  <done>
- `src/bones/` directory is gone.
- No stray `registry.js` or `.bones.json` files remain in the repo.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Remove boneyard-js from package.json and update lockfile</name>
  <files>package.json, pnpm-lock.yaml</files>
  <behavior>
    - `"boneyard-js": "^1.7.6"` line is removed from the `dependencies` block in `package.json`.
    - `pnpm install` is run (or `pnpm remove boneyard-js` which handles both files in one step); lockfile reflects the removal.
    - No other dependencies or scripts in `package.json` are changed.
    - `motion` dependency from Plan 04-01 remains.
  </behavior>
  <action>
1. Run: `pnpm remove boneyard-js` at project root. This:
   - Strips `"boneyard-js": "^1.7.6"` from `package.json` dependencies.
   - Updates `pnpm-lock.yaml` to drop the `boneyard-js` entry and any transitive deps that were only pulled in for it.

2. Inspect the resulting `package.json`:
   - `"boneyard-js"` must be absent.
   - `"motion": "^12.38.0"` must still be present.
   - The rest of `dependencies` and `devDependencies` sections unchanged.

3. Run the full verification chain:
   ```bash
   pnpm typecheck
   pnpm lint
   pnpm build
   ```

   All three must succeed. If `pnpm build` fails with "Module not found: boneyard-js" or "Cannot find module '@/shared/ui/boneyard-skeleton'" or "Cannot find module '@/bones/registry'", it means a migration gap in an earlier plan was missed. STOP, restore files with `git restore`, report which file still imports what, and open a gap plan via `/gsd-plan-phase --gaps`.

4. Do NOT commit yet — Task 5 creates the single atomic commit covering Tasks 2–4.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm build && ! grep -q '"boneyard-js"' package.json</automated>
  </verify>
  <done>
- `boneyard-js` is absent from `package.json` dependencies.
- `pnpm-lock.yaml` no longer lists `boneyard-js`.
- `motion` dependency is still present.
- `pnpm typecheck && pnpm lint && pnpm build` all green — the authoritative A6 RESEARCH check.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Atomic commit — all teardown changes in one commit (Pattern E)</name>
  <files>(git commit only)</files>
  <behavior>
    - All changes from Tasks 2, 3, 4 land in a single `git commit`.
    - Commit message uses the phase-4 refactor convention.
    - Post-commit: `git log -1` shows one commit that includes the component deletion, the bones directory deletion, the layout import removal, and the package.json / lockfile updates.
  </behavior>
  <action>
1. Stage all teardown changes explicitly (avoid `git add -A` per the commit safety protocol — no .env files or secrets should be lurking, but be deliberate):
   ```bash
   git add src/shared/ui/boneyard-skeleton.tsx   # staged as deletion
   git add src/bones                              # staged as deletion (with -u-like behavior for tracked files in the dir)
   git add src/app/(site)/layout.tsx
   git add package.json pnpm-lock.yaml
   ```

   If `git status` shows any other modified files not in the plan's `files_modified`, STOP and investigate — do not commit stray changes.

2. Create the commit with a HEREDOC for message formatting:
   ```bash
   git commit -m "$(cat <<'EOF'
   refactor(04): atomic teardown — remove boneyard-js, BoneyardSkeleton, and src/bones

   - Delete src/shared/ui/boneyard-skeleton.tsx (all consumers migrated to motion in 04-02..04-05)
   - Delete src/bones/ (15 .bones.json + registry.js, all auto-generated build artifacts)
   - Remove `import "@/bones/registry.js";` from src/app/(site)/layout.tsx
   - Remove boneyard-js from package.json and pnpm-lock.yaml
   - pnpm typecheck / lint / build all green post-teardown (authoritative gap check per RESEARCH.md A6)

   Closes Phase 4 (SAFARI-01, SAFARI-02, ANI-01, ANI-02).

   Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
   EOF
   )"
   ```

   (Note: `Co-Authored-By` attribution line retained per commit convention, but this is in a refactor commit, not the PR-description template — adjust if the project has a different signing convention.)

3. Run `git status` to verify clean working tree.

4. Run `git log -1 --stat` to verify the commit includes: boneyard-skeleton.tsx (D), src/bones/* (D), layout.tsx (M), package.json (M), pnpm-lock.yaml (M). Count should match `files_modified` entries minus `.bones.json` items that are deletions.
  </action>
  <verify>
    <automated>git log -1 --stat | grep -E 'boneyard-skeleton|bones|layout|package.json' && pnpm build</automated>
  </verify>
  <done>
- Single commit in git log whose message starts with `refactor(04): atomic teardown`.
- All teardown files (Tasks 2–4) are included in that commit.
- `git status` shows clean working tree.
- `pnpm build` still green post-commit (sanity).
- Phase 4 is complete — ROADMAP.md Phase 4 can be checked off in a follow-up bookkeeping commit outside this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-teardown codebase (with boneyard imports) ↔ post-teardown codebase (without) | Any residual import of a deleted symbol breaks the build. The atomic commit strategy ensures no intermediate broken state is ever committed. |
| Package manager lockfile integrity | Removing a dependency must leave the lockfile in a consistent state (no dangling transitive refs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering (broken build) | Unresolved BoneyardSkeleton / boneyard-js import | mitigate | Task 1 pre-flight grep sweep + Task 4 `pnpm build` gate. If either fails, teardown halts and no deletion commit is created. This enforces A6 (RESEARCH.md) — the build IS the missing-consumer detector. |
| T-04-18 | Tampering (lockfile drift) | pnpm-lock.yaml after remove | mitigate | Using `pnpm remove boneyard-js` (not manual JSON edit) guarantees lockfile consistency. Manual JSON edits forbidden here. |
| T-04-19 | Accidental data loss | `rm -rf src/bones` | accept | `src/bones/` contents are 100% auto-generated by `npx boneyard-js build` per RESEARCH.md Runtime State Inventory + the `// Auto-generated ... — do not edit` banner in `registry.js`. Per auto-mode carveout, bulk deletion of build artifacts is not destructive to user data. Git history preserves them if ever needed. |
| T-04-20 | Denial-of-service (package removal breaking postinstall) | `pnpm remove boneyard-js` triggers postinstall script | mitigate | Project's `postinstall` is `node scripts/ensure-env-local.mjs` — unrelated to boneyard. No postinstall hook calls boneyard-js directly. |
</threat_model>

<verification>
Plan-level verification:

1. Task 1 grep sweep returns no blockers.
2. After Tasks 2–4: `pnpm typecheck && pnpm lint && pnpm build` all green.
3. Task 5 produces a single commit with the expected files.
4. `git log --oneline -n 1` shows one refactor(04) commit.
5. Developer smoke (optional):
   - `pnpm dev`, open http://localhost:3000, verify the site renders with no console errors about missing modules.
   - Quick pass through each migrated section (team photo, philosophy team card, cases cards on 360/480/768/1024/1440) — reveals still work.

**Phase-close meta-verification:**
- All four Phase 4 requirements (SAFARI-01, SAFARI-02, ANI-01, ANI-02) are addressed:
  - SAFARI-01: motion reveal works identically in Safari and Chrome (UAT Plan 04-05 Task 5 item 1).
  - SAFARI-02: pin-scroll holds in Safari (UAT Plan 04-05 Task 5 item 2, fallback D-07 in Task 6).
  - ANI-01: cases scroll transition verified correct (same UAT item).
  - ANI-02: smooth easing — reveal uses 220ms easeOut; hover transitions preserved (Pattern D); D-11 checklist item 3 validates no flash.
- `boneyard-js` fully removed — no runtime dependency on an unmaintained package.
</verification>

<success_criteria>
- [ ] Task 1 grep sweep clean — no unexpected residual references.
- [ ] `src/shared/ui/boneyard-skeleton.tsx` deleted.
- [ ] `src/bones/` directory deleted.
- [ ] `src/app/(site)/layout.tsx` no longer contains `@/bones/registry`.
- [ ] `boneyard-js` absent from `package.json` and `pnpm-lock.yaml`.
- [ ] `motion` dependency still present.
- [ ] `pnpm typecheck && pnpm lint && pnpm build` all green.
- [ ] Single atomic git commit titled `refactor(04): atomic teardown — remove boneyard-js, BoneyardSkeleton, and src/bones`.
- [ ] `git status` clean post-commit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-safari-animations/04-06-SUMMARY.md` noting: files deleted, lines changed in layout, package.json diff (one line removed), lockfile delta count, final build result, and explicit confirmation that Phase 4 is closed. Include a reminder for the orchestrator to check off Phase 4 in ROADMAP.md and update STATE.md.
</output>
