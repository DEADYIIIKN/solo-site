---
phase: 16-bundle-fonts-p1
plan: 02
type: execute
wave: 1
depends_on: [16-01-fonts-woff2-subset]
files_modified:
  - next.config.ts
  - package.json
  - pnpm-lock.yaml
  - src/widgets/first-screen/ui/first-screen-consultation-modal-*.tsx
  - src/widgets/tg-popup/ui/tg-popup-host.tsx
  - src/widgets/services/lib/use-services-pin-scroll-progress.ts
  - tests/unit/bundle-analyzer-config.test.ts
  - tests/unit/consultation-modal-dynamic-import.test.ts
autonomous: true
requirements: [PERF-07, PERF-08]
must_haves:
  truths:
    - "`pnpm analyze` runs a production build with @next/bundle-analyzer"
    - "Consultation modal wrappers lazy-load the shared modal implementation"
    - "Delayed Telegram popup lazy-loads the popup UI"
    - "Production mobile smoke reports 0 console errors and 0 failed requests"
  artifacts:
    - path: ".next/analyze/client.html"
      provides: "client bundle analyzer report generated by pnpm analyze"
    - path: "tests/unit/bundle-analyzer-config.test.ts"
      provides: "analyzer config regression guard"
    - path: "tests/unit/consultation-modal-dynamic-import.test.ts"
      provides: "modal dynamic-import regression guard"
---

# Plan 16-02: Bundle analyzer / dynamic imports / console errors

## Objective

Add a local bundle analyzer workflow, split modal-only UI out of the initial client path, and verify the mobile production page has no console errors or failed requests.

## Tasks

1. Add `@next/bundle-analyzer` and expose `pnpm analyze`.
2. Wire analyzer into `next.config.ts` behind `ANALYZE=true`.
3. Add regression tests for analyzer config and modal dynamic imports.
4. Lazy-load consultation modal implementations used by breakpoint wrappers.
5. Lazy-load delayed `TgPopup` UI from `TgPopupHost`.
6. Remove the Tailwind scanner false positive caused by a literal arbitrary-variant example in a source comment.
7. Verify unit tests, typecheck, build, analyzer reports, and production mobile browser smoke.

## Verification

```bash
pnpm exec vitest run tests/unit/consultation-modal-dynamic-import.test.ts tests/unit/bundle-analyzer-config.test.ts
pnpm exec tsc --noEmit -p tsconfig.json
pnpm build
pnpm analyze
```

Production smoke:

- `next start -p 3016`
- Playwright Chromium mobile viewport `390x844`
- Result: `logs: []`, `failed: []`
