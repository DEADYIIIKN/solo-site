---
phase: 02-desktop-layout-1440px-1180px
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/business-goals/ui/business-goals.tsx
autonomous: true
requirements:
  - LY1440-01
  - LY1440-02

must_haves:
  truths:
    - "На 1440px все заголовки карточек Business Goals выровнены по нижнему краю карточки, как в Figma"
    - "На 1440px текстовый блок карточки #4 имеет ширину и font-weight, соответствующие Figma"
    - "Соседние карточки визуально не сломаны после изменений"
  artifacts:
    - path: "src/widgets/business-goals/ui/business-goals.tsx"
      provides: "AccordionCard: title позиционирован через bottom вместо top (1440px expanded state)"
      contains: "bottom.*30px.*title|titleBlock.*bottom"
  key_links:
    - from: "AccordionCard (строка ~284)"
      to: "expanded title div"
      via: "style={{ top: '30px' }} → style={{ bottom: 'Npx' }}"
      pattern: "bottom.*[0-9]+px"
---

<objective>
Исправить два визуальных бага в Business Goals на 1440px согласно Figma "Адаптивы актуальные":
1. LY1440-01 — заголовки всех четырёх карточек должны быть выровнены по нижнему краю (сейчас — по верхнему).
2. LY1440-02 — карточка #4 (четвёртая по DOM-порядку, index=3) должна иметь корректную ширину текстового блока и font-weight из Figma.

Purpose: Соответствие Figma на брейкпоинте 1440px; карточки — основная секция лендинга.
Output: Обновлённый business-goals.tsx с bottom-aligned title в expanded state и корректными параметрами карточки #4.
</objective>

<execution_context>
@/Users/a_savinkov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desktop-layout-1440px-1180px/02-CONTEXT.md
</context>

<interfaces>
<!-- Ключевые контракты из business-goals.tsx (извлечено при планировании) -->

Компонент AccordionCard (~строка 165-330):
- Props: active, card, expandedImageClass, expandedImageStyle, collapsedImageLeftPx, imageSrc, collapsedImageWidthPx, is1024?, motionDirection?, onClick, onFocusCard?, onHoverCancel?, onHover?, topPx
- Expanded title div (строка ~284): `className={\`absolute ${is1024 ? "left-[20px] top-[20px] w-[460px] text-[40px] tracking-[-0.4px]" : "left-[30px] top-[30px] w-[470px] text-[50px] tracking-[-0.5px]"} lowercase leading-[0.9] text-white\``}
- Для 1440px (is1024=false): `left-[30px] top-[30px] w-[470px] text-[50px] tracking-[-0.5px]`
- Title lines: card.titlePrimary (font-bold), card.titleAccent (font-normal italic), card.titleSuffix (font-bold, optional)

Cards index:
- 0 = карточка #1, 1 = #2, 2 = #3, 3 = #4 (карточка #4 = index 3)

Констрейнты сетки (1440px):
- collapsedWidths1440 = [88, 87, 88, 87]
- activeWidths1440 = [868, 867, 868, 867]
- Карточки расположены в grid: left=140px, top=214px, h=500px, w=1160px
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Извлечь точные px-значения из Figma через Figma MCP</name>
  <files>src/widgets/business-goals/ui/business-goals.tsx</files>
  <read_first>
    - src/widgets/business-goals/ui/business-goals.tsx (весь файл — 1300 строк; AccordionCard ~165-330, 1440px рендер ~973-1079)
  </read_first>
  <action>
Использовать Figma MCP для получения точных значений из листа "Адаптивы актуальные":

1. Найти секцию Business Goals на 1440px. Figma-ноды для ориентира: файл содержит комментарии `Figma 805:14096`, `Figma 783:9521` — использовать их как якоря поиска в MCP.

2. Для LY1440-01 — извлечь из Figma расположение текстового блока заголовка в EXPANDED карточке (активной):
   - Нужно: значение `bottom` (в px от нижнего края карточки) для title div
   - Карточка высотой 500px, title сейчас размещён через `top: 30px` — Figma покажет реальное расположение
   - Ожидаемый паттерн: title находится в нижней трети карточки (bottom ~150-250px от низа)

3. Для LY1440-02 — извлечь из Figma параметры текстового блока карточки #4 (четвёртая, "Производство" или аналогичная):
   - Ширина текстового фрейма (px) — сейчас все карточки используют w-[470px]
   - Font-weight заголовка (сейчас titlePrimary = font-bold, titleAccent = font-normal italic)

Зафиксировать все извлечённые значения для применения в Task 2.
  </action>
  <verify>
    Значения зафиксированы: bottom_px для title (LY1440-01) и width_px + font-weight для карточки #4 (LY1440-02).
  </verify>
  <done>Из Figma MCP получены точные числовые значения для обоих фиксов.</done>
</task>

<task type="auto">
  <name>Task 2: Применить исправления в AccordionCard (business-goals.tsx)</name>
  <files>src/widgets/business-goals/ui/business-goals.tsx</files>
  <read_first>
    - src/widgets/business-goals/ui/business-goals.tsx (строки 280-310 — expanded title div и description в AccordionCard)
  </read_first>
  <action>
ВНИМАНИЕ: Файл ~1300 строк — вносить только точечные изменения, не переписывать логику.

**LY1440-01 — Bottom-aligned title (per D-01, D-02):**

Найти в AccordionCard строку ~284 — expanded title div:
```tsx
className={`absolute ${is1024 ? "left-[20px] top-[20px] w-[460px] text-[40px] tracking-[-0.4px]" : "left-[30px] top-[30px] w-[470px] text-[50px] tracking-[-0.5px]"} lowercase leading-[0.9] text-white`}
```

Заменить на вариант с `bottom` для 1440px (is1024=false). Точное значение bottom — из Task 1 (Figma MCP). Паттерн замены:
```tsx
// Было:
"left-[30px] top-[30px] w-[470px] text-[50px] tracking-[-0.5px]"

// Стало (значение N — из Figma):
"left-[30px] bottom-[Npx] w-[470px] text-[50px] tracking-[-0.5px]"
```

Убедиться, что для is1024=true (`"left-[20px] top-[20px] ..."`) значение НЕ изменяется — это другой брейкпоинт.

**LY1440-02 — Карточка #4 ширина и font-weight (per D-03, D-04):**

Карточка #4 = index 3 в accordionCards. В AccordionCard компонент не получает index как prop — он рендерится единообразно. Если Figma показывает, что карточка #4 требует отличной ширины/веса:

Вариант A (если ширина отличается только для #4): Добавить prop `cardIndex?: number` в AccordionCardProps и передать `index` при рендере в строке ~1043. В AccordionCard использовать `cardIndex === 3` для применения иной ширины/font-weight.

Вариант B (если ширина одинакова, но font-weight другой для titlePrimary карточки #4): Передать данные через существующий `card` объект (если `businessGoalsContent.cards[3]` можно расширить — проверить `business-goals.data.ts`).

Применить вариант, наиболее соответствующий данным Figma и минимальным изменениям.

Точные значения — из Task 1. Не вводить `fontSize` или `fontFamily` изменения вне того, что требует Figma.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    Дополнительно: npm run build должен проходить без ошибок в этом файле.
  </verify>
  <done>
    - business-goals.tsx компилируется без ошибок TypeScript
    - AccordionCard expanded title для 1440px использует `bottom: Npx` вместо `top: 30px`
    - Карточка #4 при раскрытии показывает ширину и font-weight из Figma
    - Изменения ограничены AccordionCard (~строки 280-330) и при необходимости рендером в строках 1043-1064
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Figma MCP → код | Значения из Figma попадают в hardcoded px — не внешний ввод, не угроза |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | business-goals.tsx (1300 lines) | accept | Точечные изменения в 2 местах; минимальный риск регрессии соседних карточек — проверить визуально после |
</threat_model>

<verification>
1. `npx tsc --noEmit` — нет ошибок TypeScript
2. `npm run build` — сборка проходит
3. Визуальная проверка (DevTools → 1440px): раскрыть каждую из 4 карточек — title должен быть внизу карточки
4. Карточка #4: сравнить ширину текстового блока и font-weight с Figma
</verification>

<success_criteria>
- TypeScript компиляция без ошибок
- На 1440px expanded-заголовки всех 4 карточек Business Goals выровнены по нижнему краю
- Карточка #4 имеет ширину текстового блока и font-weight из Figma
- Карточки #1, #2, #3 выглядят идентично тому, что было (кроме выравнивания заголовка)
- На 1024px поведение не изменилось (is1024 ветка не тронута)
</success_criteria>

<output>
После завершения создать `.planning/phases/02-desktop-layout-1440px-1180px/02-01-SUMMARY.md`
</output>
