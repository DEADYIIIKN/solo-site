---
phase: 01-quick-wins-stack-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/lead-form/ui/lead-form-fields.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx
autonomous: true
requirements:
  - FORM-02
  - FORM-03

must_haves:
  truths:
    - "Clicking 'Политикой конфиденциальности' link opens /privacy in a new tab without toggling the checkbox"
    - "Clicking the rest of the consent label text toggles the checkbox"
    - "The 'оставить заявку' button text is visually centered in all five consultation modal breakpoints"
  artifacts:
    - path: "src/widgets/lead-form/ui/lead-form-fields.tsx"
      provides: "Updated privacy link with target=_blank and rel=noopener noreferrer"
      contains: "target=\"_blank\""
    - path: "src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx"
      provides: "Submit button without asymmetric padding at h-[60px]"
      contains: "h-[60px]"
    - path: "src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx"
      provides: "Submit button without asymmetric padding at h-[44px]"
      contains: "h-[44px]"
  key_links:
    - from: "lead-form-fields.tsx <a onClick={stopPropagation}>"
      to: "/privacy new tab"
      via: "target=_blank + rel=noopener noreferrer"
      pattern: "target=\"_blank\""
    - from: "consultation modal button className"
      to: "flex centering"
      via: "removal of pb-[20px] pt-[N]"
      pattern: "h-\\[\\d+px\\] w-full shrink-0 items-center justify-center"
---

<objective>
Fix two form UX bugs: (FORM-02) make the privacy link open in a new tab without toggling the checkbox, and (FORM-03) remove asymmetric vertical padding from submit buttons in all five consultation modal breakpoints so the button text is visually centered.

Purpose: Both bugs are immediately user-visible and block trust. FORM-02 has been reported — clicking the privacy link triggers unexpected page navigation inside the same tab. FORM-03 produces visibly misaligned button labels across all breakpoints.

Output: Six modified files — one for FORM-02, five for FORM-03.
</objective>

<execution_context>
@/Users/a_savinkov/solo-site/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/solo-site/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/a_savinkov/solo-site/.planning/PROJECT.md
@/Users/a_savinkov/solo-site/.planning/ROADMAP.md
@/Users/a_savinkov/solo-site/.planning/STATE.md

<interfaces>
<!-- Key patterns extracted from codebase. Executor should use these directly. -->

From src/widgets/lead-form/ui/lead-form-fields.tsx (lines 478-484) — CURRENT STATE:
```tsx
<a
  className="underline decoration-solid [text-decoration-skip-ink:none]"
  href="/privacy"
  onClick={(e) => e.stopPropagation()}
>
  Политикой конфиденциальности
</a>
```

From src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx — CURRENT button className (lines 501, 543):
```
"flex h-[60px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[24px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"
```

From src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx — CURRENT button className (lines 222, 443):
```
"flex h-[56px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[22px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"
```

From src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx — CURRENT button className (lines 222, 445):
```
"flex h-[52px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[19px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"
```

From src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx — CURRENT button className (lines 222, 456):
```
"flex h-[48px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[19px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"
```

From src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx — CURRENT button className (lines 222, 443):
```
"flex h-[44px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[17px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"
```

CORRECT centered button pattern (already used in lead-form-fields.tsx, lines 489-494):
```tsx
className={cn(
  "flex w-full items-center justify-center rounded-[50px] border-0 bg-[#ff5c00] px-10 font-semibold lowercase text-white transition-colors hover:bg-[#de4f00]",
  d.btn,
  d.btnH,
)}
```
Note: uses min-h via density class, no asymmetric padding. This is the reference — do NOT modify this button.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FORM-02 — Add target=_blank and rel=noopener noreferrer to privacy link</name>
  <files>src/widgets/lead-form/ui/lead-form-fields.tsx</files>
  <read_first>
    - src/widgets/lead-form/ui/lead-form-fields.tsx — read around lines 478–486 to confirm current <a> element state before editing
  </read_first>
  <action>
In src/widgets/lead-form/ui/lead-form-fields.tsx, find the `<a>` element at approximately line 478 that has `href="/privacy"` and `onClick={(e) => e.stopPropagation()}`. Add exactly two attributes to this element:

1. `target="_blank"`
2. `rel="noopener noreferrer"`

The resulting element must look exactly like this:
```tsx
<a
  className="underline decoration-solid [text-decoration-skip-ink:none]"
  href="/privacy"
  onClick={(e) => e.stopPropagation()}
  target="_blank"
  rel="noopener noreferrer"
>
  Политикой конфиденциальности
</a>
```

IMPORTANT constraints (per D-04, D-05, D-06):
- Do NOT remove `onClick={(e) => e.stopPropagation()}` — this prevents the link click from toggling the checkbox.
- Do NOT change `href` — it already points to `/privacy` (correct).
- Do NOT touch the submit button in this file (lines 489–498) — it uses `min-h-[...]` with no asymmetric padding and is already correctly centered.
- Do NOT modify the `<input type="checkbox">` or `<label htmlFor>` structure.
  </action>
  <verify>
    <automated>grep -n 'target="_blank"' /Users/a_savinkov/solo-site/src/widgets/lead-form/ui/lead-form-fields.tsx</automated>
    <automated>grep -n 'rel="noopener noreferrer"' /Users/a_savinkov/solo-site/src/widgets/lead-form/ui/lead-form-fields.tsx</automated>
    <automated>grep -n 'stopPropagation' /Users/a_savinkov/solo-site/src/widgets/lead-form/ui/lead-form-fields.tsx</automated>
  </verify>
  <done>
    - lead-form-fields.tsx contains `target="_blank"` on the privacy link
    - lead-form-fields.tsx contains `rel="noopener noreferrer"` on the privacy link
    - `e.stopPropagation()` is still present on the same `<a>` element
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix FORM-03 — Remove asymmetric padding from consultation modal submit buttons (all 5 breakpoints)</name>
  <files>
    src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx
  </files>
  <read_first>
    - src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx — read around lines 495–550 to confirm both button occurrences before editing
    - src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx — read around lines 215–230 and 435–450
    - src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx — read around lines 215–230 and 437–452
    - src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx — read around lines 215–230 and 448–463
    - src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx — read around lines 215–230 and 435–450
  </read_first>
  <action>
In each of the five consultation modal files, find ALL submit button `<button>` elements and remove the asymmetric vertical padding from their `className`. Each file has exactly TWO button occurrences (one in the form state, one in the success state).

The fix per file — remove only the padding tokens listed; keep all other tokens:

**modal-1440.tsx** (lines ~501 and ~543) — remove `pb-[20px] pt-[24px]`:
- Before: `"flex h-[60px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[24px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`
- After:  `"flex h-[60px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`

**modal-1024.tsx** (lines ~222 and ~443) — remove `pb-[20px] pt-[22px]`:
- Before: `"flex h-[56px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[22px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`
- After:  `"flex h-[56px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`

**modal-768.tsx** (lines ~222 and ~445) — remove `pb-[20px] pt-[19px]`:
- Before: `"flex h-[52px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[19px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`
- After:  `"flex h-[52px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`

**modal-480.tsx** (lines ~222 and ~456) — remove `pb-[20px] pt-[19px]`:
- Before: `"flex h-[48px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[19px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`
- After:  `"flex h-[48px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`

**modal-360.tsx** (lines ~222 and ~443) — remove `pb-[20px] pt-[17px]`:
- Before: `"flex h-[44px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] pb-[20px] pt-[17px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`
- After:  `"flex h-[44px] w-full shrink-0 items-center justify-center rounded-[50px] bg-[#ff5c00] px-[40px] text-center lowercase text-white transition-colors hover:bg-[#de4f00]"`

IMPORTANT constraints (per D-07, RESEARCH.md Pitfall 3 and 5):
- Do NOT add `text-center` — it is already present and will not fix vertical miscentering.
- Do NOT touch lead-form-fields.tsx — that file's button is already correctly centered.
- Do NOT remove `px-[40px]` or any other padding tokens — only remove the `pb-[20px]` and `pt-[Npx]` asymmetric vertical padding.
- Keep `h-[Npx]` (fixed height) and `flex items-center justify-center` — these provide the correct centering after padding removal.
- Each file has exactly 2 button occurrences — update both in every file (10 total changes across 5 files).
  </action>
  <verify>
    <automated>grep -n 'pb-\[20px\]' /Users/a_savinkov/solo-site/src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx /Users/a_savinkov/solo-site/src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx /Users/a_savinkov/solo-site/src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx /Users/a_savinkov/solo-site/src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx /Users/a_savinkov/solo-site/src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx</automated>
    <automated>grep -n 'h-\[60px\].*justify-center' /Users/a_savinkov/solo-site/src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx</automated>
    <automated>grep -n 'h-\[44px\].*justify-center' /Users/a_savinkov/solo-site/src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx</automated>
  </verify>
  <done>
    - grep for `pb-[20px]` across all 5 modal files returns zero matches
    - Each file retains `h-[Npx]`, `items-center`, `justify-center` on button elements
    - `flex items-center justify-center` is still present on all button className strings
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None for this plan | All changes are static className edits and HTML attribute additions; no user input crosses a trust boundary in these two tasks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `<a target="_blank">` to /privacy | accept | Opens same-origin /privacy page; rel="noopener noreferrer" prevents opener access — standard practice |
</threat_model>

<verification>
After execution, verify end-to-end:
1. `grep -n 'target="_blank"' src/widgets/lead-form/ui/lead-form-fields.tsx` — must return a result
2. `grep -n 'rel="noopener noreferrer"' src/widgets/lead-form/ui/lead-form-fields.tsx` — must return a result
3. `grep -rn 'pb-\[20px\]' src/widgets/first-screen/ui/first-screen-consultation-modal-*.tsx` — must return zero results
4. TypeScript check: `cd /Users/a_savinkov/solo-site && pnpm tsc --noEmit` — must exit 0 (or with only pre-existing errors)
</verification>

<success_criteria>
- FORM-02: The privacy link `<a>` in lead-form-fields.tsx has both `target="_blank"` and `rel="noopener noreferrer"` attributes while retaining `onClick={(e) => e.stopPropagation()}`
- FORM-03: Zero occurrences of `pb-[20px]` remain in any of the five consultation modal files; all button elements retain `h-[Npx] flex items-center justify-center`
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `/Users/a_savinkov/solo-site/.planning/phases/01-quick-wins-stack-audit/01-01-SUMMARY.md`
</output>
