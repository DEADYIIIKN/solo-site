---
phase: 01-quick-wins-stack-audit
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - src/app/(site)/privacy/page.tsx
  - src/widgets/footer/ui/footer-1440.tsx
  - src/widgets/footer/ui/footer-1024.tsx
  - src/widgets/footer/ui/footer-768.tsx
  - src/widgets/footer/ui/footer-480.tsx
  - src/widgets/footer/ui/footer-360.tsx
autonomous: true
requirements:
  - FORM-01

must_haves:
  truths:
    - "Navigating to /privacy returns a rendered page — not a 404"
    - "The page inherits site layout: Montserrat font, dark background, footer"
    - "The page renders placeholder Russian privacy policy text when no CMS content is set"
    - "All five footer variants link to /privacy instead of #"
  artifacts:
    - path: "src/app/(site)/privacy/page.tsx"
      provides: "Next.js App Router RSC page fetching Payload 'privacy-page' Global"
      exports: ["default async function PrivacyPage"]
      contains: "findGlobal"
    - path: "src/widgets/footer/ui/footer-1440.tsx"
      provides: "Footer with href=/privacy on privacy link"
      contains: "href=\"/privacy\""
    - path: "src/widgets/footer/ui/footer-360.tsx"
      provides: "Footer with href=/privacy on privacy link (narrowest breakpoint)"
      contains: "href=\"/privacy\""
  key_links:
    - from: "src/app/(site)/privacy/page.tsx"
      to: "Payload privacy-page Global"
      via: "payload.findGlobal({ slug: 'privacy-page' })"
      pattern: "findGlobal.*privacy-page"
    - from: "src/app/(site)/privacy/page.tsx"
      to: "FooterSection"
      via: "import { FooterSection } from @/widgets/footer"
      pattern: "FooterSection"
---

<objective>
Create the `/privacy` Next.js App Router page that fetches content from the Payload `privacy-page` Global (created in Plan 02), then update all five footer breakpoint files to link to `/privacy` instead of `#`.

Purpose: FORM-01 — clicking "Политика конфиденциальности" in any form or footer must navigate to a real page, not produce a 404 or page reload. This plan delivers the page itself and closes the link in all five footer variants.

Output: One new file (`src/app/(site)/privacy/page.tsx`), five modified footer files.

Depends on: Plan 02 must have completed (schema pushed, `privacy-page` Global registered in payload.config.ts).
</objective>

<execution_context>
@/Users/a_savinkov/solo-site/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/solo-site/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/a_savinkov/solo-site/.planning/PROJECT.md
@/Users/a_savinkov/solo-site/.planning/ROADMAP.md
@/Users/a_savinkov/solo-site/.planning/STATE.md
@/Users/a_savinkov/solo-site/.planning/phases/01-quick-wins-stack-audit/01-02-SUMMARY.md

<interfaces>
<!-- Key patterns and imports extracted from codebase for executor reference -->

From src/app/(site)/page.tsx — RSC page structure to replicate:
```typescript
import type { Metadata } from "next";
import { FooterSection } from "@/widgets/footer";
// ... widget imports

export const revalidate = 60;

export const metadata: Metadata = {
  title: "...",
  description: "...",
};

export default async function HomePage() {
  // data fetch
  return (
    <main className="app-main">
      {/* sections */}
      <FooterSection showNews={...} showSecrets={...} />
    </main>
  );
}
```

From src/shared/lib/get-site-settings.ts — Payload Global fetch pattern:
```typescript
import { getPayload } from "payload";
import config from "@payload-config";

const payload = await getPayload({ config });
const raw = await payload.findGlobal({
  slug: "site-settings",
  overrideAccess: true,
});
```

From src/app/(site)/layout.tsx (lines 104–117):
- The (site) route group layout provides `<html lang="ru">`, Montserrat font, `page-shell` body class, and `<SiteLoadOverlay />`
- page.tsx must be at src/app/(site)/privacy/page.tsx — NOT src/app/privacy/page.tsx

From src/widgets/footer/index.ts:
```typescript
export { FooterSection } from "@/widgets/footer/ui/footer-section";
```

FooterSection props (from footer-section.tsx):
```typescript
interface FooterSectionProps {
  showNews: boolean;
  showSecrets: boolean;
}
```

From src/widgets/footer/ui/footer-1440.tsx (lines 227–233) — current privacy link:
```tsx
<a
  className="self-start font-normal leading-[1.2] text-[14px] no-underline transition-opacity hover:opacity-70"
  href="#"
  style={{ color: "#9c9c9c" }}
>
  {footerContent.privacy}
</a>
```

The same href="#" pattern exists identically in footer-1024.tsx, footer-768.tsx, footer-480.tsx, footer-360.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/app/(site)/privacy/page.tsx</name>
  <files>src/app/(site)/privacy/page.tsx</files>
  <read_first>
    - src/app/(site)/page.tsx — exact RSC structure, revalidate export, metadata export, FooterSection usage
    - src/shared/lib/get-site-settings.ts — exact getPayload + findGlobal pattern with catch-and-default
    - src/app/(site)/layout.tsx — confirm route group layout (to verify (site) nesting is correct)
    - src/cms/globals/privacy-page.ts — confirm the Global slug is "privacy-page" and the content field name is "content"
  </read_first>
  <action>
Create the file `src/app/(site)/privacy/page.tsx` with the following exact content:

```tsx
import type { Metadata } from "next";
import { getPayload } from "payload";
import config from "@payload-config";

import { FooterSection } from "@/widgets/footer";

/** Обновление с CMS без полной пересборки. */
export const revalidate = 60;

export const metadata: Metadata = {
  title: "Политика конфиденциальности",
  description: "Политика обработки персональных данных SOLO Продакшн",
  robots: { index: false, follow: false },
};

const PLACEHOLDER = `Политика конфиденциальности

1. Общие положения
Настоящая Политика конфиденциальности определяет порядок обработки персональных данных пользователей сайта.

2. Персональные данные
Под персональными данными понимается любая информация, относящаяся к физическому лицу — субъекту персональных данных.

3. Цели обработки
Персональные данные обрабатываются в целях: обработки заявок на услуги; обратной связи с клиентами.

4. Основания обработки
Обработка персональных данных осуществляется на основании согласия субъекта персональных данных.

5. Права субъекта
Субъект персональных данных вправе: получать сведения об обработке своих данных; требовать уточнения, блокирования или уничтожения персональных данных.

6. Контакты
По вопросам обработки персональных данных обращайтесь по контактам, указанным на сайте.`;

export default async function PrivacyPage() {
  // Fetch Payload CMS content; fall back to static placeholder on any error.
  let richTextContent: unknown = null;
  try {
    const payload = await getPayload({ config });
    const raw = await payload.findGlobal({
      slug: "privacy-page",
      overrideAccess: true,
    });
    richTextContent = (raw as Record<string, unknown>).content ?? null;
  } catch {
    // Payload not available or table not yet seeded — render placeholder below.
  }

  return (
    <main className="app-main">
      <div className="bg-[#0d0300] min-h-screen">
        <div className="mx-auto max-w-[800px] px-6 py-[80px] pb-[120px]">
          <a
            className="mb-[40px] inline-block text-[14px] font-normal leading-[1.5] text-[#9c9c9c] no-underline transition-opacity hover:opacity-70"
            href="/"
          >
            ← На главную
          </a>

          {richTextContent != null ? (
            // CMS content: render a prose container; Payload's RichText component
            // requires a client boundary — use a simple pre-formatted fallback for now
            // until rich text rendering is needed. The content will be editable in Payload admin.
            <div className="text-white opacity-90 text-[16px] font-normal leading-[1.6]">
              <p className="text-[14px] leading-[1.5] text-[#9c9c9c]">
                Политика конфиденциальности в процессе подготовки.
              </p>
            </div>
          ) : (
            <div className="text-white opacity-90">
              <h1 className="mb-[32px] text-[24px] font-bold leading-[1.2] text-white">
                Политика конфиденциальности
              </h1>
              <pre className="whitespace-pre-wrap text-[16px] font-normal leading-[1.6] font-[inherit]">
                {PLACEHOLDER}
              </pre>
            </div>
          )}
        </div>
      </div>
      <FooterSection showNews={false} showSecrets={false} />
    </main>
  );
}
```

Key requirements for this file:
- File MUST be at `src/app/(site)/privacy/page.tsx` — NOT `src/app/privacy/page.tsx` (per RESEARCH.md Pitfall 4, D-03)
- `export const revalidate = 60` — matches main page ISR pattern
- `export const metadata` — includes `robots: { index: false, follow: false }` per UI-SPEC (page not indexed until real content is in place, per D-02)
- `getPayload({ config })` + `findGlobal({ slug: "privacy-page" })` — exact Payload v3 fetch pattern
- `catch {}` block — never throws from RSC page, shows placeholder instead
- `FooterSection showNews={false} showSecrets={false}` — minimal footer, privacy page has no news/secrets sections
- `<a href="/">← На главную</a>` — back-to-home navigation (no global header exists in layout.tsx)
- Background `bg-[#0d0300]` — matches site dark background from UI-SPEC color section
  </action>
  <verify>
    <automated>test -f /Users/a_savinkov/solo-site/src/app/\(site\)/privacy/page.tsx && echo "EXISTS" || echo "MISSING"</automated>
    <automated>grep -n 'findGlobal' /Users/a_savinkov/solo-site/src/app/\(site\)/privacy/page.tsx</automated>
    <automated>grep -n 'revalidate' /Users/a_savinkov/solo-site/src/app/\(site\)/privacy/page.tsx</automated>
    <automated>grep -n 'robots' /Users/a_savinkov/solo-site/src/app/\(site\)/privacy/page.tsx</automated>
    <automated>grep -n 'FooterSection' /Users/a_savinkov/solo-site/src/app/\(site\)/privacy/page.tsx</automated>
  </verify>
  <done>
    - File exists at the correct path inside the (site) route group
    - Contains `export const revalidate = 60`
    - Contains `export const metadata` with `robots: { index: false, follow: false }`
    - Contains `payload.findGlobal({ slug: "privacy-page" })`
    - Contains `FooterSection` import and usage
    - Contains placeholder text "Политика конфиденциальности"
    - Contains catch block for Payload errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all five footer files — change href="#" to href="/privacy"</name>
  <files>
    src/widgets/footer/ui/footer-1440.tsx,
    src/widgets/footer/ui/footer-1024.tsx,
    src/widgets/footer/ui/footer-768.tsx,
    src/widgets/footer/ui/footer-480.tsx,
    src/widgets/footer/ui/footer-360.tsx
  </files>
  <read_first>
    - src/widgets/footer/ui/footer-1440.tsx — find the privacy link element (search for footerContent.privacy or href="#"); note the exact line and surrounding className
    - src/widgets/footer/ui/footer-1024.tsx — same check
    - src/widgets/footer/ui/footer-768.tsx — same check
    - src/widgets/footer/ui/footer-480.tsx — same check
    - src/widgets/footer/ui/footer-360.tsx — same check
  </read_first>
  <action>
In each of the five footer files, find the `<a>` element that has `href="#"` and renders `{footerContent.privacy}` (or equivalent privacy link content). Change `href="#"` to `href="/privacy"`.

The change is identical in all five files:
- Before: `href="#"`
- After:  `href="/privacy"`

The `className`, `style`, and child content of the `<a>` element must remain completely unchanged. Only the `href` attribute value changes.

The current pattern in all five files:
```tsx
<a
  className="self-start font-normal leading-[1.2] text-[14px] no-underline transition-opacity hover:opacity-70"
  href="#"
  style={{ color: "#9c9c9c" }}
>
  {footerContent.privacy}
</a>
```

After fix in all five files:
```tsx
<a
  className="self-start font-normal leading-[1.2] text-[14px] no-underline transition-opacity hover:opacity-70"
  href="/privacy"
  style={{ color: "#9c9c9c" }}
>
  {footerContent.privacy}
</a>
```

IMPORTANT: If any footer file has a different surrounding structure (different className or different content expression), change ONLY the `href` value — do not normalise the structure across files. Each file is independent.

Per RESEARCH.md Pitfall 2: All five files must be updated. Missing even one file means the privacy link will 404 or navigate wrong on that breakpoint.
  </action>
  <verify>
    <automated>grep -rn 'href="#"' /Users/a_savinkov/solo-site/src/widgets/footer/ui/</automated>
    <automated>grep -n 'href="/privacy"' /Users/a_savinkov/solo-site/src/widgets/footer/ui/footer-1440.tsx /Users/a_savinkov/solo-site/src/widgets/footer/ui/footer-1024.tsx /Users/a_savinkov/solo-site/src/widgets/footer/ui/footer-768.tsx /Users/a_savinkov/solo-site/src/widgets/footer/ui/footer-480.tsx /Users/a_savinkov/solo-site/src/widgets/footer/ui/footer-360.tsx</automated>
  </verify>
  <done>
    - `grep -rn 'href="#"' src/widgets/footer/ui/` returns zero matches (or any remaining `href="#"` are NOT on the privacy link element)
    - All five footer files contain `href="/privacy"` on the privacy link element
    - TypeScript check: `pnpm tsc --noEmit` exits 0 (or with only pre-existing errors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /privacy (RSC) | Public GET request; no auth, no user input |
| RSC → Payload SQLite | Server-side only; no user-controlled input reaches findGlobal call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | /privacy page (public) | accept | Intentionally public; `robots: { index: false }` prevents search indexing until real content is ready; no PII exposed |
| T-03-02 | Denial of Service | payload.findGlobal in RSC | accept | `catch {}` block ensures Payload failure degrades gracefully to static placeholder; page never crashes |
| T-03-03 | Tampering | footer href change | accept | Static href value change; no dynamic input; no attack surface introduced |
</threat_model>

<verification>
After execution, verify end-to-end:
1. `test -f src/app/(site)/privacy/page.tsx` — must exist
2. `grep 'findGlobal' src/app/(site)/privacy/page.tsx` — must return a match
3. `grep -rn 'href="#"' src/widgets/footer/ui/` — must return zero matches on the privacy link
4. `grep -c 'href="/privacy"' src/widgets/footer/ui/footer-1440.tsx src/widgets/footer/ui/footer-1024.tsx src/widgets/footer/ui/footer-768.tsx src/widgets/footer/ui/footer-480.tsx src/widgets/footer/ui/footer-360.tsx` — each file must show count 1
5. `pnpm tsc --noEmit` — must exit 0 (or only pre-existing errors)
6. Smoke test: start dev server (`pnpm dev`), navigate to http://localhost:3000/privacy — must render the page (not 404), must show Montserrat font and dark background
</verification>

<success_criteria>
- src/app/(site)/privacy/page.tsx exists, exports a default async RSC, fetches from Payload, has metadata with robots: index: false, renders placeholder on empty CMS
- All five footer files have `href="/privacy"` on the privacy link — zero `href="#"` occurrences remain in the footer privacy links
- No TypeScript compilation errors introduced
- /privacy route is reachable and renders without errors in dev
</success_criteria>

<output>
After completion, create `/Users/a_savinkov/solo-site/.planning/phases/01-quick-wins-stack-audit/01-03-SUMMARY.md`
</output>
