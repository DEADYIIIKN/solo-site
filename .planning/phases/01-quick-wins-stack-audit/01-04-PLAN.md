---
phase: 01-quick-wins-stack-audit
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/AUDIT-STACK.md
  - .planning/AUDIT-ANIMATIONS.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "A written comparison of Next.js App Router vs React SPA exists with a concrete recommendation"
    - "A written comparison of boneyard-js+CSS vs GSAP vs Framer Motion exists with a concrete recommendation"
    - "The animation audit explicitly addresses Safari compatibility as a named criterion"
    - "Both recommendations are unambiguous — 'Keep Next.js' or 'Migrate to SPA'; 'Use X' for animations"
  artifacts:
    - path: ".planning/AUDIT-STACK.md"
      provides: "Next.js vs SPA comparison table + recommendation per D-08, D-09, D-10"
      contains: "## Recommendation"
    - path: ".planning/AUDIT-ANIMATIONS.md"
      provides: "boneyard+CSS vs GSAP vs Framer Motion comparison table + recommendation per D-11, D-12, D-13, D-14"
      contains: "Safari"
  key_links:
    - from: ".planning/AUDIT-ANIMATIONS.md recommendation"
      to: "Phase 4 Safari + Animations planning"
      via: "D-14: recommendation must account for Safari bugs"
      pattern: "Safari"
---

<objective>
Produce two Markdown audit documents: `.planning/AUDIT-STACK.md` (Next.js App Router vs React SPA for this specific landing page) and `.planning/AUDIT-ANIMATIONS.md` (boneyard-js+CSS vs GSAP vs Framer Motion). Both documents must have a comparison table and a single, unambiguous recommendation.

Purpose: AUDIT-01 documents the rationale for keeping or migrating the current stack — a decision the team needs on record. AUDIT-02 feeds directly into Phase 4 (Safari + Animations): its recommendation must account for known Safari compatibility issues with the current boneyard-js approach, giving Phase 4 a clear library direction.

Output: Two new Markdown files in .planning/.
</objective>

<execution_context>
@/Users/a_savinkov/solo-site/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/solo-site/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/a_savinkov/solo-site/.planning/PROJECT.md
@/Users/a_savinkov/solo-site/.planning/ROADMAP.md
@/Users/a_savinkov/solo-site/.planning/STATE.md

<interfaces>
<!-- Project context relevant to audit content -->

From CONTEXT.md decisions:
- D-08: AUDIT-01 format = comparison table (Next.js App Router vs React SPA) + mandatory clear recommendation
- D-09: AUDIT-01 saved in .planning/AUDIT-STACK.md
- D-10: Recommendation must be specific: "Keep Next.js" or "Migrate to SPA" with reasoning
- D-11: AUDIT-02 compares three options: current (boneyard-js + CSS transitions), GSAP, Framer Motion
- D-12: AUDIT-02 format = comparison table with columns (criterion | boneyard+CSS | GSAP | Framer Motion) + clear recommendation
- D-13: AUDIT-02 saved in .planning/AUDIT-ANIMATIONS.md
- D-14: AUDIT-02 recommendation must account for Safari bugs from Phase 4

From RESEARCH.md standard stack:
- Next.js 15.4.11, React 19, Tailwind CSS 4.1.4, Payload CMS 3.82.0, SQLite adapter — all already installed
- boneyard-js ^1.7.6 — currently installed
- GSAP 3.x — not installed (audit candidate)
- Framer Motion 11.x — not installed (audit candidate)

From STATE.md concerns:
- "boneyard-js + CSS transitions may need Safari-specific vendor prefix audit before Phase 4"

From ROADMAP.md Phase 4 goal:
- "Make animations and transitions run identically in Safari and Chrome"
- SAFARI-01, SAFARI-02, ANI-01, ANI-02 requirements all depend on animation library choice

Site characteristics relevant to audit:
- Single-page landing (not a multi-page app)
- Payload CMS for content management (requires server-side rendering for ISR)
- 4 breakpoints: 1440px, 1180px, 820px, 360px
- Current build: Next.js 15 App Router, React 19, Tailwind 4
- Animations: scroll-driven cases transition, CSS transitions for hover states, boneyard-js skeleton abstraction
- No client-side routing between pages (single landing page)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT-STACK.md — Next.js App Router vs React SPA</name>
  <files>.planning/AUDIT-STACK.md</files>
  <read_first>
    - .planning/ROADMAP.md — Phase 1 goal and AUDIT-01 requirement context
    - .planning/REQUIREMENTS.md — AUDIT-01 definition
    - src/payload.config.ts — confirms Payload CMS is a core dependency (affects SPA viability)
    - src/app/(site)/page.tsx — confirms how pages fetch data from Payload (server-side, RSC)
  </read_first>
  <action>
Create `.planning/AUDIT-STACK.md` with the following structure and content:

**Required structure (per D-08, D-09, D-10):**

1. Header with audit date and author (Claude)
2. Context section: site description and why this decision matters
3. Comparison table: Next.js App Router vs React SPA (columns: Criterion | Next.js App Router | React SPA)
4. A `## Recommendation` section with an unambiguous verdict: "Keep Next.js App Router" or "Migrate to React SPA"

**Criteria to cover in the comparison table (must include all of these):**

| Criterion | Description |
|-----------|-------------|
| Payload CMS integration | How well each approach integrates with Payload 3.x server-side data fetching |
| SEO / crawlability | Server-rendered HTML vs client-rendered (relevant for a landing page) |
| ISR / revalidation | Next.js ISR (`revalidate = 60`) vs manual cache approaches in SPA |
| Bundle size | Typical client-side JS payload difference |
| Migration cost | Effort to migrate from current Next.js 15 codebase |
| Deployment complexity | Current Payload + Next.js monorepo vs separate frontend + API |
| Developer experience | Current team familiarity; hot reload, routing |
| Long-term maintenance | Framework trajectory, community, LTS |

**Recommendation reasoning must address:**
- The site uses Payload CMS for server-side data fetching via `getPayload()` in RSC pages
- Migration to SPA would require either: (a) Payload as a separate API server + REST/GraphQL calls, or (b) abandoning Payload admin for content
- The site is a single landing page — no complex client routing need
- The existing codebase is already a well-structured Next.js 15 App Router project

**Expected recommendation:** "Keep Next.js App Router" — but reason through it; do not state the conclusion without the table.

The file must contain:
- `# Аудит стека: Next.js App Router vs React SPA` as h1
- `**Дата аудита:**` field
- `## Контекст` section
- `## Сравнение` section with a Markdown table
- `## Recommendation` section (in English or Russian — your choice, must be clear)
- The recommendation must be a single declarative sentence beginning with "Recommendation:" or "Рекомендация:"
  </action>
  <verify>
    <automated>test -f /Users/a_savinkov/solo-site/.planning/AUDIT-STACK.md && echo "EXISTS" || echo "MISSING"</automated>
    <automated>grep -n '## Recommendation\|## Рекомендация' /Users/a_savinkov/solo-site/.planning/AUDIT-STACK.md</automated>
    <automated>grep -n 'Next.js\|SPA' /Users/a_savinkov/solo-site/.planning/AUDIT-STACK.md | head -5</automated>
  </verify>
  <done>
    - .planning/AUDIT-STACK.md exists
    - Contains a Markdown table comparing Next.js App Router vs React SPA
    - Contains a `## Recommendation` (or `## Рекомендация`) section
    - The recommendation section contains a specific verdict ("Keep Next.js" equivalent — not "it depends")
  </done>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT-ANIMATIONS.md — boneyard-js+CSS vs GSAP vs Framer Motion</name>
  <files>.planning/AUDIT-ANIMATIONS.md</files>
  <read_first>
    - .planning/ROADMAP.md — Phase 4 (Safari + Animations) goal, SAFARI-01, SAFARI-02, ANI-01, ANI-02 requirements
    - .planning/REQUIREMENTS.md — AUDIT-02, SAFARI-01, SAFARI-02, ANI-01, ANI-02 definitions
    - src/shared/ui/boneyard-skeleton.tsx — current animation abstraction, to understand what boneyard-js does in this codebase
    - package.json — confirm boneyard-js is installed, GSAP/Framer Motion are not
  </read_first>
  <action>
Create `.planning/AUDIT-ANIMATIONS.md` with the following structure and content:

**Required structure (per D-11, D-12, D-13, D-14):**

1. Header with audit date
2. Context section: current animation approach and Phase 4 dependency
3. Comparison table: columns = Criterion | boneyard-js + CSS | GSAP | Framer Motion
4. `## Recommendation` section with an unambiguous library choice

**Criteria to cover in the comparison table (must include ALL of these):**

| Criterion | Description |
|-----------|-------------|
| Safari compatibility | Known issues with CSS transitions/animations in Safari; vendor prefix requirements; scroll-driven animation support |
| React 19 integration | How well each library integrates with React 19 concurrent features |
| Scroll-driven animations | Support for scroll-based transitions (cases section ANI-01) |
| Bundle size | npm package size impact |
| Learning curve | Time to adopt for a solo developer |
| CSS transitions support | Whether library replaces or complements CSS transitions |
| Declarative API | React-friendly component API vs imperative timeline API |
| Installation required | Whether library requires adding a new npm dependency |
| Active maintenance | Release frequency, community, GitHub stars (as of 2025) |

**Safari compatibility criterion must explicitly state (per D-14):**
- CSS transitions: `will-change`, `transform`, `opacity` generally work; `scroll-timeline` and `animation-timeline` NOT supported in Safari < 18
- boneyard-js: a skeleton/reveal animation library; relies on Intersection Observer + CSS transitions; IntersectionObserver is well-supported in Safari, but scroll-driven animation APIs may have gaps
- GSAP: explicitly maintains Safari compatibility; has been tested against Safari for years; uses requestAnimationFrame internally, not CSS animation APIs that Safari lags on
- Framer Motion: generally good Safari support; uses `transform`/`opacity` for hardware acceleration; some ScrollLinked animation features use CSS scroll-timeline (limited Safari support) but fallback exists

**Current state fact to include:** The existing codebase uses boneyard-js for skeleton/reveal animations and CSS `transition` classes for hover states. The cases section scroll animation (ANI-01) is the primary complex animation.

**Recommendation reasoning must address:**
- Phase 4 requirement: animations must work identically in Safari and Chrome
- Current boneyard-js + CSS may have Safari gaps (STATE.md concern)
- Migration cost vs Safari reliability tradeoff
- Whether GSAP or Framer Motion is the better replacement given the existing React 19 setup

**The file must contain:**
- `# Аудит анимаций: boneyard-js + CSS vs GSAP vs Framer Motion` as h1
- `**Дата аудита:**` field
- `## Контекст` section mentioning Phase 4 and Safari requirement
- `## Сравнение` section with a 4-column Markdown table (Criterion | boneyard+CSS | GSAP | Framer Motion)
- `## Recommendation` section with a single declarative sentence beginning with "Recommendation:" or "Рекомендация:"
- Safari must appear as a named row in the comparison table
  </action>
  <verify>
    <automated>test -f /Users/a_savinkov/solo-site/.planning/AUDIT-ANIMATIONS.md && echo "EXISTS" || echo "MISSING"</automated>
    <automated>grep -n '## Recommendation\|## Рекомендация' /Users/a_savinkov/solo-site/.planning/AUDIT-ANIMATIONS.md</automated>
    <automated>grep -in 'safari' /Users/a_savinkov/solo-site/.planning/AUDIT-ANIMATIONS.md | head -5</automated>
    <automated>grep -n 'GSAP\|Framer' /Users/a_savinkov/solo-site/.planning/AUDIT-ANIMATIONS.md | head -5</automated>
  </verify>
  <done>
    - .planning/AUDIT-ANIMATIONS.md exists
    - Contains a 4-column Markdown table (Criterion + 3 library columns)
    - Safari appears as a named criterion row in the table
    - Contains a `## Recommendation` section with a specific library choice (not "it depends")
    - Phase 4 is mentioned in the context section as the downstream dependency
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | These are static Markdown files in .planning/; no runtime component, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | N/A | Static Markdown documents | accept | No runtime attack surface; documents are internal planning artifacts only |
</threat_model>

<verification>
After execution, verify:
1. `test -f .planning/AUDIT-STACK.md` — must exist
2. `test -f .planning/AUDIT-ANIMATIONS.md` — must exist
3. `grep -c '##' .planning/AUDIT-STACK.md` — must be ≥ 3 (context, comparison, recommendation sections)
4. `grep -c '##' .planning/AUDIT-ANIMATIONS.md` — must be ≥ 3
5. `grep -i 'recommendation\|рекомендация' .planning/AUDIT-STACK.md` — must return a match
6. `grep -i 'recommendation\|рекомендация' .planning/AUDIT-ANIMATIONS.md` — must return a match
7. `grep -i 'safari' .planning/AUDIT-ANIMATIONS.md` — must return ≥ 1 match
</verification>

<success_criteria>
- .planning/AUDIT-STACK.md exists with a comparison table and an unambiguous "Keep Next.js App Router" or "Migrate to SPA" recommendation (per D-10)
- .planning/AUDIT-ANIMATIONS.md exists with a 3-library comparison table that names Safari as a criterion and ends with an unambiguous library recommendation (per D-14)
- Both documents are complete and self-contained — a developer reading them cold can understand the decision without additional context
</success_criteria>

<output>
After completion, create `/Users/a_savinkov/solo-site/.planning/phases/01-quick-wins-stack-audit/01-04-SUMMARY.md`
</output>
