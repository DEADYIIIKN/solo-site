---
phase: 03-mobile-tablet-layout-820px-360px
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/cases/ui/cases-section-768.tsx
  - src/widgets/cases/ui/cases-section-360.tsx
autonomous: true
requirements:
  - LY820-02
  - LY360-02

must_haves:
  truths:
    - "На 820px последняя карточка карусели видна полностью без обрезки правого края"
    - "На 360px последняя карточка карусели видна полностью без обрезки правого края"
    - "useCasesHorizontalCarousel scroll-логика не изменялась"
    - "Константы VERT_CARD_W и AD_CARD_W не изменялись"
  artifacts:
    - path: "src/widgets/cases/ui/cases-section-768.tsx"
      provides: "Контейнер карусели с padding/max-width соответствующим Figma 783:12001"
    - path: "src/widgets/cases/ui/cases-section-360.tsx"
      provides: "Контейнер карусели с padding/max-width соответствующим Figma 783:11420"
  key_links:
    - from: "cases-section-768.tsx строка ~230"
      to: "scroll container div"
      via: "overflow-x-clip на outer wrapper"
      pattern: "max-w-\\[768px\\]|overflow-x-clip|px-12"
---

<objective>
Исправить обрезку последней карточки карусели кейсов на 820px (LY820-02) и 360px (LY360-02).

Purpose: Внешний wrapper с overflow-x-clip и padding ограничивает видимую область так, что последняя карточка частично скрыта. Figma MCP определяет точный padding контейнера — executor читает значения из нод 783:12001 и 783:11420.
Output: Обе секции кейсов отображают последнюю карточку полностью при прокрутке к концу карусели.
</objective>

<execution_context>
@/Users/a_savinkov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-CONTEXT.md
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-UI-SPEC.md
</context>

<interfaces>
<!-- Извлечено при планировании -->

cases-section-768.tsx строки 206-307 — структура контейнера:
```tsx
// Строка 230: внешний wrapper с overflow-x-clip
<div className="relative mx-auto w-full max-w-[768px] overflow-x-clip pb-[140px]">
  // Строка 231: inner с px-12 (48px padding)
  <div className="relative z-[1] px-12 pt-20">
    // ...
    // Строка ~254: scroll container для вертикальных карточек
    <div className="flex min-w-0 gap-4 overflow-x-auto overflow-y-hidden pb-1 pt-2 no-scrollbar" ...>
    // Строка ~286: scroll container для рекламных карточек
    <div className="flex min-w-0 gap-4 overflow-x-auto overflow-y-hidden pb-1 no-scrollbar" ...>
```

cases-section-360.tsx строки 206-310 — структура контейнера:
```tsx
// Строка 230: внешний wrapper с overflow-x-clip
<div className="relative mx-auto w-full max-w-[360px] overflow-x-clip pb-[120px]">
  // Строка 231: inner с px-4 (16px padding)
  <div className="relative z-[1] flex flex-col gap-[100px] px-4 pt-[70px]">
    // ...
    // Строки ~254, ~289: scroll containers
```

НЕИЗМЕНЯЕМЫЕ константы (per D-03):
- cases-section-768.tsx: VERT_CARD_W=242, AD_CARD_W=414, CASES_SCROLL_GAP_PX=16
- cases-section-360.tsx: VERT_CARD_W=208, AD_CARD_W=242, CASES_SCROLL_GAP_PX=16
- useCasesHorizontalCarousel — НЕ ТРОГАТЬ
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Figma MCP — извлечь padding контейнеров карусели (нод 783:12001 и 783:11420)</name>
  <files>src/widgets/cases/ui/cases-section-768.tsx, src/widgets/cases/ui/cases-section-360.tsx</files>
  <read_first>
    - src/widgets/cases/ui/cases-section-768.tsx (строки 206-307 — вся секция компонента CasesSection768)
    - src/widgets/cases/ui/cases-section-360.tsx (строки 206-310 — вся секция компонента CasesSection360)
  </read_first>
  <action>
**Шаг 1 — Figma MCP для 768px (нод 783:12001):**

Использовать Figma MCP `get_design_context` или `get_metadata` на ноде 783:12001.
Искать: `paddingLeft`, `paddingRight`, `paddingTop`, `paddingBottom` контейнера карусели.
Записать точные значения px.

**Шаг 2 — Figma MCP для 360px (нод 783:11420):**

То же самое для ноды 783:11420.
Записать точные значения padding px.

**Шаг 3 — Диагностика обрезки:**

Проблема: `overflow-x-clip` на outer wrapper (строка ~230 в обоих файлах) обрезает карточки.
Когда scroll container прокручен до конца, последняя карточка упирается в padding внутреннего div.
Но `overflow-x-clip` не позволяет контенту выйти за пределы max-width контейнера.

**Шаг 4 — Применить фикс в cases-section-768.tsx:**

Вариант A: Убрать `overflow-x-clip` с outer wrapper и добавить достаточный `padding-right` на scroll container для последней карточки.

Конкретно: если Figma показывает что правый padding должен быть N px — добавить `pr-[Npx]` на div с overflow-x-auto.
Или изменить `max-w-[768px]` чтобы scroll container мог показать полную карточку.

Правило: не менять `px-12` (48px) если Figma его подтверждает; не трогать VERT_CARD_W, AD_CARD_W.

Если проблема в overflow-x-clip — убрать его с outer wrapper, так как scroll container сам управляет overflow.

**Шаг 5 — Применить аналогичный фикс в cases-section-360.tsx:**

Применить соответствующие значения из ноды 783:11420.
Аналогичный подход: убрать overflow-x-clip с outer wrapper если он обрезает,
и/или скорректировать padding чтобы последняя карточка была полностью видна.

**ЗАПРЕЩЕНО:** изменять VERT_CARD_W, AD_CARD_W, CASES_SCROLL_GAP_PX, useCasesHorizontalCarousel.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    Дополнительно: проверить что константы не изменились:
    `grep -n "VERT_CARD_W\|AD_CARD_W\|CASES_SCROLL_GAP_PX" /Users/a_savinkov/solo-site/src/widgets/cases/ui/cases-section-768.tsx`
    `grep -n "VERT_CARD_W\|AD_CARD_W\|CASES_SCROLL_GAP_PX" /Users/a_savinkov/solo-site/src/widgets/cases/ui/cases-section-360.tsx`
  </verify>
  <acceptance_criteria>
    - TypeScript компилируется без ошибок
    - В cases-section-768.tsx: `VERT_CARD_W = 242`, `AD_CARD_W = 414`, `CASES_SCROLL_GAP_PX = 16` — не изменились
    - В cases-section-360.tsx: `VERT_CARD_W = 208`, `AD_CARD_W = 242`, `CASES_SCROLL_GAP_PX = 16` — не изменились
    - В обоих файлах отсутствует `useCasesHorizontalCarousel` среди изменённых функций (hook не трогался)
    - Figma-значения padding задокументированы в SUMMARY (для трассируемости)
    - Scroll containers (overflow-x-auto) не имеют overflow-x-clip на прямом родителе или outer wrapper
  </acceptance_criteria>
  <done>
    Padding контейнера в обоих файлах соответствует Figma-значениям из нод 783:12001 и 783:11420.
    overflow-x-clip не блокирует видимость последней карточки при прокрутке.
    TypeScript без ошибок.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет внешних входов | Layout-only изменения в CSS классах |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | cases-section-768.tsx, cases-section-360.tsx | accept | Риск регрессии card widths — acceptance criteria проверяет константы grep-ом |
</threat_model>

<verification>
1. `npx tsc --noEmit` — без ошибок
2. grep: VERT_CARD_W, AD_CARD_W, CASES_SCROLL_GAP_PX не изменились в обоих файлах
3. Визуальная проверка: при прокрутке до последней карточки на 820px — карточка видна полностью
4. Визуальная проверка: при прокрутке до последней карточки на 360px — карточка видна полностью
</verification>

<success_criteria>
- TypeScript компилируется без ошибок
- На 820px (viewport): последняя вертикальная и рекламная карточка показываются полностью
- На 360px (viewport): то же самое
- Константы карточек не изменились
- useCasesHorizontalCarousel не изменялся
</success_criteria>

<output>
После завершения создать `.planning/phases/03-mobile-tablet-layout-820px-360px/03-02-SUMMARY.md`
</output>
