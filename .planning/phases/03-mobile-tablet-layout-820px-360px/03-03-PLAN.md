---
phase: 03-mobile-tablet-layout-820px-360px
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx
  - src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1024.tsx
autonomous: true
requirements:
  - LY820-03
  - LY360-04

must_haves:
  truths:
    - "На 820px ленты с логотипами клиентов занимают полную ширину viewport без видимой обрезки"
    - "На 360px gap между логотипами соответствует Figma-значению (не 60px)"
    - "Горизонтальный скроллбар на странице не появляется"
    - "На 768px и 1024px gap в маркизе остался 60px (дефолтное значение не изменилось)"
  artifacts:
    - path: "src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx"
      provides: "PhilosophyClientsNarrowClientsBlock с 100vw breakout вместо overflow-x-clip"
      contains: "100vw"
    - path: "src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1024.tsx"
      provides: "PhilosophyClientsMarquee1024 с gapPx prop, default=60"
  key_links:
    - from: "philosophy-clients-narrow-stack.tsx PhilosophyClientsNarrowClientsBlock"
      to: "PhilosophyClientsMarquee1024"
      via: "gapPx prop"
      pattern: "gapPx|gap-\\[60px\\]"
---

<objective>
Исправить ширину полос логотипов клиентов на 820px (LY820-03) и gap между логотипами на 360px (LY360-04).

Purpose: PhilosophyClientsNarrowClientsBlock имеет overflow-x-clip внутри контейнера с ограниченной шириной — та же проблема что в Phase 2. На 360px gap 60px слишком большой согласно Figma.
Output: Полосы логотипов выходят на всю ширину viewport на 820px. Gap на 360px соответствует Figma.
</objective>

<execution_context>
@/Users/a_savinkov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-CONTEXT.md
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-UI-SPEC.md
@.planning/phases/02-desktop-layout-1440px-1180px/02-03-PLAN.md
</context>

<interfaces>
<!-- Извлечено при планировании -->

philosophy-clients-narrow-stack.tsx строки 382-429 — PhilosophyClientsNarrowClientsBlock:
```tsx
export function PhilosophyClientsNarrowClientsBlock({
  eyebrowPlClassName,
  clientsEyebrowStyle = "default",
}: { eyebrowPlClassName: string; clientsEyebrowStyle?: "default" | "narrow" }) {
  const [clientsRef, clientsInView] = useInViewOnce<HTMLDivElement>();

  return (
    <>
      <div className="h-10 w-full shrink-0 bg-white" aria-hidden />
      <div ref={clientsRef} className="relative z-[5] w-full overflow-x-visible overflow-y-visible rounded-bl-[60px] rounded-br-[60px] bg-white pb-10">
        <div className={cn(eyebrowPlClassName, REVEAL, ...)}> ... </div>
        // СТРОКА ~419-428: wrapper маркизы — ЗДЕСЬ overflow-x-clip
        <div
          className={cn(
            "relative mt-8 h-[400px] w-full max-w-full overflow-x-clip",
            REVEAL,
            clientsInView ? REVEAL_ON : REVEAL_OFF,
          )}
          style={{ transitionDelay: clientsInView ? "100ms" : "0ms" }}
        >
          <PhilosophyClientsMarquee1024 />   // ← сюда нужно передать gapPx
        </div>
      </div>
    </>
  );
}
```

philosophy-clients-marquee-1024.tsx строки 9-10 — текущий gap:
```tsx
/** Figma 783:8703 / 783:8785 — gap между логотипами 60px, полоса 90px, padding 20px */
const MARQUEE_GAP_PX = 60;
```

Строки 181-182, 240-242 — где используется gap в классах:
```tsx
// darkStripClass (строка 240):
"philosophy-marquee-strip relative inline-flex h-[90px] min-w-max max-w-none flex-nowrap shrink-0 items-center gap-[60px] bg-[#0d0300] p-[20px]"
// orangeStripClass (строка 241):
"philosophy-marquee-strip relative inline-flex h-[90px] min-w-max max-w-none flex-nowrap shrink-0 items-center gap-[60px] bg-[#ff5c00] p-[20px]"
// MarqueeTrack trackBg — gap-[60px] также в MarqueeTrack внутренних div (строка 181):
`philosophy-marquee-track flex w-max gap-[60px] ${trackBgClass}`
```

Паттерн из Phase 2 (02-03-PLAN.md Task 1):
```tsx
// 100vw breakout — заменить overflow-x-clip wrapper:
<div
  className="relative mt-8 h-[400px]"
  style={{ width: "100vw", marginLeft: "calc(50% - 50vw)" }}
>
  <PhilosophyClientsMarquee1024 />
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Figma MCP — gap логотипов 360px + добавить gapPx prop в PhilosophyClientsMarquee1024</name>
  <files>src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1024.tsx</files>
  <read_first>
    - src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1024.tsx (весь файл — особенно строки 1-20, 140-200, 238-265)
  </read_first>
  <action>
**Шаг 1 — Figma MCP для gap 360px:**

Использовать Figma MCP `get_design_context` на листе «Адаптивы актуальные», секция клиентов 360px.
Искать: gap между logo-слотами в marquee strip.
Записать точное px значение (N).

**Шаг 2 — Добавить gapPx prop в MarqueeTrack:**

Найти функцию `MarqueeTrack` (строки ~140-195). Добавить `gapPx` prop с default=60:

```tsx
function MarqueeTrack({
  durationSec,
  stripClassName,
  trackBgClass,
  gapPx = 60,    // ← добавить prop с default
}: {
  durationSec: number;
  stripClassName: string;
  trackBgClass: string;
  gapPx?: number;  // ← добавить в тип
}) {
  // ...
  // Строка ~181: заменить gap-[60px] на динамическое значение
  // Использовать style вместо className для gap:
```

В div с flex/gap заменить `gap-[60px]` на `style={{ gap: gapPx }}`:
```tsx
<div
  className={shiftPx > 0
    ? `philosophy-marquee-track flex w-max ${trackBgClass}`
    : `flex w-max ${trackBgClass}`}
  style={{ ...style, gap: gapPx }}   // добавить gap в style
>
```

**Шаг 3 — Добавить gapPx prop в PhilosophyClientsMarquee1024:**

```tsx
export function PhilosophyClientsMarquee1024({ gapPx = 60 }: { gapPx?: number } = {}) {
```

Обновить darkStripClass и orangeStripClass: убрать `gap-[60px]` из строковых литералов,
заменить на передачу `gapPx` в MarqueeTrack:

```tsx
// darkStripClass (строка ~240) — убрать gap-[60px]:
const darkStripClass =
  "philosophy-marquee-strip relative inline-flex h-[90px] min-w-max max-w-none flex-nowrap shrink-0 items-center bg-[#0d0300] p-[20px]";
// orangeStripClass — аналогично убрать gap-[60px]:
const orangeStripClass =
  "philosophy-marquee-strip relative inline-flex h-[90px] min-w-max max-w-none flex-nowrap shrink-0 items-center bg-[#ff5c00] p-[20px]";
```

Передать gapPx в MarqueeTrack вызовах (строки ~248, ~256):
```tsx
<MarqueeTrack durationSec={85} stripClassName={darkStripClass} trackBgClass="bg-[#0d0300]" gapPx={gapPx}>
<MarqueeTrack durationSec={72} stripClassName={orangeStripClass} trackBgClass="bg-[#ff5c00]" gapPx={gapPx}>
```

**ВАЖНО:** Дефолтное значение gapPx=60 сохраняет текущее поведение для 768px и 1024px.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    Дополнительно:
    `grep -n "gapPx" /Users/a_savinkov/solo-site/src/widgets/philosophy-clients/ui/philosophy-clients-marquee-1024.tsx`
    Ожидаемый вывод: минимум 4 строки (prop в MarqueeTrack, prop в Marquee1024, 2× передача в вызовах)
  </verify>
  <acceptance_criteria>
    - TypeScript компилируется без ошибок
    - PhilosophyClientsMarquee1024 экспортируется с сигнатурой `({ gapPx = 60 }: { gapPx?: number } = {})`
    - MarqueeTrack имеет prop `gapPx?: number` с default=60
    - gap-[60px] удалён из darkStripClass и orangeStripClass строковых литералов
    - gapPx передаётся в оба вызова MarqueeTrack
    - Figma-значение gap 360px задокументировано в SUMMARY
  </acceptance_criteria>
  <done>
    PhilosophyClientsMarquee1024 принимает gapPx prop. MarqueeTrack использует gapPx для gap. Default=60 сохраняет текущее поведение.
  </done>
</task>

<task type="auto">
  <name>Task 2: Применить 100vw breakout и передать gapPx в PhilosophyClientsNarrowClientsBlock (per D-05, D-06, D-10)</name>
  <files>src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx</files>
  <read_first>
    - src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx (строки 382-432 — PhilosophyClientsNarrowClientsBlock)
  </read_first>
  <action>
**Шаг 1 — Найти вызов PhilosophyClientsMarquee1024 (строки ~419-428):**

Текущий код:
```tsx
<div
  className={cn(
    "relative mt-8 h-[400px] w-full max-w-full overflow-x-clip",
    REVEAL,
    clientsInView ? REVEAL_ON : REVEAL_OFF,
  )}
  style={{ transitionDelay: clientsInView ? "100ms" : "0ms" }}
>
  <PhilosophyClientsMarquee1024 />
</div>
```

**Шаг 2 — Применить 100vw breakout (per D-06):**

Заменить на:
```tsx
<div
  className={cn(
    "relative mt-8 h-[400px]",
    REVEAL,
    clientsInView ? REVEAL_ON : REVEAL_OFF,
  )}
  style={{
    width: "100vw",
    marginLeft: "calc(50% - 50vw)",
    transitionDelay: clientsInView ? "100ms" : "0ms",
  }}
>
  <PhilosophyClientsMarquee1024 gapPx={GAP_360} />
</div>
```

Где GAP_360 — это число, полученное из Figma MCP в Task 1 (записать его как константу вверху функции или передать напрямую).

**Шаг 3 — Удалить overflow-x-clip:**

`overflow-x-clip` убран из className в шаге 2. `w-full max-w-full` тоже убраны (заменены width: 100vw через style).

**Шаг 4 — Проверить горизонтальный скроллбар:**

Если после этого изменения появляется горизонтальный скроллбар — проверить overflow родительских элементов.
Родительский div (строка ~397): `overflow-x-visible overflow-y-visible` — это нормально.
Если скроллбар всё равно есть — добавить `overflow-x: hidden` на этот родительский div, НЕ на прямой родитель маркизы.

**ЗАПРЕЩЕНО:** добавлять overflow-x-clip обратно на wrapper маркизы.
  </action>
  <verify>
    <automated>grep -n "100vw\|overflow-x-clip\|gapPx" /Users/a_savinkov/solo-site/src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx</automated>
    Ожидаемый вывод:
    - строка с "100vw" — должна присутствовать
    - строка с "overflow-x-clip" в маркиза-wrapper — должна ОТСУТСТВОВАТЬ
    - строка с "gapPx" — должна присутствовать (передача в PhilosophyClientsMarquee1024)
    Дополнительно: `npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <acceptance_criteria>
    - TypeScript компилируется без ошибок
    - philosophy-clients-narrow-stack.tsx содержит `width: "100vw"` и `marginLeft: "calc(50% - 50vw)"`
    - philosophy-clients-narrow-stack.tsx НЕ содержит `overflow-x-clip` в маркиза-wrapper
    - PhilosophyClientsMarquee1024 вызывается с `gapPx={N}` где N — Figma-значение для 360px (не 60)
    - Файл компилируется без TypeScript ошибок
  </acceptance_criteria>
  <done>
    PhilosophyClientsNarrowClientsBlock использует 100vw breakout для маркизы и передаёт Figma-корректный gapPx.
    overflow-x-clip удалён с wrapper маркизы.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет внешних входов | Layout и prop-изменения в UI компонентах |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | philosophy-clients-narrow-stack.tsx | accept | 100vw breakout может создать горизонтальный скроллбар — action содержит инструкции по запасному варианту |
| T-03-04 | Tampering | philosophy-clients-marquee-1024.tsx | accept | Изменение API компонента может сломать другие вызовы — TypeScript проверяет все call sites |
</threat_model>

<verification>
1. `npx tsc --noEmit` — без ошибок
2. grep: `100vw` присутствует в narrow-stack, `overflow-x-clip` отсутствует в маркиза-wrapper
3. grep: `gapPx` присутствует в marquee-1024 (минимум 4 строки) и в narrow-stack
4. Визуальная проверка 820px: ленты логотипов на всю ширину viewport
5. Визуальная проверка 360px: gap меньше 60px, соответствует Figma
6. Горизонтальный скроллбар отсутствует на 820px и 360px
7. Визуальная проверка 1024px: gap в маркизе не изменился (остался 60px дефолт)
</verification>

<success_criteria>
- TypeScript без ошибок
- На 820px: ленты логотипов клиентов занимают 100% ширины viewport без обрезки
- На 360px: gap между логотипами = Figma-значение из nodes секции клиентов 360px
- На 1024px: gap в маркизе = 60px (не изменился)
- Горизонтальный скроллбар на странице не появился
</success_criteria>

<output>
После завершения создать `.planning/phases/03-mobile-tablet-layout-820px-360px/03-03-SUMMARY.md`
</output>
