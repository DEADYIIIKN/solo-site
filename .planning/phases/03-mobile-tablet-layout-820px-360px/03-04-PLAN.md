---
phase: 03-mobile-tablet-layout-820px-360px
plan: 04
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - src/widgets/cases/ui/cases-section-768.tsx
  - src/widgets/cases/ui/cases-section-360.tsx
autonomous: true
requirements:
  - LY820-04
  - LY360-05

must_haves:
  truths:
    - "Строки 'Режиссер' и 'DOP' в карточках кейсов на 820px имеют line-height из Figma (не leading-[1.2])"
    - "Строки 'Режиссер' и 'DOP' в карточках кейсов на 360px имеют line-height из Figma"
    - "Отступ между строками credits (mb) соответствует Figma на обоих брейкпоинтах"
    - "Текст credits не перекрывается визуально"
  artifacts:
    - path: "src/widgets/cases/ui/cases-section-768.tsx"
      provides: "VerticalCard768 и AdCard768 с Figma-корректным leading и mb на credits строках"
    - path: "src/widgets/cases/ui/cases-section-360.tsx"
      provides: "VerticalCard360 и AdCard360 с Figma-корректным leading и mb на credits строках"
  key_links:
    - from: "cases-section-768.tsx VerticalCard768"
      to: "credits div"
      via: "leading-[1.2] и mb-[10px]"
      pattern: "leading-\\[1\\.2\\]|mb-\\[10px\\]|mb-\\[6px\\]"
---

<objective>
Исправить line-height и межстрочный отступ в строках credits кейсов (LY820-04, LY360-05).

Purpose: Строки «Режиссер / DOP» используют leading-[1.2] — значение, возможно не совпадающее с Figma. Figma MCP определяет точное значение line-height и mb между строками.
Output: Credits строки в карточках кейсов на 820px и 360px соответствуют Figma без текстового перекрытия.
</objective>

<execution_context>
@/Users/a_savinkov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-CONTEXT.md
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-UI-SPEC.md
</context>

<interfaces>
<!-- Извлечено при планировании -->

cases-section-768.tsx VerticalCard768 — credits блок (строки ~129-134):
```tsx
// Вертикальная карточка 768 — нижний текстовый блок:
<div className="flex min-h-[32px] flex-col justify-end text-[13px] font-normal leading-[1.2]">
  {credits.map((line) => (
    <p className="m-0" key={line}>   // ← нет mb между строками
      {line}
    </p>
  ))}
</div>
```

cases-section-768.tsx AdCard768 — credits блок (строки ~193-197):
```tsx
// Рекламная карточка 768:
<div className="flex min-h-[25px] max-w-[340px] flex-col justify-end text-[13px] font-normal leading-[1.2]">
  {credits.map((line, i) => (
    <p className={cn("m-0 leading-[1.2]", i < credits.length - 1 && "mb-[10px]")} key={line}>
      {line}
    </p>
  ))}
</div>
```

cases-section-360.tsx VerticalCard360 — credits блок (строки ~129-134):
```tsx
// Вертикальная карточка 360:
<div className="flex min-h-[28px] flex-col justify-end text-[11px] font-normal leading-[1.2]">
  {credits.map((line) => (
    <p className="m-0" key={line}>   // ← нет mb между строками
      {line}
    </p>
  ))}
</div>
```

cases-section-360.tsx AdCard360 — credits блок (строки ~193-197):
```tsx
// Рекламная карточка 360:
<div className="flex min-h-[18px] max-w-[220px] flex-col justify-end text-[10px] font-normal leading-[1.2]">
  {credits.map((line, i) => (
    <p className={cn("m-0 leading-[1.2]", i < credits.length - 1 && "mb-[6px]")} key={line}>
      {line}
    </p>
  ))}
</div>
```

Figma nodes для MCP:
- Cases 768: 783:12001 (секция кейсов на 768px)
- Cases 360: 783:11420 (секция кейсов на 360px)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Figma MCP — извлечь line-height и mb credits; обновить cases-section-768.tsx и cases-section-360.tsx</name>
  <files>src/widgets/cases/ui/cases-section-768.tsx, src/widgets/cases/ui/cases-section-360.tsx</files>
  <read_first>
    - src/widgets/cases/ui/cases-section-768.tsx (строки 59-203 — VerticalCard768 и AdCard768 полностью)
    - src/widgets/cases/ui/cases-section-360.tsx (строки 59-204 — VerticalCard360 и AdCard360 полностью)
  </read_first>
  <action>
**Шаг 1 — Figma MCP для 768px (нод 783:12001):**

Использовать Figma MCP `get_design_context` на ноде 783:12001.
Найти text nodes с labels «Режиссер» и «DOP» (credits строки).
Извлечь:
- `lineHeight` (px или ratio) — для замены leading-[1.2]
- расстояние между строками credits (gap или margin)
Записать значения: LINE_HEIGHT_768=N, MB_768=N.

Маппинг Tailwind:
- Если lineHeight задан как ratio (например 1.4): использовать `leading-[1.4]`
- Если lineHeight задан в px (например 16px при font-size 13px = 1.23): `leading-[16px]` или `leading-[1.23]`
- Если mb=8px: `mb-[8px]`, если mb=10px: `mb-[10px]` (оставить без изменений), если mb=0: убрать mb

**Шаг 2 — Figma MCP для 360px (нод 783:11420):**

Аналогично для 360px. Извлечь LINE_HEIGHT_360, MB_360.

**Шаг 3 — Обновить cases-section-768.tsx:**

VerticalCard768 credits div (строка ~129):
- Заменить `leading-[1.2]` на `leading-[LINE_HEIGHT_768]` (значение из Figma)
- Если нужен mb между строками credits — добавить `i < credits.length - 1 && "mb-[MB_768]"` по паттерну AdCard768

AdCard768 credits div (строки ~193-197):
- Заменить `leading-[1.2]` на `leading-[LINE_HEIGHT_768]` в wrapper div
- Заменить `mb-[10px]` на `mb-[MB_768]` в p className (если Figma значение отличается)
- Если Figma значение совпадает с 10px — не трогать

**Шаг 4 — Обновить cases-section-360.tsx:**

VerticalCard360 credits div (строка ~129):
- Заменить `leading-[1.2]` на `leading-[LINE_HEIGHT_360]`
- Если нужен mb — добавить по паттерну AdCard360

AdCard360 credits div (строки ~193-197):
- Заменить `leading-[1.2]` на `leading-[LINE_HEIGHT_360]`
- Заменить `mb-[6px]` на `mb-[MB_360]` (если Figma значение отличается)

**Правило:** Если Figma-значение совпадает с текущим — не трогать. Документировать в SUMMARY все случаи "подтверждено Figma".
**ЗАПРЕЩЕНО:** изменять font-size, font-weight, font-family, цвета, размеры карточек.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    Дополнительно:
    `grep -n "leading-\[1\.2\]" /Users/a_savinkov/solo-site/src/widgets/cases/ui/cases-section-768.tsx`
    `grep -n "leading-\[1\.2\]" /Users/a_savinkov/solo-site/src/widgets/cases/ui/cases-section-360.tsx`
    Если Figma подтвердил что 1.2 — правильное значение, то grep может найти совпадения (и это нормально — задокументировать в SUMMARY).
    Если Figma дал другое значение — grep должен показать 0 совпадений.
  </verify>
  <acceptance_criteria>
    - TypeScript компилируется без ошибок
    - Figma-значения LINE_HEIGHT_768, MB_768, LINE_HEIGHT_360, MB_360 задокументированы в SUMMARY
    - В обоих файлах credits VerticalCard и AdCard используют Figma-корректные значения leading и mb
    - font-size в credits не изменился: 13px (768), 11px (VerticalCard360), 10px (AdCard360)
    - VERT_CARD_W, AD_CARD_W, CASES_SCROLL_GAP_PX не изменились (grep подтверждает)
  </acceptance_criteria>
  <done>
    credits строки в VerticalCard768, AdCard768, VerticalCard360, AdCard360 используют Figma-корректные leading и mb значения.
    TypeScript без ошибок.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет внешних входов | Typography-только изменения в CSS классах |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | cases-section-768.tsx, cases-section-360.tsx | accept | Риск изменить не те строки — acceptance criteria требует проверку font-size и card widths через grep |
</threat_model>

<verification>
1. `npx tsc --noEmit` — без ошибок
2. grep: font-size credits не изменились (13px, 11px, 10px)
3. grep: VERT_CARD_W, AD_CARD_W — не изменились
4. Визуальная проверка 820px: credits строки без перекрытия, line-height соответствует Figma
5. Визуальная проверка 360px: credits строки без перекрытия
</verification>

<success_criteria>
- TypeScript без ошибок
- credits строки кейсов на 820px и 360px не перекрываются визуально
- leading и mb значения задокументированы как Figma-sourced в SUMMARY
- font-size, card dimensions не изменились
</success_criteria>

<output>
После завершения создать `.planning/phases/03-mobile-tablet-layout-820px-360px/03-04-SUMMARY.md`
</output>
