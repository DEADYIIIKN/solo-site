---
phase: 04-safari-animations
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx
autonomous: true
requirements:
  - SAFARI-01
  - ANI-02
must_haves:
  truths:
    - "Philosophy narrow-stack card 03 (team) fades in once when it enters the viewport in Safari and Chrome"
    - "Existing useInViewOnce + CSS reveal classes for the surrounding stack remain untouched (D-09-style conservatism)"
    - "No shimmer placeholder, no <BoneyardSkeleton> wrapper, no teamCardLoaded gate"
  artifacts:
    - path: "src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx"
      provides: "Philosophy narrow-stack reveal — card 03 via motion.div; other cards untouched"
      contains: "motion.div"
  key_links:
    - from: "src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx"
      to: "motion/react"
      via: "import { motion } from \"motion/react\""
      pattern: "from \"motion/react\""
---

<objective>
In `src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx`, replace ONLY the `<BoneyardSkeleton>` wrapper that guards the team card (card 03) with a `<motion.div>` fade-in. Leave the file's existing `useInViewOnce` + CSS `REVEAL / REVEAL_ON / REVEAL_OFF` reveal machinery untouched — it is stable on Safari per current behavior and in-scope migration risk is isolated to the BoneyardSkeleton wrapper only (PATTERNS.md §philosophy-clients-narrow-stack).

Purpose: Closes the narrow-stack boneyard dependency while keeping Wave 2 blast radius minimal. The file's parallel CSS-reveal system is an acceptable D-09-style "if it works, don't touch" scenario — migrating it would be scope creep per D-04.

Output: Surgical edit around the single BoneyardSkeleton usage, deletion of the associated `teamCardLoaded` state + handlers. Single atomic commit. Parallelizable with Plan 04-02 (different files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-safari-animations/04-CONTEXT.md
@.planning/phases/04-safari-animations/04-RESEARCH.md
@.planning/phases/04-safari-animations/04-PATTERNS.md
@src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx

<interfaces>
<!-- File-local state and components; API surface unchanged -->
The file currently imports `BoneyardSkeleton` from `@/shared/ui/boneyard-skeleton` and uses it once around card 03 (team card). The file also defines:
- `const [teamCardLoaded, setTeamCardLoaded] = useState(false);` (local state that gates the skeleton loading prop)
- `useInViewOnce` (its OWN reveal system — NOT touched in this plan)
- `REVEAL / REVEAL_ON / REVEAL_OFF` class constants (NOT touched)

After this plan the file still has `useInViewOnce` + REVEAL classes for non-team cards; card 03 gets an additional, independent `motion.div` reveal that plays at most once.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace BoneyardSkeleton around card 03 with motion.div fade</name>
  <files>src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx</files>
  <behavior>
    - `<BoneyardSkeleton name="..." loading={!teamCardLoaded}>...</BoneyardSkeleton>` wrapper is replaced by the underlying `<motion.div>` carrying the card 03 contents.
    - `motion.div` uses the D-05 canonical reveal props (`initial opacity 0`, `whileInView opacity 1`, `viewport once true amount 0.1`, `transition duration 0.22 easeOut`).
    - `teamCardLoaded` state and its `setTeamCardLoaded` setter are removed.
    - `onLoad` / `onError` handlers that called `setTeamCardLoaded` are removed from the `<Image>` inside card 03.
    - Card 03's existing inline styles (positioning, pointer-events, transform if any) are preserved as a `style` prop on `motion.div` (Pattern B applies if a `translate3d` transform is present from an ancestor scroll hook — animate ONLY opacity).
    - Import of `BoneyardSkeleton` is removed.
    - Import of `motion` from `motion/react` is added.
    - `useInViewOnce` hook call, `REVEAL / REVEAL_ON / REVEAL_OFF` classes, and all other cards (01, 02, ...) are UNCHANGED.
  </behavior>
  <action>
1. Open `src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx` and read it in full (the file is large; PATTERNS.md references lines 28–36, 210, 230–234, 286–325 as the points of interest).

2. Locate the single `<BoneyardSkeleton>` instance. Per PATTERNS.md it wraps card 03 (the "Команда" card) at approximately lines 286–325. The wrapper has the form:
   ```tsx
   <BoneyardSkeleton loading={!teamCardLoaded} name="philosophy-team-card-...">
     <div data-philosophy-card="..." className="..." style={{ ... }}>
       ...
       <Image onLoad={() => setTeamCardLoaded(true)} onError={() => setTeamCardLoaded(true)} ... />
       ...
     </div>
   </BoneyardSkeleton>
   ```

3. Transform to:
   ```tsx
   <motion.div
     data-philosophy-card="..."
     className="..."
     style={{ ... /* preserved exactly */ }}
     initial={{ opacity: 0 }}
     whileInView={{ opacity: 1 }}
     viewport={{ once: true, amount: 0.1 }}
     transition={{ duration: 0.22, ease: "easeOut" }}
   >
     ...
     <Image /* onLoad and onError removed */ ... />
     ...
   </motion.div>
   ```

   Pattern B (from PATTERNS.md): if the preserved `style` object contains a `transform: translate3d(...)` value computed from a scroll hook (e.g. `enterY(...)`), **keep it in `style`**. Do not move transform into `animate` — motion animates only `opacity`. This avoids the property conflict noted in RESEARCH.md §Example 2 note.

4. Remove `const [teamCardLoaded, setTeamCardLoaded] = useState(false);` (and its closing `;`). If `useState` is no longer used elsewhere in the file, also remove `useState` from the `react` import. If it is still used for other state (carousel indices, etc.), keep `useState` in the import.

5. Remove `import { BoneyardSkeleton } from "@/shared/ui/boneyard-skeleton";` from the imports block.

6. Add `import { motion } from "motion/react";` to the imports block (alphabetical by path: after `next/image`, before `@/shared/...` group).

7. Do NOT touch:
   - `useInViewOnce` imports or calls.
   - `REVEAL`, `REVEAL_ON`, `REVEAL_OFF` class constants.
   - Cards 01, 02 and any other non-team card JSX.
   - Hover classes, `transition-*` Tailwind classes (D-09).
   - Any scroll hook, any pointer-events logic, any data-* attributes on card 03 other than changing the tag.

8. Do NOT migrate the file's own CSS-class reveal system to motion — that is deferred per D-04 minimum-diff principle.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx</automated>
  </verify>
  <done>
- File no longer imports `BoneyardSkeleton`.
- File imports `motion` from `motion/react`.
- `teamCardLoaded` state and setter are gone.
- Card 03's `<Image>` has no `onLoad` / `onError` handlers.
- Card 03's root element is `<motion.div>` with the four D-05 motion props.
- `useInViewOnce` + REVEAL classes still present and functionally untouched.
- `pnpm typecheck` exits 0.
- `pnpm lint` exits 0 on the file.
- Commit message: `refactor(04): migrate philosophy-narrow-stack team card to motion.div fade`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing CSS reveal (useInViewOnce) ↔ new motion.div reveal | Two reveal systems coexist in the same component tree; risk of double fade or flicker if they interfere on the same card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering (visual regression) | Dual reveal coexistence | mitigate | The new `motion.div` reveal is applied ONLY to card 03 (previously boneyard-wrapped). The `useInViewOnce` CSS reveal applies to other cards and the surrounding stack — no overlap per PATTERNS.md. Manual verify in Safari UAT (Plan 04-05) D-11 checklist item #1. |
| T-04-07 | Denial-of-service | Viewport-observer cost | mitigate | `viewport.once: true` — observer disconnects after the single trigger. Single additional observer per page is negligible. |
</threat_model>

<verification>
1. `pnpm typecheck` — clean.
2. `pnpm lint` — clean on the modified file.
3. Manual dev smoke (recommended, not blocking):
   - `pnpm dev`, open a breakpoint where narrow-stack renders (360/480/768 range — `<NarrowStack>` is used on narrow layouts per the widget name).
   - Scroll down to the philosophy section, watch card 03 fade in smoothly.
   - Other cards still reveal via their existing CSS-class system; no regression.
   - Reload with `prefers-reduced-motion: reduce` enabled — card 03 should appear at full opacity immediately (MotionConfig from Plan 04-01 inherits).
</verification>

<success_criteria>
- [ ] Single `<BoneyardSkeleton>` in this file replaced with `<motion.div>`.
- [ ] `teamCardLoaded` state and its handlers removed.
- [ ] `useInViewOnce` + REVEAL/REVEAL_ON/REVEAL_OFF machinery untouched.
- [ ] `pnpm typecheck && pnpm lint` green.
- [ ] Single commit `refactor(04): migrate philosophy-narrow-stack team card to motion.div fade`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-safari-animations/04-03-SUMMARY.md` documenting which card was migrated (03 / team), which reveal system was intentionally left untouched (useInViewOnce + REVEAL classes), and the rationale (D-04 minimum-diff, D-09-style stability).
</output>
