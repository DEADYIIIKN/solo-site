---
phase: 04-safari-animations
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - src/widgets/cases/ui/cases-section-1024.tsx
  - src/widgets/cases/ui/cases-section-1440.tsx
  - src/widgets/philosophy-clients/ui/philosophy-clients-1024.tsx
  - src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx
autonomous: false
requirements:
  - SAFARI-01
  - SAFARI-02
  - ANI-01
  - ANI-02
must_haves:
  truths:
    - "Cases sections on 1024 and 1440 use motion.article reveal for VerticalCard + AdCard (Pattern A), while the pin-scroll logic (use-cases-pin-scroll-progress) stays untouched (Pattern C / D-06)"
    - "Philosophy-clients 1024 and 1440 team-card (card 03) uses motion.div for opacity reveal while style.transform driven by the scroll hook is preserved (Pattern B)"
    - "After migration, manual Safari latest UAT passes all four items of the D-11 checklist: reveal fades no flicker, ANI-01 pin holds position, hover no flash, scroll plays smoothly vs Chrome"
    - "If Safari UAT fails on ANI-01 pin desync, D-07 drop-in is applied and re-verified"
  artifacts:
    - path: "src/widgets/cases/ui/cases-section-1024.tsx"
      provides: "Cases section (1024px) with motion reveal; pin-scroll hook untouched"
      contains: "motion.article"
    - path: "src/widgets/cases/ui/cases-section-1440.tsx"
      provides: "Cases section (1440px) with motion reveal; pin-scroll hook untouched"
      contains: "motion.article"
    - path: "src/widgets/philosophy-clients/ui/philosophy-clients-1024.tsx"
      provides: "Philosophy-clients (1024px) team card with motion.div reveal + preserved style.transform"
      contains: "motion.div"
    - path: "src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx"
      provides: "Philosophy-clients (1440px) team card with motion.div reveal + preserved style.transform"
      contains: "motion.div"
    - path: ".planning/phases/04-safari-animations/04-05-UAT.md"
      provides: "Safari UAT checklist results (D-11)"
      contains: "SAFARI-01"
  key_links:
    - from: "src/widgets/cases/ui/cases-section-1440.tsx"
      to: "motion/react"
      via: "import { motion } from \"motion/react\""
      pattern: "from \"motion/react\""
    - from: "src/widgets/cases/ui/cases-section-1024.tsx"
      to: "@/widgets/cases/lib/use-cases-pin-scroll-progress"
      via: "existing useCasesPinScrollProgress hook — unchanged"
      pattern: "useCasesPinScrollProgress"
    - from: "src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx"
      to: "inline style.transform"
      via: "translate3d driven by scroll hook; motion animates only opacity"
      pattern: "translate3d"
---

<objective>
Migrate the four remaining BoneyardSkeleton consumers — the two wide cases sections (1024, 1440) and the two wide philosophy-clients sections (1024, 1440) — to motion reveal. These files are the most Safari-sensitive because (a) cases 1024/1440 participate in the ANI-01 pin-scroll system via `useCasesPinScrollProgress`, and (b) philosophy-clients 1024/1440 drive card 03's inline `style.transform: translate3d(...)` from a scroll hook while also previously gating reveal on image load.

After the four file migrations, run a manual Safari latest UAT against the D-11 checklist. This plan **contains a blocking checkpoint** — the phase cannot advance to Plan 04-06 (final teardown) until the UAT passes.

Purpose: Closes the remaining 4 BoneyardSkeleton usages (PATTERNS.md §Wave 3 scope). Applies Pattern B (preserve style.transform, animate opacity only) explicitly in the two philosophy files. Leaves Pattern C (`use-cases-pin-scroll-progress.ts`) untouched per D-06; D-07 drop-in is reserved for a contingency task gated by UAT results.

Output: Four files migrated (Tasks 1–4), one human UAT checkpoint (Task 5), and either zero follow-up tasks (happy path) or a D-07 drop-in contingency (Task 6, conditional).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-safari-animations/04-CONTEXT.md
@.planning/phases/04-safari-animations/04-RESEARCH.md
@.planning/phases/04-safari-animations/04-PATTERNS.md
@src/widgets/cases/ui/cases-section-1024.tsx
@src/widgets/cases/ui/cases-section-1440.tsx
@src/widgets/philosophy-clients/ui/philosophy-clients-1024.tsx
@src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx
@src/widgets/cases/lib/use-cases-pin-scroll-progress.ts

<interfaces>
<!-- Exports of all four files are unchanged after migration -->
`CasesSection1024`, `CasesSection1440`: accept `CasesSectionCardsProps` (as 360/480/768).
`PhilosophyClients1024`, `PhilosophyClients1440`: check current exports before editing (their prop shape is file-local and must be preserved).

The pin-scroll hook stays as-is:

```typescript
// src/widgets/cases/lib/use-cases-pin-scroll-progress.ts — UNCHANGED in this plan
export function useCasesPinScrollProgress(pinElement: HTMLElement | null): {
  collapseProgress: number;
  pinPhase: "before" | "active" | "after" | "static";
};
export function getCasesPinWeights(collapseProgress: number): CasesPinWeights;
// smootherstep01, CASES_PIN_SCROLL_VH, CASES_PIN_HOLD_END_FRAC — unchanged
```

Pattern B interface (Philosophy team card): `style.transform` is a computed string like `translate3d(0, ${enterY(2)}px, 0)` fed from the parent scroll hook. Motion animates ONLY `opacity` via `whileInView`. The motion runtime does not touch the transform channel when it is set via `style` and not via `animate` — the two live in different property layers per motion docs (RESEARCH.md §Example 2 note).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate cases-section-1024.tsx (VerticalCard + AdCard) — Pattern A only, DO NOT touch hook</name>
  <files>src/widgets/cases/ui/cases-section-1024.tsx</files>
  <behavior>
    - Same Pattern A as Plan 04-04 Tasks 1–3: both inner card components get `<motion.article>` roots with the canonical D-05 reveal (amount 0.15).
    - `useCasesPinScrollProgress` import and call stay identical. Any element that receives `weights.*` via inline style or className receives them through the same pathway (Pattern C).
    - Hover classes (grayscale, mix-blend, transition-[filter]) preserved (Pattern D).
    - `imageLoaded` state and onLoad/onError removed in the two inner cards.
    - Outer `CasesSection1024` component (carousel, arrows, modals, section eyebrow, pin refs, weights application) is NOT touched.
  </behavior>
  <action>
1. Read `cases-section-1024.tsx` fully. Identify the two `<BoneyardSkeleton>` wrappers (per PATTERNS.md the structure mirrors 360/480/768; class and dimension tokens differ).

2. Apply the Pattern A transformation in the exact same way as Plan 04-04 did for narrow variants:
   - `VerticalCard1024`: remove `imageLoaded` state, replace `<BoneyardSkeleton>` wrapper + `<article>` root with `<motion.article>` + D-05 props + `viewport.amount = 0.15`, remove `onLoad`/`onError` from `<Image>`.
   - `AdCard1024`: same transformation.

3. Update imports: remove `BoneyardSkeleton` import, add `import { motion } from "motion/react";`. Keep `useState` import (outer component uses it for modal state and pin refs likely include `useRef`).

4. **DO NOT TOUCH** any of the following:
   - `useCasesPinScrollProgress` hook import, invocation, or its returned `collapseProgress` / `pinPhase` consumers.
   - Inline styles or className expressions that read from `weights` (e.g., `getCasesPinWeights(collapseProgress)`).
   - `transition-[filter]`, `transition-opacity`, `will-change-*` classes anywhere.
   - Pin-related `ref={pinRef}`, `data-cases-pin` attributes, section wrapper heights.
   - Outer section structure, divider, ad-header, arrows, modals, eyebrow.

5. This task applies **Pattern A + Pattern D** only. **Pattern B is not needed** in cases files — no translate3d is being driven into any card root from the scroll hook; the weights drive `opacity` and `maxHeight` via inline style on different DOM nodes than the card roots.

6. **Pattern C (D-06):** the hook remains untouched. A contingency task (Task 6) is reserved if Safari UAT in Task 5 flags pin desync.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint src/widgets/cases/ui/cases-section-1024.tsx</automated>
  </verify>
  <done>
- `VerticalCard1024` and `AdCard1024` use `motion.article` with D-05 props.
- No `BoneyardSkeleton` / `imageLoaded` / `onLoad` / `onError` in either inner card.
- `useCasesPinScrollProgress` and weights logic completely unchanged.
- `pnpm typecheck` / `pnpm lint` green.
- Commit message: `refactor(04): migrate cases-section-1024 cards to motion.article fade`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate cases-section-1440.tsx (VerticalCard + AdCard) — identical to Task 1</name>
  <files>src/widgets/cases/ui/cases-section-1440.tsx</files>
  <behavior>
    - Same transformation as Task 1, targeting the 1440 variant.
    - Pin-scroll hook integration preserved verbatim (Pattern C / D-06).
    - Hover classes preserved (Pattern D).
  </behavior>
  <action>
Apply the Pattern A transformation to `cases-section-1440.tsx` exactly as Task 1 did for 1024. Internal components `VerticalCard1440` (PATTERNS.md anchors L158–224) and `AdCard1440` (L237–285) each get `<motion.article>` roots with D-05 reveal props.

Same constraints as Task 1:
- Remove `BoneyardSkeleton` import; add `motion` from `motion/react`.
- Remove `imageLoaded` state, `onLoad`, `onError` in both inner components.
- **Do not touch** `useCasesPinScrollProgress`, `weights`, any pin-related ref/attr, or the outer `CasesSection1440` structure.
- **Do not touch** mix-blend, grayscale, transition-[filter] classes (Pattern D).

The diff for this task should read almost identically to Task 1 — only tokens that vary are class sizes/dimensions.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint src/widgets/cases/ui/cases-section-1440.tsx</automated>
  </verify>
  <done>
- Two `<motion.article>` elements in this file.
- No `BoneyardSkeleton` / `imageLoaded` / `onLoad` / `onError`.
- Pin-scroll integration verbatim.
- `pnpm typecheck` / `pnpm lint` green.
- Commit message: `refactor(04): migrate cases-section-1440 cards to motion.article fade`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate philosophy-clients-1024.tsx team card — Pattern A + Pattern B (preserve style.transform)</name>
  <files>src/widgets/philosophy-clients/ui/philosophy-clients-1024.tsx</files>
  <behavior>
    - Single `<BoneyardSkeleton>` around card 03 (team card) is replaced with `<motion.div>`.
    - `style.transform` remains set via the `style` prop, driven by `enterY(2)` or equivalent scroll hook output (Pattern B — motion does NOT see transform in its animation channel).
    - `motion.div` animates ONLY `opacity` via `initial` + `whileInView` (D-05 reveal).
    - `teamCardLoaded` state and setter are removed; `onLoad`/`onError` on the team card `<Image>` are removed.
    - Other philosophy cards (01, 02, etc.), card layout constants, scroll hook (`usePhilosophyPinScrollProgress` or whatever the file uses), pointer-events gating logic (`isCardInteractive`) are NOT touched.
  </behavior>
  <action>
1. Read `philosophy-clients-1024.tsx` fully to confirm structure. Per PATTERNS.md, the single `<BoneyardSkeleton>` wraps card 03 at approximately lines 240–274, and the state `const [teamCardLoaded, setTeamCardLoaded] = useState(false);` is at approximately line 102.

2. **Before (approximate):**
   ```tsx
   <BoneyardSkeleton loading={!teamCardLoaded} name="philosophy-team-card-1024">
     <div
       data-philosophy-card="2"
       className={`${CARD} z-[3] bg-[#0d0300]`}
       style={{
         left: ..., top: ..., width: ..., height: ..., borderRadius: ...,
         transform: `translate3d(0, ${enterY(2)}px, 0)`,
         pointerEvents: isCardInteractive(2) ? "auto" : "none",
       }}
     >
       {/* <Image onLoad={() => setTeamCardLoaded(true)} onError={() => setTeamCardLoaded(true)} ... /> */}
       {/* ... rest of card content */}
     </div>
   </BoneyardSkeleton>
   ```

3. **After (Pattern A + Pattern B):**
   ```tsx
   <motion.div
     data-philosophy-card="2"
     className={`${CARD} z-[3] bg-[#0d0300]`}
     style={{
       left: ..., top: ..., width: ..., height: ..., borderRadius: ...,
       transform: `translate3d(0, ${enterY(2)}px, 0)`,
       pointerEvents: isCardInteractive(2) ? "auto" : "none",
     }}
     initial={{ opacity: 0 }}
     whileInView={{ opacity: 1 }}
     viewport={{ once: true, amount: 0.1 }}
     transition={{ duration: 0.22, ease: "easeOut" }}
   >
     {/* Image without onLoad/onError */}
     {/* rest unchanged */}
   </motion.div>
   ```

4. Critical Pattern B constraint per RESEARCH.md §Example 2 note: `style.transform` sits in the inline style layer; motion's `animate` property layer only touches `opacity`. The two DO NOT conflict as long as `transform` is not also in `animate={{ ... }}` / `whileInView={{ ... }}` / `initial={{ ... }}`. We confirm this by setting only `opacity` in those motion props.

5. Remove `const [teamCardLoaded, setTeamCardLoaded] = useState(false);`. If `useState` is still used for anything else in this file (scroll hook typically uses `useState`), keep the import; otherwise remove it.

6. Remove `import { BoneyardSkeleton } from "@/shared/ui/boneyard-skeleton";`. Add `import { motion } from "motion/react";`.

7. **Do not touch:** other cards, `CARD` class constant, `enterY` helper, `isCardInteractive`, the scroll hook, pin refs, any other state.

8. Spot-check: after the edit, the file should have exactly one `<motion.` occurrence (the team card root).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint src/widgets/philosophy-clients/ui/philosophy-clients-1024.tsx</automated>
  </verify>
  <done>
- Team card 03 root is `<motion.div>` with `initial / whileInView / viewport / transition` props.
- `style` prop still includes `transform: translate3d(...)` sourced from the scroll hook.
- No `teamCardLoaded` / `setTeamCardLoaded` / `onLoad` / `onError` in the file.
- Other cards untouched.
- `pnpm typecheck` / `pnpm lint` green.
- Commit message: `refactor(04): migrate philosophy-clients-1024 team card to motion.div (Pattern B)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Migrate philosophy-clients-1440.tsx team card — identical to Task 3</name>
  <files>src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx</files>
  <behavior>
    - Same Pattern A + Pattern B treatment as Task 3, applied to the 1440 variant.
    - Per PATTERNS.md the BoneyardSkeleton around card 03 is at approximately lines 226–260; `teamCardLoaded` state at approximately line 86.
  </behavior>
  <action>
Apply the same transformation as Task 3 to `philosophy-clients-1440.tsx`. The 1440 version uses different coordinates (`left: 140, top: 252, width: 640, height: 340, borderRadius: 20`) but the structural transformation is identical:

1. `<BoneyardSkeleton loading={!teamCardLoaded} ...> <div> ... </div> </BoneyardSkeleton>` → `<motion.div>` with the same `style` object and the D-05 reveal props.
2. Remove `teamCardLoaded` state and `onLoad`/`onError`.
3. Swap imports.
4. Preserve all other cards, constants, hooks, and pointer-events logic.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint src/widgets/philosophy-clients/ui/philosophy-clients-1440.tsx && pnpm build</automated>
  </verify>
  <done>
- Team card 03 root is `<motion.div>`.
- `style.transform` preserved (Pattern B).
- No `teamCardLoaded` / `onLoad` / `onError` references in this file.
- `pnpm typecheck && pnpm lint && pnpm build` all green (full build sanity after all four Wave 4 code edits).
- Commit message: `refactor(04): migrate philosophy-clients-1440 team card to motion.div (Pattern B)`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Safari latest UAT against D-11 checklist</name>
  <what-built>
All BoneyardSkeleton consumers except `boneyard-skeleton.tsx` itself are migrated to motion. MotionConfig is wired globally. The cases pin-scroll hook is unchanged; philosophy team card uses Pattern B (style.transform preserved, motion animates only opacity).

Before approving, the user manually validates the four D-11 checklist items in **Safari latest on macOS** (per D-10) and compares against Chrome.
  </what-built>
  <how-to-verify>
Prerequisites:
- `pnpm dev` is running on port 3000 (or `pnpm build && pnpm start` for production-parity smoke — recommended because A4 in RESEARCH.md flags hydration-flash risk that only appears in production builds).
- macOS with Safari latest available.

Procedure — complete ALL four items and record pass/fail in a new file `.planning/phases/04-safari-animations/04-05-UAT.md`:

1. **Reveal fades have no flicker in Safari (SAFARI-01, ANI-02 → D-11 item 1)**
   - Open http://localhost:3000 in Safari at desktop viewport (≥1440).
   - Hard-reload (Cmd+Option+R). Scroll from top to bottom slowly.
   - Watch each migrated section fade in as it enters viewport: team photo (team section), philosophy team card 03, cases section vertical cards, cases section ad cards.
   - PASS if: each fade is a smooth 220ms opacity ramp with no mid-animation flash, no opacity jump at the end, no flicker of the underlying black `bg-[#0d0300]` after the image loads.
   - FAIL if: any of the above flickers, jumps, or replays on subsequent scroll.
   - Repeat at 1024, 768, 480, 360 viewport widths (resize Safari or use Responsive Design Mode).

2. **ANI-01 pin holds position (SAFARI-02, ANI-01 → D-11 item 2)**
   - At ≥1024 viewport in Safari, scroll into the cases section.
   - The vertical slider, divider, and "Рекламные кейсы" header should pin and stay in place while the collapse animation progresses; then release and continue with the ad strip expanded.
   - PASS if: the pinned stack stays at the top of the viewport throughout the pin zone, no jumps upward or "unpin" glitches, weights transition smoothly as scroll progresses, scroll input maps 1:1 to progress.
   - FAIL if: the pinned stack jumps up or down at pin entry/exit, content "falls off", progress stutters or overshoots, scroll feels unresponsive inside the pin zone.

3. **Hover without flash (ANI-02 → D-11 item 3)**
   - Hover over cases cards (grayscale → color) and any other hover-animated element.
   - PASS if: grayscale-to-color transition is smooth, `mix-blend-mode` overlay opacity changes without flickering, no "pop" frame at the start of the hover.
   - FAIL if: a color or saturation flash precedes the transition, or the transition stutters.

4. **Scroll playback parity vs Chrome (SAFARI-01/02 → D-11 item 4)**
   - Open the same page in Chrome side by side.
   - Scroll at similar speed in each; watch for obvious behavioral differences in cases pin, reveal fades, and hover.
   - PASS if: Safari behavior is indistinguishable from Chrome to a careful human observer — no visual glitches that only Safari exhibits.
   - FAIL if: Safari clearly diverges in a way that a client would notice.

**Document results in `.planning/phases/04-safari-animations/04-05-UAT.md`** with the template:

```markdown
# Phase 04 Safari UAT Results

**Tester:** <user>
**Date:** <YYYY-MM-DD>
**Safari version:** <version>
**macOS version:** <version>

## D-11 Checklist

| # | Item | Result | Notes |
|---|------|--------|-------|
| 1 | Reveal fades without flicker (per breakpoint: 1440/1024/768/480/360) | PASS / FAIL | ... |
| 2 | ANI-01 pin holds position (≥1024) | PASS / FAIL | ... |
| 3 | Hover transitions without flash | PASS / FAIL | ... |
| 4 | Scroll playback parity vs Chrome | PASS / FAIL | ... |

## Overall: PASS / FAIL

If any FAIL → describe observed defect, affected file, and whether D-07 drop-in or another follow-up is required.
```
  </how-to-verify>
  <resume-signal>
Type **"UAT PASS"** if all four items pass → plan proceeds to teardown (Plan 04-06); Task 6 is skipped.
Type **"UAT FAIL: <which items>"** if any items fail → executor proceeds to Task 6 (D-07 drop-in) or creates a gap plan depending on which item failed. If pin failed specifically (item 2), Task 6 applies. For item 1/3/4 failures unrelated to pin, flag to user for scoping a follow-up plan.
  </resume-signal>
</task>

<task type="auto" tdd="false" gate="conditional:pin-uat-fail">
  <name>Task 6 (conditional): D-07 drop-in — replace scroll progress computation in useCasesPinScrollProgress with useScroll</name>
  <files>src/widgets/cases/lib/use-cases-pin-scroll-progress.ts</files>
  <behavior>
    - ONLY executed if Task 5 reports UAT FAIL on item 2 (ANI-01 pin desync in Safari).
    - Replace the internal scroll-position math (`scrollY` + `rangeStart/rangeEnd` + `(scrollY - start) / span`) with `useScroll({ target, offset })` from `motion/react`.
    - All exported types, constants, and function signatures of `use-cases-pin-scroll-progress.ts` stay identical so cases-section-1024/1440 need no changes.
    - `smootherstep01`, `CASES_PIN_SCROLL_VH`, `CASES_PIN_HOLD_END_FRAC`, `getCasesPinWeights`, `computeCasesPinCollapseProgress`, `casesPinViewportAllowsPin`, the `prefers-reduced-motion` check — all preserved verbatim (D-07).
    - After the drop-in, re-run Task 5 UAT item 2 in Safari to confirm pin now holds position.
  </behavior>
  <action>
Executor: **skip this task entirely if Task 5 resumed with "UAT PASS"**. Only proceed if pin-specific UAT failure was reported.

1. Open `src/widgets/cases/lib/use-cases-pin-scroll-progress.ts` (full current file is in context).

2. The target is the `useCasesPinScrollProgress` hook (lines 105–238). Specifically, replace the `tick` implementation's scroll-math chunk (reading `window.scrollY`, computing `t = (scrollY - start) / span`) with motion's `useScroll`. The rest of the hook (phase state machine, reduced-motion check, viewport gate, ResizeObserver for range recalculation) stays.

3. Refactor shape (per RESEARCH.md §Example 3):

   - Change the signature so the hook accepts `RefObject<HTMLElement | null>` (the existing signature accepts `HTMLElement | null` — if the consumer passes `ref.current`, you can preserve the current shape and adapt internally; prefer to minimize consumer-side diff, so keep the current `HTMLElement | null` signature and internally wrap into a ref for `useScroll({ target })`).

   - Inside the hook:
     ```tsx
     import { useScroll, useMotionValueEvent } from "motion/react";

     // Convert pinElement to a RefObject for useScroll:
     const pinRef = useRef<HTMLElement | null>(pinElement);
     pinRef.current = pinElement;

     const { scrollYProgress } = useScroll({
       target: pinRef,
       offset: ["start start", "end start"],
     });

     useMotionValueEvent(scrollYProgress, "change", (t) => {
       if (window.matchMedia("(prefers-reduced-motion: reduce)").matches || !casesPinViewportAllowsPin()) {
         setPhase("static");
         applySnap(1);
         return;
       }
       if (t <= 0) { setPhase("before"); applySnap(0); return; }
       if (t >= 1) { setPhase("after"); applySnap(1); return; }
       setPhase("active");
       applyProgress(computeCasesPinCollapseProgress(t));
     });
     ```

   - Remove the `scrollY` listener, `resize` listener, `visualViewport` listener, and `recalculateRange` — `useScroll` handles these internally via IntersectionObserver + scroll proxy.

   - Keep the `ResizeObserver` on `pinElement` ONLY if it is needed to trigger a motion value recompute; per motion docs `useScroll` already handles target size changes. Remove the RO for simplicity.

   - Preserve `CasesPinPhase`, `CasesPinCollapseProgress`, `CasesPinWeights`, `getCasesPinScrollRange`, `casesPinViewportAllowsPin`, `smootherstep01`, `CASES_PIN_SCROLL_VH`, `CASES_PIN_HOLD_END_FRAC`, `computeCasesPinCollapseProgress` — all exported identically.

   - The return shape `{ collapseProgress, pinPhase }` is unchanged.

4. Run `pnpm typecheck && pnpm lint` and then re-run Task 5 item 2 in Safari.

5. If the drop-in does NOT fix pin desync, STOP and escalate to user — do not attempt further hook rewrites in this phase; flag for a separate follow-up plan.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint src/widgets/cases/lib/use-cases-pin-scroll-progress.ts && pnpm build</automated>
  </verify>
  <done>
- `use-cases-pin-scroll-progress.ts` uses `useScroll` + `useMotionValueEvent` from `motion/react` for scroll progress.
- All exported members (types, constants, helper functions) have identical public signatures.
- `pnpm typecheck && pnpm lint && pnpm build` green.
- Task 5 item 2 re-run in Safari: PASS.
- Commit message: `fix(04): apply D-07 useScroll drop-in for ANI-01 pin Safari parity`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scroll hook ↔ motion opacity reveal | Two independent animations on card 03 (hook-driven transform, motion-driven opacity). Risk of one layer stomping the other. |
| Safari render engine ↔ sticky + transform ancestor | Classic Safari bug (RESEARCH.md Pitfall 1). |
| mix-blend-mode + opacity transition in Safari | Known Safari flicker risk (RESEARCH.md Pitfall 2). |
| grayscale filter transition in Safari | Known Safari jank risk (RESEARCH.md Pitfall 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering (visual regression) | Pattern B property-layer conflict (transform in style + opacity in animate) | mitigate | RESEARCH.md §Example 2 note explicitly verified: `style.transform` and `animate` on different properties do NOT collide. UAT Task 5 item 1 validates no visible regression. |
| T-04-12 | Tampering (Safari sticky bug) | `useCasesPinScrollProgress` pin zone in Safari | mitigate | Existing hook already uses JS-driven progress without CSS scroll-timeline — architecturally Safari-safe per RESEARCH.md §Pitfall 1. UAT Task 5 item 2 validates. If fail, D-07 drop-in (Task 6) swaps to `useScroll` which uses motion's battle-tested scroll-progress engine. |
| T-04-13 | Tampering (hover flicker Safari) | `mix-blend-color` + `transition-opacity` on cases cards | mitigate | Pattern D leaves CSS hover classes intact — they work in Chrome today. UAT Task 5 item 3 validates Safari. If fail, follow-up plan adds `will-change-[opacity]` or isolates the overlay (RESEARCH.md Pitfall 2 mitigations). |
| T-04-14 | Tampering (grayscale jank Safari) | `transition-[filter]` on cases card images | mitigate | `will-change-[filter]` already present. UAT Task 5 item 3 validates. If fail, reduce duration 300→200ms or swap to `backdrop-filter` per RESEARCH.md Pitfall 3. |
| T-04-15 | Denial-of-service | Extra motion observers on wide layouts | mitigate | `viewport.once: true` — observers disconnect after first trigger. D-07 drop-in replaces one JS scroll listener with motion's internal scroll-progress primitive — net negative listener count. |
| T-04-16 | Information disclosure | UAT document contents | accept | `04-05-UAT.md` contains only pass/fail notes about animation behavior; no PII or secrets. |
</threat_model>

<verification>
Per-task verification is defined inside each `<verify>` block. Plan-level verification:

- After Task 4: `pnpm typecheck && pnpm lint && pnpm build` — all green.
- Task 5: manual Safari UAT produces `.planning/phases/04-safari-animations/04-05-UAT.md` with explicit PASS on all four items (possibly after Task 6).
- Zero changes to `use-cases-pin-scroll-progress.ts` in the happy path (Task 6 not executed).

**Explicit Safari UAT checklist (D-11, copied here for atomic reference in the plan):**

| # | Item | Req | Method |
|---|------|-----|--------|
| 1 | Reveal fades have no flicker at 1440, 1024, 768, 480, 360 in Safari latest | SAFARI-01, ANI-02 | Visual, per-breakpoint scroll |
| 2 | ANI-01 pin zone (cases section ≥1024) holds position without jumps in Safari | SAFARI-02, ANI-01 | Visual, slow scroll through pin |
| 3 | Hover transitions (grayscale, mix-blend) have no flash in Safari | ANI-02 | Hover over cases cards |
| 4 | Scroll playback is indistinguishable between Safari and Chrome to a careful observer | SAFARI-01, SAFARI-02 | Side-by-side comparison |
</verification>

<success_criteria>
- [ ] `cases-section-1024.tsx` and `cases-section-1440.tsx` use `<motion.article>` reveal on both inner card components (Pattern A + D); pin-scroll integration untouched (Pattern C / D-06).
- [ ] `philosophy-clients-1024.tsx` and `philosophy-clients-1440.tsx` use `<motion.div>` reveal on card 03 with preserved `style.transform` (Pattern B).
- [ ] No `BoneyardSkeleton` / `imageLoaded` / `teamCardLoaded` / `onLoad` / `onError` references in any of the four files.
- [ ] `pnpm typecheck && pnpm lint && pnpm build` all green.
- [ ] `.planning/phases/04-safari-animations/04-05-UAT.md` exists with the D-11 checklist filled in and overall PASS.
- [ ] Either Task 6 executed (with its own commit) OR Task 6 skipped because UAT passed without the drop-in.
- [ ] Up to 4 atomic commits (one per file, Tasks 1–4) + optional 5th commit if Task 6 runs.
</success_criteria>

<output>
After Task 5 passes (with or without Task 6), create `.planning/phases/04-safari-animations/04-05-SUMMARY.md` covering: files migrated, Pattern A/B/C/D application matrix, UAT outcome with link to `04-05-UAT.md`, and whether Task 6 (D-07 drop-in) was executed. Note explicitly that Plan 04-06 (teardown) is now unblocked.
</output>
