---
phase: 07-modal-unification
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx
  - .planning/phases/07-modal-unification/07-SVERKA.md
autonomous: false
requirements:
  - REFAC-01
  - REFAC-02
  - REFAC-03
must_haves:
  truths:
    - "Удалены backwards-compat re-exports types из modal-1440 (если их использование подтверждено как нулевое)"
    - "Никакой импорт типов в src/ больше не идёт из first-screen-consultation-modal-1440 — все идут из first-screen-consultation-form-state"
    - "Полный test suite (tsc + eslint + vitest + playwright) проходит на main config"
    - "Manual Figma sverka на 1 breakpoint выполнена и зафиксирована в 07-SVERKA.md (pixel diff ≤ 1px)"
    - "ROADMAP.md и REQUIREMENTS.md обновлены: REFAC-01/02/03 marked complete"
  artifacts:
    - path: ".planning/phases/07-modal-unification/07-SVERKA.md"
      provides: "Manual Figma diff report для выбранного breakpoint"
      contains: "pixel diff"
    - path: "src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx"
      provides: "Финальный thin wrapper без re-export tail (≤20 строк)"
      contains: "ConsultationModal variant=\"1440\""
  key_links:
    - from: ".planning/phases/07-modal-unification/07-SVERKA.md"
      to: "src/widgets/first-screen/ui/consultation-modal.tsx"
      via: "verification artifact ↔ unified component"
      pattern: "consultation-modal\\.tsx"
---

<objective>
Cleanup-фаза unification:
1. Удалить backwards-compat re-exports types из `first-screen-consultation-modal-1440.tsx` (после подтверждения, что внешние импортёры переведены).
2. Прогнать **полный** test suite (E2E + cross-browser + unit + tsc + eslint).
3. **Обязательная manual Figma sverka на 1 breakpoint** — это единственный гард pixel-perfect parity (E2E ловит behavior, не pixel positions). Per CONTEXT.md D5.
4. Обновить REQUIREMENTS.md / ROADMAP.md: REFAC-01/02/03 marked complete.

**Соответствие locked decisions:** D5 (visual parity guard via Figma sverka на 1 breakpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-modal-unification/07-CONTEXT.md
@.planning/phases/07-modal-unification/07-01-SUMMARY.md
@.planning/phases/07-modal-unification/07-02-SUMMARY.md
@.planning/phases/07-modal-unification/07-03-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx
@src/widgets/first-screen/ui/consultation-modal.tsx
@src/widgets/first-screen/ui/consultation-modal-variants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить backwards-compat re-exports + финальный test suite</name>
  <files>src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx</files>
  <action>
**Шаг 1 — Подтвердить что re-exports не используются:**
```bash
# Никто не должен импортировать types/const FROM modal-1440 файла
grep -rn "from.*first-screen-consultation-modal-1440" src/ \
  | grep -E "(FirstScreenConsultationContactMethod|FirstScreenConsultationFormState|FirstScreenConsultationModalTitleVariant|defaultFirstScreenConsultationFormState)"
```
Ожидание: **пусто**. Если что-то нашлось — это потребитель, которого Plan 07-01 пропустил. Перевести его на импорт из `first-screen-consultation-form-state` ДО удаления re-exports.

**Шаг 2 — Удалить re-export tail из modal-1440:**
Финальный файл должен выглядеть так (≤20 строк, БЕЗ re-exports):

```tsx
"use client";

import type { Dispatch, SetStateAction } from "react";

import type {
  FirstScreenConsultationFormState,
  FirstScreenConsultationModalTitleVariant,
} from "@/widgets/first-screen/model/first-screen-consultation-form-state";
import { ConsultationModal } from "@/widgets/first-screen/ui/consultation-modal";

type FirstScreenConsultationModal1440Props = {
  open: boolean;
  onClose: () => void;
  formState: FirstScreenConsultationFormState;
  setFormState: Dispatch<SetStateAction<FirstScreenConsultationFormState>>;
  titleVariant?: FirstScreenConsultationModalTitleVariant;
};

export function FirstScreenConsultationModal1440(
  props: FirstScreenConsultationModal1440Props,
) {
  return <ConsultationModal variant="1440" {...props} />;
}
```

**Шаг 3 — Полный test suite:**
```bash
pnpm exec tsc --noEmit
pnpm exec eslint src/
pnpm run test:unit          # vitest
pnpm run test:e2e           # все Playwright specs (chromium + webkit + mobile-safari)
```

Все 4 шага должны пройти clean. Если что-то падает — не коммитить, diagnose, fix, retry.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm run test:unit &amp;&amp; pnpm run test:e2e</automated>
  </verify>
  <done>
- modal-1440 ≤20 строк, без re-exports.
- grep на старые re-export пути возвращает пусто.
- Полный test suite проходит на main config.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Figma sverka на 1 breakpoint (D5 visual parity guard)</name>
  <what-built>
Unified ConsultationModal component, заменяющий 5 идентичных модалок без потери visual parity (per CONTEXT.md D5).
  </what-built>
  <how-to-verify>
**Cel:** Подтвердить pixel-perfect соответствие Figma макету на **1 breakpoint** (любом — выберите тот, что максимально нагружен по различиям, например 1440 или 360).

**Шаги (для пользователя):**
1. Запустить dev: `pnpm run dev`
2. Открыть выбранный viewport (DevTools → device emulation, либо resize окна).
3. Открыть first-screen, кликнуть CTA консультации. Модалка открывается.
4. Сравнить с Figma макетом:
   - **1440:** Figma 783:9898 (consultation) и 783:9917 (task)
   - **360:** Figma 783:9819
   - **480:** Figma TBD (см. варианты в файле)
   - **768:** Figma TBD
   - **1024:** Figma TBD
5. Проверить (allowed diff ≤ 1px):
   - max-width контента
   - padding form card
   - font-size заголовка / inputs / submit button
   - размер крестика
   - расстояние крестик ↔ карточка (column gap)
   - layout contact-method (radio vs pill — должен совпасть с Figma вариантом для этого breakpoint)
   - submit-button height + label
6. Открыть Figma MCP (если доступен) для попиксельного сравнения.
7. Заполнить форму, нажать submit:
   - На 1440/1024/768: должна быть плавная анимация form → leave → success.
   - На 480/360: **поведенческое отличие от старого кода** — теперь тоже плавная анимация (раньше был instant switch). Это intended (per Plan 07-02 решение использовать полный successStep flow). Если выглядит «слишком долго» — обсудить здесь.

**Чекпоинт зафиксировать в `.planning/phases/07-modal-unification/07-SVERKA.md`** (создать файл с фиксацией):
- Какой breakpoint проверен
- Скриншот / описание diff'ов (если ≤1px — OK, если больше — список с причинами)
- Решение по поведенческому отличию success-step animation на 360/480
- Resume signal: `approved` либо список issue'ов для revision
  </how-to-verify>
  <resume-signal>Type "approved" + создать 07-SVERKA.md с диффом, либо описать issues для revision (тогда возврат в Plan 07-02 для правок variant table).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Обновить REQUIREMENTS.md / ROADMAP.md / создать SUMMARY</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/phases/07-modal-unification/07-SUMMARY.md
  </files>
  <action>
**1. Обновить `.planning/REQUIREMENTS.md`:**
- В секции «Modal Refactor (REFAC)»: пометить REFAC-01, REFAC-02, REFAC-03 как `[x]`.
- В таблице Traceability: статус `Pending` → `Done` для трёх REFAC-XX, поле Phase → `7`.

**2. Обновить `.planning/ROADMAP.md`:**
- Phase 7 чекбокс `[ ]` → `[x]`.
- В таблице Progress: «Phase 7 → 4/4 plans complete, Status: Shipped, Completed: 2026-04-XX».

**3. Создать `.planning/phases/07-modal-unification/07-SUMMARY.md`** (overall phase summary):
- Цель и результат фазы (2433 → ~120 LOC; X plans / Y commits).
- Список созданных артефактов:
  - `src/widgets/first-screen/model/first-screen-consultation-form-state.ts` (Plan 07-01)
  - `src/widgets/first-screen/ui/consultation-modal-variants.ts` (Plan 07-02)
  - `src/widgets/first-screen/ui/consultation-modal.tsx` (Plan 07-02)
  - 5 thin wrappers `first-screen-consultation-modal-XXX.tsx` (Plan 07-03)
  - `07-SVERKA.md` (Plan 07-04)
- Key decisions honored: D1 (variant prop), D2 (co-location), D3 (big-bang), D4 (submit shape unchanged), D5 (sverka выполнена).
- Behavioral change disclosed: success-step animation теперь играет на 360/480 (раньше был instant). Approved через checkpoint в Plan 07-04.
- Готовность к Phase 8: один submit handler в одном месте (consultation-modal.tsx) → FUNC-01..04 будут централизованы тривиально.
  </action>
  <verify>
    <automated>grep -E "^\\- \\[x\\] \\*\\*REFAC-0[123]\\*\\*" .planning/REQUIREMENTS.md | wc -l | grep -q "^3$" &amp;&amp; test -f .planning/phases/07-modal-unification/07-SUMMARY.md &amp;&amp; test -f .planning/phases/07-modal-unification/07-SVERKA.md</automated>
  </verify>
  <done>
- REQUIREMENTS.md: REFAC-01/02/03 checked, traceability обновлена.
- ROADMAP.md: Phase 7 marked complete.
- 07-SUMMARY.md описывает фазу целиком.
- 07-SVERKA.md существует (создан в Task 2).
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit && pnpm run test:unit && pnpm run test:e2e` — all clean.
2. `grep -rn "first-screen-consultation-modal-1440\".*\\(FirstScreenConsultationContactMethod\\|FormState\\|TitleVariant\\|defaultFirstScreen\\)" src/` — пусто.
3. `wc -l src/widgets/first-screen/ui/first-screen-consultation-modal-*.tsx` — каждый ≤30 строк.
4. `wc -l src/widgets/first-screen/ui/consultation-modal*.{ts,tsx}` — sanity (combined ~700 строк).
5. `.planning/phases/07-modal-unification/07-SVERKA.md` существует, содержит pixel diff отчёт.
6. REQUIREMENTS.md / ROADMAP.md отражают завершение REFAC-01/02/03 и Phase 7.
</verification>

<success_criteria>
- LOC reduction подтверждён: 2433 → ~120 (95% reduction).
- 13 callsite импортов работают без изменений.
- Full test suite passing (chromium + webkit + mobile-safari + vitest + tsc + eslint).
- Manual Figma sverka на 1 breakpoint: diff ≤ 1px (либо документированные accepted differences).
- REFAC-01, REFAC-02, REFAC-03 closed.
- Phase 7 готова к мерджу; Phase 8 разблокирован.
</success_criteria>

<output>
После Task 3 — все 3 артефакта (07-SUMMARY.md, 07-SVERKA.md, обновлённые REQUIREMENTS / ROADMAP) commits в один атомарный commit. Phase 7 закрыт.
</output>
