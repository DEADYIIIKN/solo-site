---
phase: 07-modal-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/first-screen/model/first-screen-consultation-form-state.ts
  - src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx
  - src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx
  - src/widgets/first-screen/ui/first-screen.tsx
  - src/widgets/lead-form/ui/lead-form-fields.tsx
autonomous: true
requirements:
  - REFAC-01
must_haves:
  truths:
    - "Все типы (FirstScreenConsultationContactMethod, FirstScreenConsultationFormState, FirstScreenConsultationModalTitleVariant) и default state импортируются из одного нового model-файла, а не из modal-1440"
    - "13 callsite-импортов FirstScreenConsultationModalXXX продолжают работать без изменений (все 5 модалок остаются именованными экспортами)"
    - "pnpm exec tsc --noEmit проходит без ошибок"
    - "pnpm exec playwright test tests/e2e/consultation-modal.spec.ts проходит без правок (visual / behavior parity)"
  artifacts:
    - path: "src/widgets/first-screen/model/first-screen-consultation-form-state.ts"
      provides: "Shared types + defaultFirstScreenConsultationFormState"
      contains: "export type FirstScreenConsultationFormState"
    - path: "src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx"
      provides: "1440 modal — больше НЕ источник типов, импортирует их"
      contains: "import.*first-screen-consultation-form-state"
  key_links:
    - from: "src/widgets/first-screen/ui/first-screen.tsx"
      to: "src/widgets/first-screen/model/first-screen-consultation-form-state.ts"
      via: "default state + title variant type import"
      pattern: "from \"@/widgets/first-screen/model/first-screen-consultation-form-state\""
    - from: "src/widgets/lead-form/ui/lead-form-fields.tsx"
      to: "src/widgets/first-screen/model/first-screen-consultation-form-state.ts"
      via: "form state types import"
      pattern: "from \"@/widgets/first-screen/model/first-screen-consultation-form-state\""
---

<objective>
Подготовительный шаг рефакторинга: вынести shared form-state types и default value из `first-screen-consultation-modal-1440.tsx` в отдельный model-файл. Никаких поведенческих и визуальных изменений — только перенос types/constants и обновление импортов в потребителях.

**Purpose:** Текущая модалка 1440 фактически выступает «host'ом» для типов формы (см. CONTEXT.md D2 / scout-таблицу). Перед введением base-компонента и variants table (Plan 07-02) типы должны жить отдельно, чтобы и base, и thin-wrappers могли их импортировать без циклических зависимостей.

**Output:**
- Новый файл `src/widgets/first-screen/model/first-screen-consultation-form-state.ts` с экспортами: `FirstScreenConsultationContactMethod`, `FirstScreenConsultationFormState`, `FirstScreenConsultationModalTitleVariant`, `defaultFirstScreenConsultationFormState`.
- Обновлённые импорты во всех 5 модалках, `first-screen.tsx` и `lead-form-fields.tsx`.

**Соответствие locked decisions:** D2 (co-location в `src/widgets/first-screen/`), D3 (big-bang в одной фазе — это первый атомарный коммит), D4 (submit shape не трогаем).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-modal-unification/07-CONTEXT.md
@src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx
@src/widgets/first-screen/ui/first-screen.tsx
@src/widgets/lead-form/ui/lead-form-fields.tsx

<interfaces>
<!-- Текущее место экспортов (modal-1440.tsx, строки 15-35) -->

```typescript
// src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx (СЕЙЧАС)
export type FirstScreenConsultationModalTitleVariant = "task" | "consultation";
export type FirstScreenConsultationContactMethod = "call" | "telegram" | "whatsapp";
export type FirstScreenConsultationFormState = {
  name: string;
  phone: string;
  message: string;
  contactMethod: FirstScreenConsultationContactMethod;
  consent: boolean;
};
export const defaultFirstScreenConsultationFormState: FirstScreenConsultationFormState = {
  name: "",
  phone: "",
  message: "",
  contactMethod: "call",
  consent: false,
};
```

После Plan 07-01 эти 4 экспорта живут только в `first-screen-consultation-form-state.ts`. Modal-1440 импортирует их как все остальные модалки.

<!-- Текущие импорты у потребителей (которые надо переадресовать) -->

`src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx:14-18`:
```typescript
import type {
  FirstScreenConsultationContactMethod,
  FirstScreenConsultationFormState,
  FirstScreenConsultationModalTitleVariant,
} from "@/widgets/first-screen/ui/first-screen-consultation-modal-1440";
```
(аналогично в 480/768/1024 модалках)

`src/widgets/first-screen/ui/first-screen.tsx:7-10`:
```typescript
import {
  defaultFirstScreenConsultationFormState
} from "@/widgets/first-screen/ui/first-screen-consultation-modal-1440";
import type { FirstScreenConsultationModalTitleVariant } from "@/widgets/first-screen/ui/first-screen-consultation-modal-1440";
```

`src/widgets/lead-form/ui/lead-form-fields.tsx:12-16`:
```typescript
import {
  type FirstScreenConsultationContactMethod,
  type FirstScreenConsultationFormState,
  defaultFirstScreenConsultationFormState,
} from "@/widgets/first-screen/ui/first-screen-consultation-modal-1440";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать model-файл с shared form-state types</name>
  <files>src/widgets/first-screen/model/first-screen-consultation-form-state.ts</files>
  <action>
Создать новый файл `src/widgets/first-screen/model/first-screen-consultation-form-state.ts` (директория `model/` уже существует — проверить и использовать её).

Содержимое файла — точный перенос из `first-screen-consultation-modal-1440.tsx` строки 15-35 (см. interfaces выше) **без изменений**:
- `export type FirstScreenConsultationModalTitleVariant`
- `export type FirstScreenConsultationContactMethod`
- `export type FirstScreenConsultationFormState`
- `export const defaultFirstScreenConsultationFormState`

JSDoc-комментарий к `FirstScreenConsultationModalTitleVariant` сохранить (`/** «связаться» в шапке — задача; круглый CTA — бесплатная консультация */`).

Никакой логики, никаких хелперов, никакого `"use client"` (это pure types/const).
  </action>
  <verify>
    <automated>test -f src/widgets/first-screen/model/first-screen-consultation-form-state.ts &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Файл существует, TypeScript компилируется. Старые импорты из modal-1440 ещё работают (modal-1440 пока re-exports не трогали).</done>
</task>

<task type="auto">
  <name>Task 2: Переключить modal-1440 на импорт из model-файла + обновить все потребители</name>
  <files>
    src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx,
    src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx,
    src/widgets/first-screen/ui/first-screen.tsx,
    src/widgets/lead-form/ui/lead-form-fields.tsx
  </files>
  <action>
**В `first-screen-consultation-modal-1440.tsx`:**
1. Удалить строки 15-35 (4 экспорта types + default).
2. Добавить импорт сверху (рядом с другими импортами из `@/widgets/first-screen`):
   ```typescript
   import {
     type FirstScreenConsultationContactMethod,
     type FirstScreenConsultationFormState,
     type FirstScreenConsultationModalTitleVariant,
     defaultFirstScreenConsultationFormState,
   } from "@/widgets/first-screen/model/first-screen-consultation-form-state";
   ```
   (импорт `defaultFirstScreenConsultationFormState` нужен только если он используется внутри файла; на текущий момент modal-1440 его НЕ использует — это значит импортируем только types. Проверить grep'ом по файлу перед добавлением — лишний импорт не оставлять.)
3. **CRITICAL:** Чтобы внешние потребители не сломались мгновенно, временно добавить re-exports в конце файла:
   ```typescript
   // Re-exports для обратной совместимости. Удалить в Plan 07-04 после конверсии всех импортов.
   export type {
     FirstScreenConsultationContactMethod,
     FirstScreenConsultationFormState,
     FirstScreenConsultationModalTitleVariant,
   };
   export { defaultFirstScreenConsultationFormState };
   ```

**В `first-screen-consultation-modal-360.tsx`, `-480.tsx`, `-768.tsx`, `-1024.tsx`:**
Заменить `from "@/widgets/first-screen/ui/first-screen-consultation-modal-1440"` на `from "@/widgets/first-screen/model/first-screen-consultation-form-state"` в импорте types. Других изменений в этих файлах НЕ делать (они будут переписаны в Plan 07-03).

**В `first-screen.tsx` (строки 7-10):**
Заменить два импорта из `first-screen-consultation-modal-1440` на один из нового model-файла:
```typescript
import {
  defaultFirstScreenConsultationFormState,
  type FirstScreenConsultationModalTitleVariant,
} from "@/widgets/first-screen/model/first-screen-consultation-form-state";
```

**В `lead-form-fields.tsx` (строки 12-16):**
Заменить импорт из `first-screen-consultation-modal-1440` на тот же model-файл, сохранив форму:
```typescript
import {
  type FirstScreenConsultationContactMethod,
  type FirstScreenConsultationFormState,
  defaultFirstScreenConsultationFormState,
} from "@/widgets/first-screen/model/first-screen-consultation-form-state";
```

**Sanity check:**
- `grep -rn "from \"@/widgets/first-screen/ui/first-screen-consultation-modal-1440\"" src/` — должно остаться **только** в файлах, которые используют сам компонент (sections), но НЕ для импорта types/const.
- `grep -rn "FirstScreenConsultationContactMethod\\|FirstScreenConsultationFormState\\|defaultFirstScreenConsultationFormState" src/` — все импорты должны идти из `model/first-screen-consultation-form-state`, кроме самого этого файла и re-exports внутри modal-1440.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm exec playwright test tests/e2e/consultation-modal.spec.ts --reporter=line</automated>
  </verify>
  <done>
- TypeScript zero errors.
- E2E `consultation-modal.spec.ts` passing без правок.
- Все 13 callsite импортов модалок (`FirstScreenConsultationModalXXX` named exports) продолжают разрешаться.
- `git diff --stat` показывает изменения в 7 файлах + 1 новый файл.
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` — zero errors
2. `pnpm exec playwright test tests/e2e/consultation-modal.spec.ts` — passing
3. `pnpm exec playwright test tests/e2e/cross-browser.spec.ts` — passing (smoke render)
4. Manual: открыть любой breakpoint в dev, кликнуть CTA консультации — модалка открывается, форма работает (типы не сломаны).
</verification>

<success_criteria>
- Файл `first-screen-consultation-form-state.ts` существует и содержит 4 экспорта.
- Modal-1440 НЕ источник types больше (есть только re-exports для backwards compat — будут удалены в Plan 07-04).
- Никаких визуальных или поведенческих изменений в модалках (это чистый рефакторинг импортов).
- E2E + tsc проходят.
</success_criteria>

<output>
После выполнения создать `.planning/phases/07-modal-unification/07-01-SUMMARY.md` по шаблону. Зафиксировать:
- Список изменённых файлов
- Новые экспорты model-файла
- Что re-exports в modal-1440 будут удалены в Plan 07-04
</output>
