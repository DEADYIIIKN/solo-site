# Deferred items from Plan 07-04

## Pre-existing eslint issues (out of scope)

1. `src/app/(site)/privacy/page.tsx:69` — error: `<a>` to `/` should be `<Link />` from `next/link`. Pre-existing, не вносится этим планом.
2. `src/widgets/philosophy-clients/ui/philosophy-clients-narrow-stack.tsx:21` — warning: unused var `MARQUEE_GAP_360_PX`. Pre-existing.

Не фикшу в Plan 07-04 (scope boundary — Phase 7 = consultation modal unification only).
