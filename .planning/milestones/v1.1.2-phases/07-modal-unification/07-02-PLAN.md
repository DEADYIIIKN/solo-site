---
phase: 07-modal-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/widgets/first-screen/ui/consultation-modal-variants.ts
  - src/widgets/first-screen/ui/consultation-modal.tsx
autonomous: true
requirements:
  - REFAC-01
  - REFAC-02
must_haves:
  truths:
    - "Существует один файл consultation-modal.tsx, реализующий весь shared JSX + form logic + success-step animation"
    - "Существует exhaustive lookup table consultation-modal-variants.ts, покрывающая все 5 breakpoints (360/480/768/1024/1440)"
    - "ConsultationModal принимает variant prop ('360'|'480'|'768'|'1024'|'1440') и обязательные props (open, onClose, formState, setFormState, titleVariant?)"
    - "В base нет inline if/else по variant — все per-variant отличия читаются из lookup map"
    - "pnpm exec tsc --noEmit проходит (даже если ConsultationModal пока никем не импортируется — это OK, проверка типов и компиляция компонента)"
    - "E2E consultation-modal.spec.ts продолжает проходить (старые модалки ещё активны — этот plan их НЕ трогает)"
  artifacts:
    - path: "src/widgets/first-screen/ui/consultation-modal-variants.ts"
      provides: "Variant lookup table + ConsultationModalVariant type"
      contains: "export const consultationModalVariants"
    - path: "src/widgets/first-screen/ui/consultation-modal.tsx"
      provides: "Unified ConsultationModal component с variant prop"
      contains: "export function ConsultationModal"
  key_links:
    - from: "src/widgets/first-screen/ui/consultation-modal.tsx"
      to: "src/widgets/first-screen/ui/consultation-modal-variants.ts"
      via: "variant lookup при рендере"
      pattern: "consultationModalVariants\\[variant\\]"
    - from: "src/widgets/first-screen/ui/consultation-modal.tsx"
      to: "src/widgets/first-screen/model/first-screen-consultation-form-state.ts"
      via: "types import (от Plan 07-01)"
      pattern: "from \"@/widgets/first-screen/model/first-screen-consultation-form-state\""
---

<objective>
Создать **unified base component** `ConsultationModal` + variant lookup table. На этом этапе компонент существует параллельно с 5 старыми модалками — никаких callsite изменений ещё нет (это Plan 07-03).

**Purpose:** Per CONTEXT.md D1 — «один компонент + variant prop». Plan 07-02 материализует это решение: вся разметка/логика — в `consultation-modal.tsx`, все per-variant отличия (max-width, font-size, gap, padding, icon size, media query selector для visibility) — в exhaustive lookup table `consultation-modal-variants.ts`.

**Source of truth для разметки:** `first-screen-consultation-modal-1440.tsx` (наиболее полная — содержит form/leave/success step animation). 360 имеет упрощённый `isSuccess` boolean — в base используем **полный** successStep flow (form/leave/success) из 1440 для всех вариантов: D5 visual parity guard purified — анимация на 360 раньше отсутствовала (instant switch), теперь будет плавный переход. Если E2E spec ловит instant-switch на 360 как regression — обсудить в чекпоинт-чате revision; CONTEXT.md D5 явно допускает «pixel diff ≤ 1px» (поведение transition не pixel-метрика).

**Output:**
- `consultation-modal-variants.ts` — single source of truth для всех точечных отличий по 5 breakpoints.
- `consultation-modal.tsx` — exported `ConsultationModal({ variant, open, onClose, formState, setFormState, titleVariant }: Props)`.

**Соответствие locked decisions:** D1 (variant prop, не CSS-only), D2 (co-location в `src/widgets/first-screen/ui/`), D4 (submit shape unchanged — только перенос).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-modal-unification/07-CONTEXT.md
@.planning/phases/07-modal-unification/07-01-SUMMARY.md
@src/widgets/first-screen/ui/first-screen-consultation-modal-1440.tsx
@src/widgets/first-screen/ui/first-screen-consultation-modal-1024.tsx
@src/widgets/first-screen/ui/first-screen-consultation-modal-768.tsx
@src/widgets/first-screen/ui/first-screen-consultation-modal-480.tsx
@src/widgets/first-screen/ui/first-screen-consultation-modal-360.tsx
@src/widgets/first-screen/model/first-screen-consultation-form-state.ts

<interfaces>
<!-- Готовые types из Plan 07-01 -->

```typescript
// from src/widgets/first-screen/model/first-screen-consultation-form-state.ts
export type FirstScreenConsultationModalTitleVariant = "task" | "consultation";
export type FirstScreenConsultationContactMethod = "call" | "telegram" | "whatsapp";
export type FirstScreenConsultationFormState = {
  name: string;
  phone: string;
  message: string;
  contactMethod: FirstScreenConsultationContactMethod;
  consent: boolean;
};
export const defaultFirstScreenConsultationFormState: FirstScreenConsultationFormState;
```

<!-- Phone format helpers — те же что используют все 5 модалок -->

```typescript
// from src/widgets/first-screen/model/first-screen-consultation-form
export function formatConsultationPhone(input: string): string;
export function formatConsultationPhoneBackspace(phone: string, caret: number): string | null;
export function isConsultationPhoneValid(phone: string): boolean;
```

<!-- Public API нового компонента (создаётся в Plan 07-02) -->

```typescript
// src/widgets/first-screen/ui/consultation-modal-variants.ts
export type ConsultationModalVariant = "360" | "480" | "768" | "1024" | "1440";

export type ConsultationModalVariantConfig = {
  /** media query selector для визиблити (тот, что был в hidden ... block ... классе layer'а) */
  layerVisibility: string;          // e.g. "max-[479px]:block" / "min-[480px]:block min-[768px]:hidden" / "min-[1440px]:block"
  /** макс. ширина контент-колонки (form card + close button) */
  maxWidth: string;                 // e.g. "max-w-[360px]" / "max-w-[686px]"
  /** ширина при success step (1440 даёт max-w-[490px]) */
  maxWidthSuccess: string;
  /** Outer wrapper padding (px-X py-Y) */
  outerPadding: string;             // e.g. "px-[16px] py-0" / "px-4 py-2"
  /** gap между крестиком и карточкой */
  columnGap: string;                // e.g. "gap-[10px]" / "gap-[20px]"
  /** ContactMethod layout: радио-чипы (1440/1024/768) или pill-сегменты (480/360) */
  contactLayout: "radio" | "pill";
  /** form card padding */
  cardPadding: string;
  /** form card gap */
  cardGap: string;
  /** размер крестика */
  closeIconSize: 24 | 34;
  /** font-size заголовка form (число px — заголовок оборачиваем нашим JSX) */
  titleSize: number;                // 23 | 28 | 32 | 40 ...
  /** font-size body input/textarea */
  inputTextSize: string;            // "text-[12px]" / "text-[16px]" ...
  /** message textarea height */
  messageHeight: string;            // "h-[80px]" / "h-[100px]"
  /** submit button height + font-size */
  submitButtonClass: string;        // "h-[44px]" / "h-[60px]"
  submitFontSize: number;           // 13 | 17
  /** success card body sizes */
  successTitleSize: number;         // 23 | 32
  successBodySize: number;
  /** field gaps */
  contactQSize: string;
  consentTextSize: string;
  // ... любые другие отличия выясняются при diff'е 5 файлов
};

export const consultationModalVariants: Record<ConsultationModalVariant, ConsultationModalVariantConfig>;
```

```typescript
// src/widgets/first-screen/ui/consultation-modal.tsx
export type ConsultationModalProps = {
  variant: ConsultationModalVariant;
  open: boolean;
  onClose: () => void;
  formState: FirstScreenConsultationFormState;
  setFormState: Dispatch<SetStateAction<FirstScreenConsultationFormState>>;
  titleVariant?: FirstScreenConsultationModalTitleVariant;
};

export function ConsultationModal(props: ConsultationModalProps): JSX.Element | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать exhaustive variant lookup table</name>
  <files>src/widgets/first-screen/ui/consultation-modal-variants.ts</files>
  <action>
Создать файл `src/widgets/first-screen/ui/consultation-modal-variants.ts`.

**Метод сбора:** прочитать все 5 файлов `first-screen-consultation-modal-{360,480,768,1024,1440}.tsx` и выписать каждое точечное отличие в таблицу. Поля variant config (см. interfaces выше) дополнить ровно тем, что РАЗЛИЧАЕТСЯ между файлами — не добавлять поля для констант, общих для всех 5.

**Обязательно собрать как минимум следующие отличия (известные из scout-таблицы CONTEXT.md):**

| Отличие | 360 | 480 | 768 | 1024 | 1440 |
|---|---|---|---|---|---|
| layer visibility class | `max-[479px]:block` | `min-[480px]:block min-[768px]:hidden` | `min-[768px]:block min-[1024px]:hidden` | `min-[1024px]:block min-[1440px]:hidden` | `min-[1440px]:block` |
| close icon size | 24 | 24 | 34 | 34 | 34 |
| max-width column | 360 | 384/aналогичный — verify | (verify) | (verify) | 686 |
| max-width success | (verify) | ... | ... | ... | 490 |
| contact layout | pill | pill | radio | radio | radio |
| outer padding | px-16 py-0 | (verify) | ... | ... | px-4 py-2 |
| column gap | 10px | (verify) | ... | ... | 20px |
| title font-size | 23 | (verify) | ... | ... | 40 |
| input text size | text-12 | text-12 | text-14 | text-16 | text-16 |
| message height | 80 | 80 | 100 | 100 | 100 |
| submit btn h | 44 | (verify) | (verify) | (verify) | 60 |
| submit font-size | 13 | (verify) | ... | ... | 17 |
| card padding | px-16 py-24 | (verify) | ... | ... | p-30 |
| card gap | 30 | (verify) | ... | ... | 36 |
| consent text size | 11 | 11 | (verify) | (verify) | 16 |

**(verify)** = читать файлы и подтвердить точные значения. Никаких догадок: variant table должна **точно** воспроизводить классы каждой из 5 модалок, иначе E2E + Figma sverka сломаются.

**Также определить и заэкспортить:**
- `export type ConsultationModalVariant = "360" | "480" | "768" | "1024" | "1440";`
- `export type ConsultationModalVariantConfig` (см. interfaces — расширить полями по факту diff'а)
- `export const consultationModalVariants: Record<ConsultationModalVariant, ConsultationModalVariantConfig>` — заполненная таблица

**Tailwind safelist sanity:** все классы — статические строки в коде (не динамическая интерполяция). Tailwind 4 JIT их подхватит. Если решено использовать template literal (`text-[${size}px]`) — добавить в safelist в `tailwind.config` (НЕ предпочитаемый путь).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; node -e "const m = require('./src/widgets/first-screen/ui/consultation-modal-variants.ts'); const expected = ['360','480','768','1024','1440']; expected.forEach(v =&gt; { if (!m.consultationModalVariants?.[v]) throw new Error('Missing variant ' + v); });" 2&gt;&amp;1 || pnpm exec tsx -e "import { consultationModalVariants } from './src/widgets/first-screen/ui/consultation-modal-variants'; const expected = ['360','480','768','1024','1440'] as const; expected.forEach(v =&gt; { if (!consultationModalVariants[v]) throw new Error('Missing variant ' + v); });"</automated>
  </verify>
  <done>Файл существует, экспорты ConsultationModalVariant + consultationModalVariants определены, все 5 ключей присутствуют, tsc проходит.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Создать unified ConsultationModal component</name>
  <files>src/widgets/first-screen/ui/consultation-modal.tsx</files>
  <behavior>
- Компонент принимает `variant` + 4 обязательных prop'а (open, onClose, formState, setFormState) + опциональный titleVariant.
- Layer (overlay) рендерится с `consultationModalVariants[variant].layerVisibility` в className — поэтому только одна модалка визуально активна на каждом breakpoint (поведение строго совпадает со старыми 5 файлами).
- Form / leave / success animation step реализован (полный flow из 1440), таймеры MODAL_TRANSITION_MS=320, FORM_SUCCESS_LEAVE_MS=380, FORM_SUCCESS_ENTER_MS=400, ease cubic-bezier(0.22,1,0.36,1).
- Submit handler: setSubmitAttempted(true) → если name/phone/consent невалидны, return; иначе setSuccessStep("leave"). **Это shape строго как сейчас в modal-1440 (D4: submit shape unchanged).** TODO для реальной отправки появится в Phase 8 (FUNC-01).
- contactLayout="radio": рендерит 3 `<label>` с круглыми индикаторами (см. modal-1440 строки 410-449); contactLayout="pill": рендерит 3 `<button aria-pressed>` с border-color/bg switching (см. modal-360 строки 365-389).
- data-testid: `consultation-modal-close`, `consultation-modal-card`, `consultation-modal-name`, `consultation-modal-phone`, `consultation-modal-message`, `consultation-modal-consent`, `consultation-modal-submit` — **все обязательны** (E2E spec их использует).
- createPortal в document.body, server-side guard `if (typeof document === "undefined") return null;`.
  </behavior>
  <action>
Создать `src/widgets/first-screen/ui/consultation-modal.tsx`. Начать со скелета на базе `first-screen-consultation-modal-1440.tsx` (наиболее полный — содержит successStep flow), затем:

1. **Top-level imports:**
   - React: `Dispatch, SetStateAction, TransitionEvent, useCallback, useEffect, useId, useRef, useState` + `createPortal` из `react-dom`.
   - `cn` из `@/shared/lib/utils`
   - phone helpers из `@/widgets/first-screen/model/first-screen-consultation-form`
   - types + default из `@/widgets/first-screen/model/first-screen-consultation-form-state`
   - variants из `./consultation-modal-variants`
   - `"use client";` директива в первой строке файла.

2. **Component signature:**
```typescript
export function ConsultationModal({
  variant,
  open,
  onClose,
  formState,
  setFormState,
  titleVariant = "task",
}: ConsultationModalProps) {
  const config = consultationModalVariants[variant];
  // ... rest
}
```

3. **State + effects:** скопировать ровно из modal-1440 (titleId, successTitleId, shouldRender, isEntered, exitUnmountTimerRef, submitAttempted, successStep, successEntered, leaveTimerRef + все useEffect блоки). Никаких изменений в логике.

4. **Layer JSX:** перенести из modal-1440 строки 231-565, заменив:
   - `min-[1440px]:block` → `${config.layerVisibility}`
   - hard-coded max-w-[490px]/max-w-[686px] → `config.maxWidthSuccess` / `config.maxWidth`
   - hard-coded outer padding → `config.outerPadding`
   - hard-coded gap-[20px] (column) → `config.columnGap`
   - CloseIcon: вынести **в** этот же файл два варианта `CloseIcon24` и `CloseIcon34` (точно как в modal-360 / modal-1440), рендерить условно по `config.closeIconSize`. Размер класса (`size-[24px]` / `size-[34px]`) — также по конфигу.
   - hard-coded form card padding/gap → `config.cardPadding` / `config.cardGap`
   - hard-coded title text-[40px] → `config.titleSize` (через inline style `fontSize: config.titleSize`).
   - hard-coded input text-[16px] → `config.inputTextSize`.
   - hard-coded message-height → `config.messageHeight`.
   - hard-coded submit button h-[60px] / fontSize 17 → `config.submitButtonClass` / `config.submitFontSize`.
   - hard-coded success title text-[32px] / body text-[17px] → `config.successTitleSize` / `config.successBodySize`.
   - hard-coded consent text-[16px] → `config.consentTextSize`.

5. **ContactMethod section:** условно рендерить `radio` vs `pill` по `config.contactLayout`. Два разных JSX блока (см. modal-1440 строки 410-449 для radio и modal-360 строки 365-389 для pill), shared только `contactOptions` массив (`call/telegram/whatsapp`). Сам массив можно объявить module-level const как в существующих файлах.

6. **Title JSX:** заголовок имеет 2 варианта (`task` vs `consultation`). Для variant=`360`/`480` заголовок разделён на 3 строки `<span class="block">` (см. modal-360 строки 258-279); для остальных — inline (см. modal-1440 строки 310-322). Это контролируется ещё одним полем variant config: `titleLayout: "inline" | "stacked"`. Добавить в variant table.

7. **Submit handler:** ровно как в modal-1440 строки 294-304 (preventDefault → setSubmitAttempted → guard → setSuccessStep("leave")). НЕ менять.

8. **CSS class assembly:** использовать `cn()` для всех условных классов. Никаких inline `if (variant === "360") {...}` — только лукапы из `config`.

**Sanity:** запустить `pnpm exec tsc --noEmit` после написания. Компонент пока никем не используется — TS должен компилироваться, lints должны быть чистыми.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm exec eslint src/widgets/first-screen/ui/consultation-modal.tsx src/widgets/first-screen/ui/consultation-modal-variants.ts &amp;&amp; pnpm exec playwright test tests/e2e/consultation-modal.spec.ts --reporter=line</automated>
  </verify>
  <done>
- `consultation-modal.tsx` существует и компилируется.
- ESLint без ошибок.
- E2E старых модалок продолжает проходить (этот plan их НЕ трогает — sanity check, что мы ничего не сломали).
- Все 7 data-testid'ов присутствуют в JSX (grep на файл).
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` — zero errors.
2. `pnpm exec eslint src/widgets/first-screen/ui/consultation-modal*.{ts,tsx}` — clean.
3. `pnpm exec playwright test tests/e2e/consultation-modal.spec.ts` — passing (старые модалки ещё работают, base пока bundle-dead-code).
4. `grep -c "data-testid=\"consultation-modal-" src/widgets/first-screen/ui/consultation-modal.tsx` — должно быть >= 7.
5. `grep -c "if (variant ===" src/widgets/first-screen/ui/consultation-modal.tsx` — должно быть 0 (все variant отличия читаются из lookup map).
</verification>

<success_criteria>
- `consultation-modal-variants.ts` exhaustive — ВСЕ 5 ключей, ВСЕ точечные отличия.
- `consultation-modal.tsx` — single source of truth для разметки и логики, без inline if/else по variant.
- ConsultationModal **не используется** ещё нигде — это Plan 07-03.
- E2E + tsc + eslint clean.
</success_criteria>

<output>
После выполнения создать `.planning/phases/07-modal-unification/07-02-SUMMARY.md`. Зафиксировать:
- Финальный список полей `ConsultationModalVariantConfig`
- Любые отличия между 5 файлами, которые НЕ удалось унифицировать (если есть — сигнал риску для Plan 07-03)
- Поведенческое отличие: успех на 360/480 теперь анимированный (как 1440), а не instant — Plan 07-03 валидация через E2E.
</output>
