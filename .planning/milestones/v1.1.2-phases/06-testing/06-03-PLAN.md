---
phase: 06-testing
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - package.json
  - vitest.config.ts
  - tests/unit/lead-form-validation.test.ts
  - tests/unit/phone-format.test.ts
  - tests/unit/use-in-view-once.test.ts
autonomous: false
requirements:
  - TEST-03

must_haves:
  truths:
    - "Vitest установлен и настроен (vitest.config.ts с jsdom env)"
    - "Скрипт `npm run test:unit` запускает Vitest"
    - "tests/unit/phone-format.test.ts: формат phone input (форматирование at type, backspace handling)"
    - "tests/unit/lead-form-validation.test.ts: submit guard logic (required fields, consent checked)"
    - "tests/unit/use-in-view-once.test.ts: animation hook fires once and not refires"
    - "Все тесты проходят (`npm run test:unit` exit 0)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config — jsdom env, tsx support"
      contains: "environment.*jsdom"
    - path: "tests/unit/phone-format.test.ts"
      provides: "Unit tests для phone formatting"
      contains: "describe.*phone"
  key_links:
    - from: "tests/unit/*.test.ts"
      to: "src/widgets/lead-form/lib/phone-format.ts (или эквивалент)"
      via: "import"
      pattern: "from \"@/widgets"
---

<objective>
Setup Vitest + jsdom для unit-тестов. Покрыть phone formatting, form validation logic, animation hook (`useInViewOnce`).

Purpose: Закрыть TEST-03 — unit coverage критичных функций без браузера.
Output:
- vitest.config.ts с jsdom environment
- 3 unit-spec файла
- npm script `test:unit`
</objective>

<context>
@.planning/PROJECT.md
</context>

<tasks>
1. **Install vitest + jsdom** — `npm install -D vitest jsdom @vitest/ui @testing-library/react @testing-library/jest-dom`.
2. **Create vitest.config.ts** — environment jsdom, alias @ → src, setupFiles если нужно.
3. **Add npm script** — `test:unit`, `test:unit:watch`, `test:unit:ui`.
4. **Phone format test** — input "9", "+7 ", typing handler, backspace clears formatting. Cover edge cases: paste, partial input.
5. **Form validation test** — submit guard returns true только при name+phone+consent. Each field invalid → false.
6. **useInViewOnce hook test** — IntersectionObserver mock: fires inView=true once, doesn't refire on re-enter.
7. **Run `npm run test:unit`** — verify pass.
</tasks>
