---
phase: 03-mobile-tablet-layout-820px-360px
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/cases/ui/cases-section-shared-ui.tsx
  - src/widgets/team/ui/team-section-360.tsx
autonomous: false
requirements:
  - LY820-01
  - LY360-01
  - LY360-03

must_haves:
  truths:
    - "Кнопка «Назад» (aria-label=Назад) в карусели кейсов показывает левый шеврон (←) на всех брейкпоинтах"
    - "Кнопка «Вперёд» (aria-label=Вперёд) показывает правый шеврон (→) на всех брейкпоинтах"
    - "Фото команды видно в секции «что мы делаем» при viewport 360px"
  artifacts:
    - path: "src/widgets/cases/ui/cases-section-shared-ui.tsx"
      provides: "CasesNavArrowIcon с исправленным тернарным выражением — back=LEFT, forward=RIGHT"
      contains: "variant === \"back\" ? CASES_ARROW_PATH_FIGMA_LEFT_FILE : CASES_ARROW_PATH_FIGMA_RIGHT_FILE"
  key_links:
    - from: "CasesSectionArrowsNav"
      to: "CasesNavArrowIcon"
      via: "variant prop"
      pattern: "variant === .back"
---

<objective>
Исправить два быстрых бага без Figma MCP: перепутанные стрелки карусели кейсов (LY820-01, LY360-01) и проверить/восстановить фото команды на 360px (LY360-03).

Purpose: Стрелки показывают неправильное направление из-за перепутанных SVG-путей в строке 24 cases-section-shared-ui.tsx. Один фикс покрывает все брейкпоинты.
Output: Файл cases-section-shared-ui.tsx с исправленным тернарным выражением; LY360-03 закрыт (resolved или исправлен).
</objective>

<execution_context>
@/Users/a_savinkov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a_savinkov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-CONTEXT.md
@.planning/phases/03-mobile-tablet-layout-820px-360px/03-UI-SPEC.md
</context>

<interfaces>
<!-- Извлечено из cases-section-shared-ui.tsx при планировании -->

Строки 12-15 — SVG-пути (имена константных переменных):
```tsx
const CASES_ARROW_PATH_FIGMA_LEFT_FILE =
  "M21.7036 16.3649C22.0432 16.7347 ...";   // ← left-pointing chevron
const CASES_ARROW_PATH_FIGMA_RIGHT_FILE =
  "M12.2964 17.6351C11.9568 17.2653 ...";   // → right-pointing chevron
```

Строка 24 — ТЕКУЩИЙ (СЛОМАННЫЙ) код:
```tsx
const d =
  variant === "back" ? CASES_ARROW_PATH_FIGMA_RIGHT_FILE : CASES_ARROW_PATH_FIGMA_LEFT_FILE;
```

Строка 24 — ИСПРАВЛЕННЫЙ код:
```tsx
const d =
  variant === "back" ? CASES_ARROW_PATH_FIGMA_LEFT_FILE : CASES_ARROW_PATH_FIGMA_RIGHT_FILE;
```

Компонент используется во ВСЕХ breakpoint-файлах (360, 480, 768, 1024, 1440).
Один фикс в shared-ui покрывает все.

team-section-360.tsx строки 44-56 — TeamSectionPhoto с variant="narrow":
```tsx
<TeamSectionPhoto
  className={...}
  frameClassName="h-[204px] w-full"
  imageWrapperClassName="inset-0 w-full"
  imageClassName="object-cover object-center"
  roundedClassName="rounded-[8px]"
  variant="narrow"
/>
```
TeamSectionPhoto использует teamSectionAssets.teamPhoto — обновлён в Phase 2.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Исправить направление стрелок карусели кейсов (per D-01, D-02)</name>
  <files>src/widgets/cases/ui/cases-section-shared-ui.tsx</files>
  <read_first>
    - src/widgets/cases/ui/cases-section-shared-ui.tsx (весь файл — 92 строки, читать полностью)
  </read_first>
  <action>
Найти строку 24 в функции CasesNavArrowIcon:

СЛОМАННЫЙ КОД (текущий):
```tsx
const d =
  variant === "back" ? CASES_ARROW_PATH_FIGMA_RIGHT_FILE : CASES_ARROW_PATH_FIGMA_LEFT_FILE;
```

Заменить на ИСПРАВЛЕННЫЙ КОД (своп двух константных имён):
```tsx
const d =
  variant === "back" ? CASES_ARROW_PATH_FIGMA_LEFT_FILE : CASES_ARROW_PATH_FIGMA_RIGHT_FILE;
```

Больше ничего не трогать. Файл не переформатировать.

Мнемоника проверки: "back" = ← = LEFT, "forward" = → = RIGHT.
CASES_ARROW_PATH_FIGMA_LEFT_FILE — это действительно левый шеврон (начинается с M21.7036, изгибается влево).
CASES_ARROW_PATH_FIGMA_RIGHT_FILE — это правый шеврон (начинается с M12.2964, изгибается вправо).
  </action>
  <verify>
    <automated>grep -n "variant === .back. ? CASES_ARROW_PATH_FIGMA_LEFT_FILE" /Users/a_savinkov/solo-site/src/widgets/cases/ui/cases-section-shared-ui.tsx</automated>
    Ожидаемый вывод: строка с `variant === "back" ? CASES_ARROW_PATH_FIGMA_LEFT_FILE : CASES_ARROW_PATH_FIGMA_RIGHT_FILE`
    Дополнительно: `npx tsc --noEmit 2>&1 | head -20` — без ошибок
  </verify>
  <acceptance_criteria>
    - grep находит `CASES_ARROW_PATH_FIGMA_LEFT_FILE` после `variant === "back" ?` в строке ~24
    - grep НЕ находит `CASES_ARROW_PATH_FIGMA_RIGHT_FILE` после `variant === "back" ?`
    - TypeScript компилируется без ошибок (`npx tsc --noEmit` выходит с кодом 0)
    - Файл содержит ровно 2 упоминания CASES_ARROW_PATH_FIGMA_LEFT_FILE и 2 упоминания CASES_ARROW_PATH_FIGMA_RIGHT_FILE (объявления + использование)
  </acceptance_criteria>
  <done>
    В cases-section-shared-ui.tsx строка ~24 содержит: `variant === "back" ? CASES_ARROW_PATH_FIGMA_LEFT_FILE : CASES_ARROW_PATH_FIGMA_RIGHT_FILE`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Исправлены стрелки карусели кейсов (1-строчный своп). Нужно визуально подтвердить корректность на трёх брейкпоинтах.
    Фото команды 360px — проверить в браузере (возможно уже исправлено в Phase 2).
  </what-built>
  <how-to-verify>
    1. Запустить `npm run dev` (если не запущен)
    2. Открыть DevTools → выбрать viewport 820px
    3. Найти секцию кейсов → убедиться что левая стрелка (←) ведёт назад, правая (→) — вперёд
    4. Переключить viewport на 360px → та же проверка в секции кейсов
    5. Переключить viewport на 1440px → убедиться что стрелки на 1440px также корректны (регрессия не появилась)
    6. НА 360px: прокрутить до секции «что мы делаем» (team section) → фото команды ДОЛЖНО быть видно
       - Если фото ВИДНО: LY360-03 resolved, отметить это в SUMMARY
       - Если фото НЕ ВИДНО: описать что видно (пустое место / skeleton / broken image) и сообщить в resume-signal
  </how-to-verify>
  <resume-signal>
    Напишите одно из:
    - "approved" — стрелки корректны на всех брейкпоинтах, фото команды видно на 360px
    - "arrows ok, photo missing: [описание]" — стрелки верны, но фото пропало (нужен дополнительный фикс)
    - "issue: [описание проблемы]" — если что-то не так
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет внешних входов | Изменение SVG-константы в UI-компоненте |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | cases-section-shared-ui.tsx | accept | Единственный риск — случайно поменять не ту строку. Acceptance criteria с grep предотвращает это. |
</threat_model>

<verification>
1. `grep -n "variant === .back. ? CASES_ARROW_PATH_FIGMA_LEFT_FILE"` — находит совпадение
2. `npx tsc --noEmit` — нет ошибок TypeScript
3. Визуальная проверка: стрелка «Назад» = ←, стрелка «Вперёд» = → на 820px, 360px, 1440px
4. Фото команды видно на 360px
</verification>

<success_criteria>
- cases-section-shared-ui.tsx строка ~24: `variant === "back" ? CASES_ARROW_PATH_FIGMA_LEFT_FILE : CASES_ARROW_PATH_FIGMA_RIGHT_FILE`
- TypeScript без ошибок
- Стрелки на 820px, 360px и 1440px направлены корректно
- LY360-03 закрыт (resolved или исправлен — задокументировать в SUMMARY)
</success_criteria>

<output>
После завершения создать `.planning/phases/03-mobile-tablet-layout-820px-360px/03-01-SUMMARY.md`
</output>
